# Owner workspace — observed gaps

> ## This is NOT the Phase 2 scope. It was published as such, and that was wrong.
>
> This file was committed as `PHASE-2-OWNER-WORKSPACE-REGISTER.md` claiming to be the scope of the plan's Phase 2, "Complete owner workspace". **It is not.** The real specification is `phase-prompts/02-COMPLETE-OWNER-WORKSPACE.md` and Master Plan §5 (P2.1–P2.5), both of which were sitting untracked in the parent checkout while this survey ran. They are now committed alongside this file.
>
> The scope below was *derived* from CLAUDE.md §2.4 and §3.1 because `PHASE-1-REPORT.md` §0 said the kit "does not exist in the repository" — a statement taken as settled for a whole session instead of being checked one directory up. The derivation cost a 75-agent, 13.5M-token survey and produced a list that **substantially disagrees with the actual phase**.
>
> **What the real Phase 2 requires that this list missed entirely:**
> - **P2.1** — simplify navigation to Today / Tasks / Create / Results / More, one primary action per state
> - **P2.2** — make three workflows genuinely complete: review replies, FAQ + JSON-LD, website basics (with `gbp-profile-fix` staying a checklist and `photo_brief` a written shot list, explicitly *not* AI-substituted)
> - **P2.3** — reuse stored business context; show a "business details used" summary; stop re-asking known facts
> - **P2.4** — an operated assisted-ownership route for no-GBP/manual-entry businesses: request → acknowledgement → authorized operator queue in the current Neon app → evidence-based decision → atomic, conflict-aware assignment or visible refusal
> - **P2.5** — one transactional-email abstraction, a real invitation flow, notification mark-as-read, and the missing host audit events
>
> **What this list contains that is *not* Phase 2 at all:** multi-location (absent from the plan), asset downloads, activity paging, Stripe portal edge cases, and most of the medium tier. Several items belong to **Phase 3** (re-scan and proof of change, billing/allowance reconciliation, the single job lifecycle and scheduler) or **Phase 4**.
>
> **Roughly three of the 51 overlap with real Phase 2 work** — notification mark-as-read (P2.5), and parts of workflow completeness (P2.2).
>
> ## Why it is kept
>
> The 51 entries are verified observations with `file:line` evidence, each adversarially checked. They are a legitimate backlog of places the owner journey breaks down — they are simply not this phase's brief. Use them as candidate input to the correct phase, after reconciling each against the real plan. **Do not treat this document as a scope of work.**

---

Produced by a 14-surface survey of the owner workspace against the product loop in CLAUDE.md §2.4 (`Discover → Diagnose → Prioritise → Draft → Approve → Export/Publish → Re-scan → Prove change`) and the route map in §3.1. One read-only agent per surface traced what an owner can actually finish in-product; every claimed gap then went to an adversarial verifier told to refute it by default. No agent could edit a file.

**Read the caveat first.** 61 gaps were claimed and **51 confirmed, 10 refuted** — an 84% confirmation rate. That is high enough to be worth distrusting: a verification pass that refutes little is equally consistent with weak verification. Several entries below also overlap (Insights never reading `action_measurements` was found independently by two surfaces). Treat severities as claims to check, not conclusions.

**Not repeated here.** The 24 findings of `PHASE-2-GAP-REGISTER.md` (plus P1–P3) are all fixed and CI-verified. That register is about promises the product made and could not keep; this one is about jobs the owner cannot finish.

**Constraints that shaped the list.** A gap requiring any of the following is recorded as **BLOCKED** rather than as work: crons in `vercel.json` or a second scheduler (§7); writing to `agent_runs` from this app's agents (§3.7, so no Fix Pack generation); publishing connectors (guardrail 6); `OWNER_SELF_SERVICE_CLAIM` (guardrail 15); and schema changes, since `scripts/neon/catalog.ts` deep-equals a frozen catalog and any migration fails `db:verify`.

**Totals:** 8 high · 37 medium · 6 low · 51 confirmed.

### High

#### 1. Ownership proven, onboarding abandoned: the workspace can never be finished or reached again

**Surface:** Onboarding and claim · **Type:** `dead_end`

**Evidence**

The OAuth claim callback creates the workspace, the owner membership and attaches the job BEFORE onboarding steps 3-4 run (app/api/oauth/google/claim/callback/route.ts:131-144), then redirects to /{locale}/owner/onboarding?claim=<slug> (route.ts:55-59). But `POST /api/workspaces/claim` -> completeWorkspaceClaim is the ONLY code path in the repo that ever inserts a `locations` row (lib/workspace/claim.ts:216-224 calling lib/repositories/claims.ts:149-150; grep for insertLocation / 'INSERT INTO locations' returns no other caller). If the owner closes the tab at step 3/4 -- or the completion POST 503s/429s (app/api/workspaces/claim/route.ts:38-39 fails closed on the rate limiter, :69-72 on any error) and they come back later -- the flow cannot be resumed: app/[locale]/owner/onboarding/page.tsx:131 loads claim evidence ONLY from `?claim=`, and without it the page renders the 'No report to claim / Start from a scan' state (components/onboarding-page.tsx:228). Nothing else in the product links to /owner/onboarding (only app/api/oauth/google/claim/callback/route.ts:55 and lib/identity/complete-sign-in-ports.ts:58 produce that URL), and a returning sign-in with no claim slug goes to select-workspace instead (lib/identity/complete-sign-in.ts:112-117). There the workspace shows as '0 locations' and still links into the shell (components/select-workspace-page.tsx:61-65); lib/workspace/page-context.ts:41 with lib/workspace/shell.ts:58-60 falls back to `all`, so getHomeBrief resolves no location (lib/workspace/queries-pages.ts:388-392) and Home renders an empty brief badged 'No scan yet' (components/workspace/home-brief.tsx:180) with no explanation and no repair control.

**Owner impact**

The merchant did the hard part -- proved ownership to Google -- and is left owning a permanently empty workspace: no location means no snapshot, no derived actions, no rescan target, no insights, and there is no control anywhere in the product to finish setup or add a location. Their only recovery is to still possess the exact ?claim=<share_slug> URL, which nothing in the app shows them again.

**Verifier**

CONFIRMED. The owner's path ends at components/workspace/home-brief.tsx:180 — an owner-role member of a workspace with zero locations lands on Home (via components/select-workspace-page.tsx:64-65 → lib/workspace/page-context.ts:41 + lib/workspace/shell.ts:58-60 falling back to `all` → lib/workspace/queries-pages.ts:388-392 resolving no location) and sees a badge reading "No scan yet" with no explanation and no repair control. They are left unable to create the primary location, and therefore unable to ever get a snapshot, derived actions, a rescan target, insights or a measurement.

Chain verified line by line:
1. app/api/oauth/google/claim/callback/route.ts:131-144 creates the workspace and owner membership and attaches the job; lib/repositories/claims.ts:30-39 (createWorkspaceWithOwner) inserts ONLY `workspaces` and `workspace_members` — no location. It then redirects to /{locale}/owner/onboarding?claim=<slug> (route.ts:55-59).
2. lib/workspace/claim.ts:216-224 calling lib/repositories/claims.ts:149-150 is the only INSERT INTO locations in the repo (grep insertLocation: one definition, one caller). Its only route is POST /api/workspaces/claim; that route's only client caller is components/onboarding-page.tsx:187, which sends `claim_slug: evidence.shareSlug`.
3. app/[locale]/owner/onboarding/page.tsx:131 loads evidence ONLY from `?claim=`; without it, owned=false and components/onboarding-page.tsx:228 renders "No report to claim / Start from a scan". The database-derived resume (page.tsx:141, resumeStep = !owned ? 1 : saved?.hasLocation ? 4 : 3) is real but unreachable without the parameter.
4. The decisive detail the claim only half-states: the sole in-product link that regenerates `?claim=` is the report's claim hand-off at components/report/dashboard-summary.tsx:63-70, gated on `report.access === "viewer"`, and lib/report-access/authorize-report.ts:90,105-111 prefers an accepted membership over the viewer grant. Proving ownership is precisely what hides the only link back. No other surface shows the share slug: grep for "/r/" in components/ hits only scanning-page.tsx and onboarding-page.tsx; lib/workspace/queries.ts:255 latestWorkspaceReport would return it but has no production caller (its own comment says it was superseded in Phase 3).
5. A returning sign-in with no claim goes to select-workspace (lib/identity/complete-sign-in.ts:112-121). No locations page or API exists anywhere (app/[locale]/owner/** and app/api/workspaces/** contain none; components/workspace/more-view.tsx:12 "Locations" links back to select-workspace).

Refutation attempts that failed: (a) Not one of the 24 fixed findings in docs/implementation/owner-platform-v1/PHASE-2-GAP-REGISTER.md:19-105 — finding 15's fix actually depends on the owner still holding this URL ("return to this page and the remaining steps unlock"). (b) A recovery does technically exist — run a brand-new scan, whose job has workspace_id NULL, so its report renders access === "viewer" and the claim CTA reappears, and the callback attaches it to the existing owned workspace — but it costs a fresh paid scan plus a second Google consent, is signposted nowhere, and is unavailable in the shipped default (WORKSPACE_CLAIM_VIA_OAUTH_ENABLED=false), where re-entry depends on a human re-sending the URL. That is a workaround, not a control. (c) Not forbidden by the hard constraints: a fix is a read plus a link (e.g. surface the already-written-but-uncalled latestWorkspaceReport on select-workspace/Home when a workspace has no location), requiring no migration, cron, second scheduler, agent_runs write, publishing connector, self-service claim, provider call or deployment.

Classification dead_end stands (the owner's journey terminates inside the product with no onward control); it is equally describable as a missing control, but the claimed label is defensible as written.

#### 2. Rescan blocks on the entire scan before revealing the job, and no workspace surface ever shows a scan in flight

**Surface:** Owner Home · **Type:** `dead_end`

**Evidence**

Home renders the Rescan control at components/workspace/home-brief.tsx:70. components/workspace/rescan-button.tsx:49-53 does `const result = await rescanLocation(...)` and only then `router.push('/{locale}/scanning/{jobId}')`. lib/workspace/client.ts:210-224 awaits BOTH calls: the enqueue, then `await post("/api/scan/process", { jobId })`. That second call is the scan itself: app/api/scan/process/route.ts:11 sets `maxDuration = 300` and :61 runs `await runScan(jobId, session.id)` inline; lib/scan/dispatch-runtime.ts:6-11 hard-returns "vercel" and :13-16 makes dispatchToScanWorker always false, so there is no hand-off path that returns early. Meanwhile the Home read model never looks at job state: getHomeBrief (lib/workspace/queries-pages.ts:386-452) reads scan_snapshots, scan_diffs, actions, measurements, draft versions and scan_schedules only, never audit_jobs.status. The one repository read of job status, lib/repositories/workspace-read.ts:63-66 `latestReport`, is reached only by lib/workspace/queries.ts:255 `latestWorkspaceReport`, which has no caller anywhere in the app. A grep of app/[locale]/owner/** and components/workspace/** finds exactly one reference to /scanning/, the push at rescan-button.tsx:53. Re-queueing is unguarded: app/api/workspaces/[workspaceId]/rescan/route.ts:71-84 and lib/workspace/rescan.ts:131-160 look up the latest *finished* job and insert a new queued row without checking whether a non-terminal job already exists for that location.

**Owner impact**

The owner clicks Rescan on Home and gets a disabled spinner with no reference number, no toast and no progress for up to the 300 s platform ceiling; only after that does the browser reach the progress page. If they navigate away, switch location, or close the tab inside that window — the natural thing to do when a button appears to hang — the job id is gone permanently. Home still reads "Snapshot · <old date>", there is no in-flight card, no scan reference, and nothing in the workspace links back to /scanning/[jobId], so the owner cannot resume it. The job is left holding a claim_audit_job lease with no cron or reaper in this app to reclaim it, so one of the workspace's three daily paid rescans is consumed and never produces a snapshot. Worse, the Rescan button is immediately enabled again and the route does not refuse a location that is already scanning, so the honest recovery move — press it again — queues a second paid scan.

**Verifier**

Confirmed at components/workspace/rescan-button.tsx:53 — the push to /scanning/{jobId} is the owner's only route to a running scan, and it sits behind `await rescanLocation(...)` (:49), which awaits both the enqueue and `await post("/api/scan/process", { jobId })` (lib/workspace/client.ts:222). That POST runs the scan inline: app/api/scan/process/route.ts:11 sets maxDuration = 300 and :61 `await runScan(jobId, session.id)`; lib/scan/run.ts:63-90 executes processScan plus website checks plus completeWorkspaceScan synchronously; lib/scan/dispatch-runtime.ts:6-11 hard-returns "vercel" and :13-16 returns false, so the route's early-return branch (:49-57) is dead. lib/workspace/client.ts request() (:48-67) has no timeout or abort, so it settles only with the response. The funnel proves this is a divergence rather than a documented pattern: components/scan-page.tsx:227 navigates immediately and components/scanning-page.tsx:124-133 fires the same process POST fire-and-forget because "the poll below is the single source of truth". No workspace surface compensates: getHomeBrief (lib/workspace/queries-pages.ts:386-452) reads snapshots, diffs, actions, measurements, drafts, schedules, integrations and evidence but never audit_jobs.status; the only job-status repository read (lib/repositories/workspace-read.ts:63) is reached solely by lib/workspace/queries.ts:255 latestWorkspaceReport, which has no app caller (only its unit test and an integration test); and a repo-wide grep for "/scanning/" finds exactly one owner-side reference, rescan-button.tsx:53. RescanButton itself is rendered on one page only (components/workspace/home-brief.tsx:70), so the control exists nowhere else. Re-queueing is unguarded: app/api/workspaces/[workspaceId]/rescan/route.ts:68 spends one of the 3/day workspace budget before lib/workspace/rescan.ts:130-160 looks up only the latest *finished* job (loadLatestFinishedJob, :111) and inserts, with no check for a non-terminal job on that location. Owner impact confirmed: during a window of up to 300 s the owner sees a disabled spinner with no toast (toast.success is at :52, after the await) and no scan reference; leaving that window loses the job id permanently, Home still shows the old snapshot date, and pressing Rescan again spends a second paid scan. Buildable within the hard constraints — the fix is to navigate before the process call and read existing job state; it needs no cron, no migration, no agent_runs write, no publishing connector and no provider/OAuth/Stripe authorization.

#### 3. No owner control to complete, dismiss, assign or schedule an action

**Surface:** Action detail · **Type:** `missing_control`

**Evidence**

PATCH /api/actions/[actionId] accepts `action_state: 'dismissed'|'completed'`, `assignee_user_id` and `due_at` and writes `completed_at` plus an `action.dismissed`/`action.updated` audit row (app/api/actions/[actionId]/route.ts:31-60, 119-131); the client helper types all four fields (lib/workspace/client.ts:39). Nothing in the UI sends any of them: `updateAction` is called exactly twice in the whole component tree, both times with `provided_inputs` only (components/workspace/action-detail-client.tsx:265, 282). The header renders assignee and due date as permanently read-only text — "Unassigned" and "Due —" (action-detail-client.tsx:411) — and the actions list's only per-card control is a link to the detail page (components/workspace/actions-list-view.tsx:63) while offering "Completed" and "Dismissed" filters (actions-list-view.tsx:35, 79). The sole code path that writes `action_state='completed'` is re-derivation after a new snapshot, and only when a *comparable* diff resolves the finding (lib/repositories/action-derivation.ts:106-113); `grep 'UPDATE actions'` across lib/repositories shows no other writer. Approve/export leave the action at `in_progress` (lib/repositories/artifacts.ts:317). Home reads the month counter straight off `action_state='completed'` (lib/repositories/workspace-read.ts:138) and renders "N actions completed" (components/workspace/home-brief.tsx:152).

**Owner impact**

An owner who has drafted, approved, exported and actually posted the reply — or who fixed the problem in Google directly, or who has decided this action is not for them — has no way to say so. The action sits in "In progress" or "Recommended" indefinitely, keeps its place in the priority ranking, and keeps counting against the urgent badge. The "Completed" and "Dismissed" tabs and the Home "actions completed this month" counter can only ever be filled by a fresh comparable scan, which is paid-gated — so on a lite workspace they are permanently zero no matter how much work the owner finishes. Assignee and due date are shown on every action but can never be set, so the team page's managers cannot be given anything.

**Verifier**

CONFIRMED. The server control exists and is tested (app/api/actions/[actionId]/route.ts:31-60 accepts action_state dismissed|completed, assignee_user_id and due_at; :119-131 writes the audit row; route.test.ts:85-107 pins it), but no UI anywhere invokes it. Repo-wide grep for `updateAction` yields exactly two non-test call sites, components/workspace/action-detail-client.tsx:265 and :282, both passing `provided_inputs` only; grep for `action_state|assignee_user_id|due_at` across all of components/ returns one hit and it is a comment (calendar-view.tsx:13). lib/workspace/client.ts:101 is the only issuer of that PATCH.

The owner's path ends at components/workspace/action-detail-client.tsx:411 and :413 — assignee renders as the literal text `action.assignee?.name ?? "Unassigned"` and due date as `action.dueAt ? formatDateTime(...) : "—"`, both inside a plain header `<div>` with no input, dialog or handler — and the sticky bar at :583 offers only Save and Approve/Export. On the list, actions-list-view.tsx:62 prints Assignee/Due as read-only <dd> elements and :63 gives one control, a <Link> to the detail page, while :79 still offers "Completed" and "Dismissed" status filters. The owner is left unable to mark an action done, dismiss one they will not do, assign it to a manager, or set a due date — the only exits from an open action are Save, Approve and Export, none of which change action_state past `in_progress` (lib/repositories/artifacts.ts:317).

The sole writer of `completed` is re-derivation after a new snapshot, and only when a *comparable* diff resolved the finding: lib/repositories/action-derivation.ts:105-113 computes `resolved = new Set(diff?.comparable ? diff.resolved_findings : [])` then `measured ? 'completed' : 'expired'`. Rescan is paid-gated (app/api/workspaces/[workspaceId]/rescan/route.ts:66 returns 403 tier_required for non-paid), so on a lite workspace the Completed/Dismissed tabs and the Home counter (lib/repositories/workspace-read.ts:138 reads action_state='completed'; rendered at components/workspace/home-brief.tsx:152 as "N actions completed") are permanently zero regardless of work finished. The codebase corroborates the gap against itself: lib/workspace/measurements.test.ts:165-166 comments "Completion is the other way into the loop: PATCH /api/actions/[id] lets an owner mark an action done with no version at all, and that is real work" — the measurement layer is written assuming a completion path the UI never exposes.

Refutation grounds checked and rejected: the control exists on no other page (grep covers all of components/, app/ and lib/); it is not among the 24 fixed findings in PHASE-2-GAP-REGISTER.md (finding 16 is the inverse defect — re-derivation clobbering owner-set state — which presumes this control should exist); no CLAUDE.md clause documents the omission as deliberate; and no hard constraint blocks the fix — action_state, assignee_user_id, due_at and completed_at all already exist in lib/db/schema/business.ts:83, so no migration is needed, and there is no cron, agent_runs write, publishing connector, paid provider, email, OAuth or deployment dependency. Classification missing_control is correct.

#### 4. Create lists required inputs it cannot collect, then fires the agent anyway — a run that succeeds strands those inputs permanently out of reach

**Surface:** Create · **Type:** `dead_end`

**Evidence**

components/workspace/create-view.tsx:134 renders a "Required before starting" block with an AlertTriangle per `selected.requiredInputs`, but the page has no field for any of them and create-view.tsx:86 always posts `run: true` with no `inputs` (the route accepts `inputs?` — app/api/actions/route.ts:60-66 — nothing sends it). Eight of the eleven goals have required inputs (lib/workspace/templates.ts:74, 88, 130, 144, 158, 186, 200, 228), and only `reviews_without_response` is ever resolved from the scan (lib/workspace/evidence-inputs.ts:66-70). So the action is created `needs_input` (app/api/actions/route.ts:154) and the agent runs immediately (route.ts:185) with the task text rendering "(not provided)" (lib/agents/prompt.ts:79-83, e.g. review-request.ts:9 "Brand voice: (not provided)"). Only social_post is gated pre-model (lib/workspace/runs.ts:325-340). If the model returns an empty facts_needed, lib/repositories/artifacts.ts:317 flips the action to `in_progress`, and components/workspace/action-detail-client.tsx:180-181 then computes `neededKeys` as [] because the state is no longer `needs_input` — so the input form never renders, while the actions list keeps reporting them missing (components/workspace/actions-list-view.tsx:62).

**Owner impact**

The owner is told inputs are required, is given no way to provide them, and a model call is spent regardless. In the branch where the draft comes back looking complete, brand voice / channel / approved claim / owner facts can never be entered again from any surface — the action shows "2 missing" in the queue with no form behind it, and the draft was written without the owner-confirmed facts guardrail 14 requires.

**Verifier**

CONFIRMED. The owner's path ends at components/workspace/action-detail-client.tsx:180 — `const neededKeys = factsNeeded ?? (action.actionState === "needs_input" ? action.missingInputs : [])` — which makes :181 `showInputForm` false and suppresses the :493-509 input form as soon as lib/repositories/artifacts.ts:317 sets `action_state='in_progress'` (the `factsNeeded.length ? 'needs_input' : 'in_progress'` branch). Full chain verified in the files: create-view.tsx:134 lists `selected.requiredInputs` as AlertTriangle labels with no field anywhere on the page; create-view.tsx:86 always posts `run: true` and never `inputs` (app/api/actions/route.ts:58-63 accepts them, nothing sends them); route.ts:126-129,154 marks the action `needs_input`, then :183-191 fires the agent on the same request; lib/workspace/runs.ts:324-340 gates only `social_post` pre-model, so every other agent reaches llm() at :362 with lib/agents/prompt.ts:78-83 rendering "(not provided)" (e.g. review-request.ts:9 "Brand voice: (not provided)"); lib/workspace/overview.ts:140 still reports the keys missing, so actions-list-view.tsx:62 keeps showing "N missing" with a "Review inputs" CTA (:49) linking to the page that has no form. Refutation attempts all failed: grep for `provided_inputs` across components/, app/, lib/ finds exactly two writers, both in action-detail-client.tsx (:265 submitInputs, gated on showInputForm; :282 addOwnerReviewNote, only for reviews_without_response) — the assistant (lib/assistant/live.ts:281,322) only reads, and brand settings writes brand_profiles.voice, which agents never read as an input (lib/workspace/evidence-inputs.ts:19-26 says so and warns this would "lock the owner out of ever supplying them"). There is also no recovery: lib/repositories/action-derivation.ts:96-98 restores needs_input only `WHEN actions.action_state IN ('recommended','ready')`, deliberately excluding in_progress, and an owner-objective action uses objectiveDedupeKey (app/api/actions/_shared/mutation.ts:174) that derivation never matches — so the transition is one-way. Not deliberate: action-derivation.ts:82-84 names this exact mechanism as a bug ("the detail page, which gates its input form on action_state, shows nothing and Generate burns a model call") fixed for a different trigger only. Not already fixed: git log on these files shows 1dd1209 and 0e5a451 addressing adjacent issues, leaving both create-view's missing fields and the :180 gate intact. Not forbidden: the fix is a UI field plus generalizing the existing socialAssetSatisfied pre-model gate — no migration (required_inputs/provided_inputs columns exist), no cron, no agent_runs write, no publishing, no paid provider. So the owner is left unable to ever supply brand_voice, channel, approved_claim, cta_link, owner_fact_1-3 or menu_items for such an action, while the queue keeps telling them those inputs are missing and the draft was written without the owner-confirmed facts guardrail 14 requires. One correction to the evidence: GOALS (create-view.tsx:57, templates with agentKey !== null) is 10, not 11 — 8 of 10 carry required inputs, and the eight cited template line numbers are correct.

#### 5. Confirming the Instagram handle in Integrations never reaches a scan, so an unmeasured IG module has no exit

**Surface:** Settings: brand and integrations · **Type:** `dead_end`

**Evidence**

The Instagram card's only control is `InstagramHandleForm` (components/workspace/integrations-view.tsx:58), presented as "Instagram public evidence" beside a live `ProviderBadge` of the IG module state (components/workspace/integrations-view.tsx:53-59). The route writes exactly two columns — `workspaces.instagram_handle` and the primary location's `ig_handle` (app/api/workspaces/[workspaceId]/instagram-handle/route.ts:43,50 → lib/repositories/workspace-profile.ts:9,12). Nothing in the scan path reads either: a rescan rebuilds its input purely from the parent job's frozen `input_snapshot` (lib/workspace/rescan.ts:80 `optionalString(snapshot, "instagramHandle")`, :136 `scanInputFromSnapshot(sourceJob.input_snapshot, …)`), and the new job row takes `ig_handle: input.instagramHandle || null` from that (lib/scan/start-job.ts:237). lib/workspace/rescan.test.ts:120 pins this behaviour (`ig_handle: "kammanhouse"` copied from the snapshot). The only consumer of `workspaces.instagram_handle` anywhere is the display model itself (lib/workspace/queries-pages.ts:369); the agent context does not receive it either (lib/workspace/runs.ts:263-278). The form also cannot clear a wrong handle — submit is disabled on an empty value (components/workspace/integrations-client.tsx:50) and the route 400s on one (route.ts:38-40).

**Owner impact**

An owner whose first scan ran without an Instagram handle sees "No handle provided" and an `unavailable` IG badge, types the correct handle, and gets a success toast — and nothing changes, now or after any number of paid rescans. IG stays `IG_HANDLE_NOT_PROVIDED` forever, coverage stays permanently lower, and every IG-driven action template (social-post, ig-bio, ig-highlights) can never be derived. Diagnose→Prioritise is closed off for one of the four primary sources by a control that looks like it opens it.

**Verifier**

CONFIRMED. The owner's path ends at lib/workspace/rescan.ts:80 — `optionalString(snapshot, "instagramHandle")` — where the rescan rebuilds its input purely from the parent job's frozen input_snapshot (:136) and therefore discards the handle the owner just confirmed.

Verified chain, all cited lines accurate:
1. WRITE: app/api/workspaces/[workspaceId]/instagram-handle/route.ts:43,50 writes exactly two columns via lib/repositories/workspace-profile.ts:9 (UPDATE workspaces SET instagram_handle) and :12 (UPDATE locations SET ig_handle WHERE is_primary=true). Returns {ok:true, handle} and the client toasts success (components/workspace/integrations-client.tsx:39).
2. READ: the scan reads audit_jobs.ig_handle only — packages/scan-engine/src/collect-providers.ts:64,66 short-circuits to {available:false, followers:0} when it is empty, never calling RapidAPI. That column is set at lib/scan/start-job.ts:237 from input.instagramHandle, which on rescan comes from the snapshot (rescan.ts:136,80). app/api/workspaces/[workspaceId]/rescan/route.ts:42-57 accepts only locationId, locale and consent — there is no override parameter. lib/workspace/rescan.test.ts:120 pins ig_handle copied from the snapshot.
3. NO OTHER CONSUMER: repo-wide grep for ig_handle|instagram_handle shows the sole reader of workspaces.instagram_handle is lib/workspace/queries-pages.ts:369 (display model). locations.ig_handle is only ever written; lib/workspace/claim.ts:210 flows job -> location, the opposite direction. Agent context (lib/workspace/runs.ts:263-294) never receives it.
4. NO CONTROL ELSEWHERE: the only path that could re-identify a location from a new scan is an OAuth-verified claim (claim.ts:194-219), gated by WORKSPACE_CLAIM_VIA_OAUTH_ENABLED and OAuth consent — separately authorized, not a control on this surface, and not discoverable from Integrations. The one already-fixed Instagram finding (IMPLEMENTATION-TRACEABILITY.md:64) is about the onboarding IG step being skippable, a different concern.
5. NOT DELIBERATE: CLAUDE.md §3.2.3 documents the snapshot-based rescan and §1.4/Phase 6 item 3 documents the confirm route, but neither the plan nor any code comment addresses their interaction or states the handle must not reach a scan. The comment block at rescan.ts:13-26 justifies only the v2-envelope refusal (identity confidence).
6. NOT FORBIDDEN: a fix passes the primary location's confirmed ig_handle into the rebuilt ScanStartInput — no migration (both columns exist, catalog unchanged), no cron, no agent_runs write, no publishing, no OAuth, no paid provider.

What the owner is left unable to do: an owner whose first scan ran without an Instagram handle cannot ever get the IG module measured. They confirm the handle, see it echoed as "@handle" in the card status while the adjacent ProviderBadge (integrations-view.tsx:59, fed by snapshot.moduleStates.instagram.status at queries-pages.ts:370) stays `unavailable`, and every subsequent paid rescan re-queues IG_HANDLE_NOT_PROVIDED. Coverage stays permanently depressed and the social-post / ig-bio / ig-highlights templates can never derive. Secondary confirmed defect: a wrong handle cannot be cleared — submit is disabled on an empty value (integrations-client.tsx:50) and the route 400s on one (route.ts:38-40).

Severity high rather than medium: the dead end is permanent and irreversible in-product for one of the four primary evidence sources, and the success toast plus the updated card status actively signal that it worked, so the owner has no reason to seek another route.

#### 6. In-app notifications can never be marked read, so the bell dot is permanent

**Surface:** Settings: notifications and More · **Type:** `dead_end`

**Evidence**

`workspace_notifications.read_at` is written by nothing in the repo. It is only ever read: `lib/repositories/workspace-read.ts:52` counts `read_at IS NULL` for the topbar dot, `lib/repositories/workspace-read.ts:162-163` selects it for the list, `components/workspace/notifications-view.tsx:19` derives the "n unread" badge from it, and `:42` puts `is-unread` on each row. The only writes to the table are INSERT (`lib/repositories/notifications.ts:60`) and a dedupe SELECT (`:76`). There is no UPDATE anywhere, and no route under `app/api/workspaces/[workspaceId]/` touches notifications (only `notification-preferences` exists). The page renders the list with no dismiss, mark-read, or mark-all-read control (`components/workspace/notifications-view.tsx:37-47`).

**Owner impact**

The first notification ever written turns on the red dot at `components/product-ui.tsx:458` and it stays on forever, on every page of the workspace. Because `version.approved` and `delivery.exported` fan out to every accepted member (`app/api/versions/[versionId]/approve/route.ts:46`, `app/api/versions/[versionId]/export/route.ts:74`, default `userIds: "all"` in `lib/workspace/notify.ts:66-71`), a workspace that runs the loop a few times has a permanently-lit bell and an "unread" count that only grows. The owner cannot tell a new signal from an old one, which is the entire point of the indicator; the surface whose job is "controls what they are told" has no exit from the notified state.

**Verifier**

CONFIRMED. Verified independently. `workspace_notifications.read_at` is written by nothing: repo-wide grep for read_at/readAt/mark-read returns only the Drizzle column (lib/db/schema/business.ts:689), the DDL (neon/migrations/0002_business.sql:458), two SELECTs (lib/repositories/workspace-read.ts:52 for the topbar dot, :162 for the list), two render reads (components/workspace/notifications-view.tsx:19, :42), and an in-memory test fixture assignment (lib/workspace/notify.test.ts:84). The repository lib/repositories/notifications.ts exposes only updatePreferences (writes workspaces.notify_*), acceptedMemberIds, insert (:60), hasSince (:76), workspaceSlug — no UPDATE or DELETE of the table exists anywhere in app/, lib/ or components/. The full app/api tree confirms the only route under app/api/workspaces/[workspaceId]/ touching notifications is notification-preferences. Page load does not mark read either: app/[locale]/owner/[workspaceSlug]/settings/notifications/page.tsx calls getNotifications, which at lib/workspace/queries-pages.ts:678-690 issues two SELECTs only. Fan-out confirmed: approve/route.ts:46 and export/route.ts:74 call notifyWithRepository with no userIds, and lib/workspace/notify.ts:66-71 defaults to every accepted member. Dot confirmed at components/product-ui.tsx:458, fed by lib/workspace/queries.ts:191 -> workspace-read.ts:52 (read_at IS NULL). Refutation attempts failed: (a) not among the 24 fixed findings — finding 14 (PHASE-2-GAP-REGISTER.md:59, :476-499) concerns the email switch copy and explicitly states the in-app bell write path works, saying nothing about read state; (b) not deliberate or documented — CLAUDE.md §3.3 ships read_at in the schema and Phase 6 item 4 specifies only the write side; no code comment declares read-marking out of scope; (c) no control elsewhere — notifications-client.tsx holds only the three email switches, and no shell dropdown, dialog or other page exposes a dismiss; (d) not forbidden by the hard constraints — the column already exists (0002_business.sql:458), sme_app_runtime already holds UPDATE under a permissive FOR ALL policy (0003_workflows.sql:162-163), and the completion_fence_notifications BEFORE INSERT OR UPDATE trigger (0004_atomic_operations.sql:595) short-circuits with `return new` when no completion context GUC is set (0004:515), so a mark-read UPDATE requires no migration, no catalog change, no cron, no agent_runs write, no publishing connector, and no paid provider, email, OAuth or Stripe action. Owner's path ends at components/workspace/notifications-view.tsx:37-47: the In-app card renders the unread badge and is-unread rows with no dismiss, mark-read or mark-all-read control, and no server path can clear the state. The owner is left unable to mark any notification read, so the bell dot at components/product-ui.tsx:458 stays permanently lit on every workspace page from the first notification onward and the unread count only grows, destroying the indicator's only function. Classification dead_end is accepted: the bell is an entry point that leads to a surface where the state it signals cannot be resolved (missing_control is a defensible near-alternative, but the terminating one-way state is the sharper description).

#### 7. The sidebar account button is inert — it looks like the account menu and does nothing

**Surface:** Settings: notifications and More · **Type:** `missing_control`

**Evidence**

`components/product-ui.tsx:438-442` renders `<button className="account-button" type="button" title={workspace.account.email}>` with the account avatar, name, role label and a `MoreHorizontal` affordance, and no `onClick`, no `aria-haspopup`, no wrapping menu — the only styling is cosmetic (`app/ramp-refresh.css:1064`, `:1069`). A working sign-out already exists and is wired elsewhere: `signOutAction` in `app/[locale]/owner/actions.ts:9`, used only by `app/[locale]/owner/select-workspace/page.tsx:47`.

**Owner impact**

Inside a workspace, the one control that reads as "my account" is dead. Signing out requires guessing that it lives behind the workspace switcher (`components/product-ui.tsx:386`) or, on mobile, behind the More page's link labelled "Locations" (`components/workspace/more-view.tsx:11`). On a shared shop tablet — the normal HK/TW case for this product — an owner who wants to hand the device over has no discoverable way to end the session.

**Verifier**

CONFIRMED, and the gap is larger than claimed. components/product-ui.tsx:438-442 renders the account button with avatar, name, role label and a MoreHorizontal affordance but no onClick, no aria-haspopup and no wrapping menu — a grep for DropdownMenu|Popover|aria-haspopup across components/ matches only the unused components/ui/ primitives, so nothing wraps it; app/ramp-refresh.css:1063-1071 only sets font sizes, confirming the styling is cosmetic. The evidence for the escape hatch is where the claim is too generous: components/select-workspace-page.tsx:48 puts the sign-out form (:54) inside the cards.length === 0 branch only. An owner who HAS a workspace — the normal case, and the only way to reach the shell — follows the workspace switcher (product-ui.tsx:386) to select-workspace and lands on the workspace-choice-grid at :59, which contains no sign-out control. The mobile More page routes to that same page (components/workspace/more-view.tsx:12), so it inherits the same dead end. docs/integration/PHASE-1-REPORT.md:105-106 records that the report view's sign-out control was also dropped ("the report.signOut* keys exist for Phase 2"). Net: signOutAction (app/[locale]/owner/actions.ts:9, unit-tested) is reachable from exactly one UI state — the zero-membership empty state a member never sees — so a signed-in owner has NO reachable way to end the session from anywhere in the product. The owner's path ends at components/product-ui.tsx:438 (inert button) with the fallback closed at components/select-workspace-page.tsx:48 (sign-out gated behind the empty-state branch): handing a shared shop tablet to staff, the owner cannot end their session without clearing browser cookies, leaving an authenticated session with approve/export authority live on the device. Not a false positive: not among the ~24 already-fixed findings (checked git log -30 and PHASE-2-GAP-REGISTER.md; the nearest entry, #15 at :522, concerns onboarding contact copy), and no CLAUDE.md rule or code comment makes the inertness deliberate. Buildable within the hard constraints — signOutAction already exists and is already bound-and-passed as a prop by a server page in this repo, the owner layout is a server component rendering the client WorkspaceShell, and components/ui/dropdown-menu.tsx is present; no migration, cron, agent_runs write, publishing connector, or provider/OAuth/Stripe authorization is required.

#### 8. "Rescan now" blocks on the inline scan: the owner sits on a spinning button for minutes instead of going to the progress page

**Surface:** Rescan and measurement · **Type:** `missing_control`

**Evidence**

rescanLocation awaits the process call before returning — `await post("/api/scan/process", { jobId: queued.data.jobId })` (lib/workspace/client.ts:222). That route runs the scan inline with `export const maxDuration = 300` and `await runScan(...)` (app/api/scan/process/route.ts:11,61), and CLAUDE.md notes a scan takes ~5-13 minutes. RescanButton awaits that promise before it clears busy or navigates: lines 49-53 of components/workspace/rescan-button.tsx (`const result = await rescanLocation(...)`; `setBusy(false)`; `router.push(\`/${locale}/scanning/${...}\`)`). The public funnel does the opposite: scan-page.tsx:227 pushes to /scanning immediately after start, and the scanning page fires the process POST itself, fire-and-forget with a cooldown (components/scanning-page.tsx:124-145).

**Owner impact**

The owner confirms the consent dialog and the button spins with no progress, no collector cards and no stalled/Resume affordance until the scan finishes or Vercel cuts the function at 300s — then they are pushed to a progress page for a job that already ended. Anyone who reloads or navigates in that window loses the jobId, which is only ever held in that pending promise (see the next gap).

**Verifier**

CONFIRMED. The owner's path ends at components/workspace/rescan-button.tsx:49-53: `await rescanLocation(...)` must resolve before `setBusy(false)` and `router.push('/{locale}/scanning/{jobId}')` run. rescanLocation blocks on lib/workspace/client.ts:222 (`await post("/api/scan/process", { jobId })`), and that route runs the scan inline — app/api/scan/process/route.ts:61 `await runScan(jobId, session.id)` under `export const maxDuration = 300` (line 11); the only non-inline branch is the cloudflare hand-off at line 49, which the hard constraints say is disabled against this Neon database. With a 5-13 minute scan, the owner confirms the consent dialog, the dialog closes, and they are left on Home with a spinning LoaderCircle (rescan-button.tsx:72) for up to 300s before being pushed to a progress page for a job that is already over or already cut off.

The control does not exist elsewhere. `grep -rn "scanning/"` across components, lib and app returns exactly three hits: components/scan-page.tsx:227 (public funnel), components/workspace/rescan-button.tsx:53, and app/robots.ts:15. RescanButton is mounted in exactly one place (components/workspace/home-brief.tsx:70). No workspace surface — Home, Activity, Calendar, Notifications — links to an in-flight job, so an owner who reloads or navigates during that window cannot reach the running scan at all: the jobId lives only in the pending promise.

Not deliberate or documented. The doc comment at lib/workspace/client.ts:206-209 claims this is "the same two-step the scan funnel uses," but the funnel does the opposite — components/scanning-page.tsx:124-145 fires the process POST as `void postProcess()` with `.catch(() => undefined)` and reads nothing from the response, having already navigated. CLAUDE.md §3.2.3 says only "then the client POSTs /api/scan/process"; it never prescribes awaiting it. Not among the annotated prior fixes in these files (consent dialog, server-resolved consentPolicyVersion, ipHash, parseScanConsent 409).

One fact strengthens the claim beyond what was offered: the result of line 222 is discarded, so even a 300s platform cut-off still resolves to `{ ok: true, data: { jobId } }` — pinned by lib/workspace/client.test.ts:187. The multi-minute block therefore buys the owner nothing observable; removing it and letting the scanning page's existing effect claim the job needs no cron, no migration, no agent_runs write and no paid provider, so it is buildable here.

Classification missing_control is correct: what is absent is any affordance that reaches a running rescan. Severity high — the primary Phase 6 owner action is unusable in practice and the job becomes unrecoverable on any reload.

### Medium

#### 9. Ownership verification has no owner-facing failure surface: raw JSON pages and silently discarded error codes

**Surface:** Onboarding and claim · **Type:** `missing_control`

**Evidence**

Step 2's 'Verify with Google' is a plain anchor straight at the API route (components/onboarding-page.tsx:234). Every failure in app/api/oauth/google/claim/start/route.ts returns NextResponse.json -- not_found 404 (:38), unavailable 503 (:50), invalid_request 400 (:56), unauthenticated 401 (:63), not_found 404 (:66), no_place_id 422 (:67), already_claimed 409 (:68) -- so a browser navigation lands the merchant on a raw JSON blob with no header, no locale, no way back. Two of these are ordinary outcomes, not exotic errors: `no_place_id` fires for EVERY manual-entry scan (the documented 'not my business -> manual entry' path leaves audit_jobs.place_id null), and `already_claimed` fires whenever someone else already claimed that report. `unavailable` (503) is guaranteed in the documented staged rollout, because onboarding renders the button on the flag alone (app/[locale]/owner/onboarding/page.tsx:157) while the route additionally requires GOOGLE_OAUTH_CLAIM_REDIRECT_URI (lib/oauth/google-connection.ts:70-72). Post-consent failures are equally mute: app/api/oauth/google/claim/callback/route.ts:56 redirects to /{locale}/r/{slug}?claim=<reason> for declined, invalid, session_mismatch, exchange_failed, no_refresh_token, missing_scope, verification_failed, place_not_managed, not_found, already_claimed, storage_failed and unavailable (:70-188), but app/[locale]/r/[slug]/page.tsx:31 accepts only `params` and never reads searchParams, so every one of those codes is discarded and the page renders exactly as before.

**Owner impact**

At the single step that decides whether they get a workspace at all, the merchant either lands on a JSON error page or is bounced back to their report with no message. They cannot tell that verification failed, why (declined consent vs. wrong Google account vs. a listing Google says they do not manage vs. a scan with no place_id that can never be verified this way), or what to do next -- and returning to step 2 shows the identical screen and the same button.

**Verifier**

CONFIRMED. Every cited line reproduces.

Where the owner's path ends (1) — pre-consent: components/onboarding-page.tsx:234 is a plain anchor navigating the browser to /api/oauth/google/claim/start. That route terminates in NextResponse.json on every branch: :38 404 not_found (flag off), :50 503 unavailable, :56 400 invalid_request, :63 401 unauthenticated, :66 404 not_found, :67 422 no_place_id, :68 409 already_claimed, :76 500. The owner is left on a raw JSON body with no header, no locale, no navigation back into onboarding. Two of these are ordinary outcomes: :67 fires for any manual-entry scan (place_id null) and :68 whenever the job already carries workspace_id but the caller is not its owner (ownsWorkspace=false in app/[locale]/owner/onboarding/page.tsx:132, so the button still renders). The 503 is a genuine same-config mismatch I verified: onboarding/page.tsx:157 sets oauthEnabled from WORKSPACE_CLAIM_VIA_OAUTH_ENABLED alone, while start/route.ts:48 also requires googleOAuthClaimConfigured(), which per lib/oauth/google-connection.ts:70-72 additionally demands GOOGLE_OAUTH_CLAIM_REDIRECT_URI — exactly the staged-rollout state the route's own comment describes.

Where the owner's path ends (2) — post-consent: app/api/oauth/google/claim/callback/route.ts:56/60 redirects to /{locale}/r/{slug}?claim=<reason> for declined, invalid, session_mismatch, exchange_failed, no_refresh_token, missing_scope, verification_failed, place_not_managed, not_found, already_claimed, storage_failed (:80-188), but app/[locale]/r/[slug]/page.tsx:31 declares only ({ params }) — no searchParams — so every code is discarded and the report renders identically to a page reached with no claim attempt. I grepped all components for the thirteen reason strings: zero hits outside the callback route, its tests and the docs. The two codes routed to the picker instead (unavailable at :70, invalid_state at :82) are dropped too — app/[locale]/owner/select-workspace/page.tsx:40 reads only query.denied. The owner cannot tell that verification failed, cannot distinguish declined consent from wrong Google account from a listing Google says they do not manage, and returning to step 2 shows the identical card and button.

Refutation checks all failed: no control exists elsewhere (components/report/dashboard-summary.tsx:63-71 renders only a forward claim CTA; the sign-in pages read `reason` but are not a destination of this flow); nothing in CLAUDE.md or the code comments makes this deliberate — the callback's back() comment at :42-52 argues for redirecting where the merchant can see the outcome, which the receiving page then ignores; it is not among the 24 fixed findings (register finding 15 is the staff-assignment fallback copy, a different branch of the same ternary; finding 10 is the state user-binding fix that introduced session_mismatch without ever adding a reader); and it is not forbidden — a fix is UI/routing only (render the reason on the report or onboarding, and/or have start redirect with a reason instead of returning JSON), needing no cron, no agent_runs write, no publishing connector, no OWNER_SELF_SERVICE_CLAIM, no schema change, and no paid provider or live OAuth consent to build and test.

Classification missing_control stands: this is the absence of any failure surface, not wording on an existing one.

Severity qualifier, verified not assumed: in the shipped default (.env.example WORKSPACE_CLAIM_VIA_OAUTH_ENABLED=false) onboarding renders the Fimmick-assignment card instead, and start/route.ts:37 404s before anything, so no owner reaches this today. The gap is latent until the flag flips — and the 503 mismatch fires on the very first flip, before the redirect URI is registered. Hence medium rather than high.

#### 10. The default ownership path records no in-product request for a returning or second-shop merchant

**Surface:** Onboarding and claim · **Type:** `missing_control`

**Evidence**

WORKSPACE_CLAIM_VIA_OAUTH_ENABLED is false in the shipped default, so step 2 renders the 'Ask Fimmick to assign your workspace' branch, whose only affordance is an external WhatsApp/LINE/phone/email link plus 'return to this page' (components/onboarding-page.tsx:246-248). The in-product staff signal, a workspace_access_requests row, is written only during magic-link sign-in (lib/identity/complete-sign-in-ports.ts:31-40 via claimsRepository.recordAccessRequest, lib/repositories/claims.ts:66-68) and only when shouldRecordAccessRequest passes -- which requires a `claim` slug on the flow AND that the user owns no workspace yet (lib/workspace/access-request.ts:22-30). So a merchant who follows the copy and returns to /owner/onboarding?claim=... later files nothing, and a merchant claiming a second shop (they already own a workspace for shop A) can never file one at all. The onboarding page never reads or displays any pending request either (app/[locale]/owner/onboarding/page.tsx:131-141 derives state from evidence, ownership and locations only).

**Owner impact**

On the only ownership path that is actually open today, the merchant gets no confirmation that a request exists, no pending state on return, and -- for a returning or multi-shop merchant -- nothing is queued for Fimmick at all; the request lives only in whatever WhatsApp or email thread they remembered to send. The assignment action itself stays with Fimmick (the staff console is the legacy app, out of scope here), but the request record and its pending state are not.

**Verifier**

CONFIRMED. The owner's path ends at components/onboarding-page.tsx:246-248: on the shipped default (WORKSPACE_CLAIM_VIA_OAUTH_ENABLED=false, .env.example) step 2 renders the "Ask Fimmick to assign your workspace" branch whose only affordances are external anchor tags built by contactHref plus a printed share_slug. The owner is left unable to (a) file an in-product request at all when they already own a workspace, and (b) see any confirmation or pending state, ever. Verified independently: lib/workspace/access-request.ts:26 returns false whenever hasWorkspace, and lib/repositories/membership.ts:29-32 ownedWorkspace selects ANY owned workspace globally (role='owner' ... LIMIT 1), not one scoped to the job -- so a merchant who owns shop A can never file for shop B. The guard's stated rationale (avoid refiling each sign-in) is already covered by the ON CONFLICT (job_id,user_id) WHERE resolved_at IS NULL DO NOTHING in lib/repositories/claims.ts:67, so it is over-broad rather than a deliberate multi-shop block. The returning-merchant case is worse than the claim states: lib/auth.ts:66-68 signInPath emits only ?returnTo= and drops claim, and parseAuthFlow reads claim from the query alone (lib/identity/sign-in-flow.ts:54), so a signed-out merchant who follows the page's own "return to this page" instruction reaches finishClaim with flow.claim === null and short-circuits at complete-sign-in-ports.ts:27 -- nothing is filed even on a genuine re-sign-in. app/[locale]/owner/onboarding/page.tsx:131-141 derives state from evidence, ownership, GBP connection and saved setup only; workspace_access_requests is never read outside tests/schema, and a repo-wide scan of app/api shows no access-request route and no equivalent affordance on select-workspace. Refutations tried and failed: CLAUDE.md:1051 explicitly prescribes this branch "with the access-request flow"; docs/integration/PHASE-2-REPORT.md:97 lists it under "Known gaps and follow-ups", not under "Not done, by design"; commit 3939e79 fixed only the false "you will be emailed" copy, not the missing record. Not forbidden by the hard constraints: the table, its open-unique index, the runtime grants and claimsRepository.recordAccessRequest already exist (neon/migrations/0002_business.sql:417, 0003_workflows.sql:148-151), so no migration and no catalog change; no paid provider, email, OAuth or Stripe; and a request row grants no authority, leaving guardrail 15 and OWNER_SELF_SERVICE_CLAIM untouched. Severity lowered to medium rather than high because the first-time single-shop merchant does get a row filed during sign-in and the branch does hand the owner a working contact channel with the report reference prefilled -- the silent failure is confined to the multi-shop merchant plus the universal absence of confirmation/pending state.

#### 11. No way to sign out (or switch account) once the owner has a workspace

**Surface:** Workspace shell and scope · **Type:** `missing_control`

**Evidence**

The sidebar account button is inert: `components/product-ui.tsx:438-442` renders `<button className="account-button" type="button" title={workspace.account.email}>` with a `MoreHorizontal` affordance, no `onClick`, no menu, and no other reference to it anywhere but CSS (`app/ramp-refresh.css`, `app/globals.css`). The only sign-out control in the whole owner product sits inside the *empty* branch of select-workspace: `components/select-workspace-page.tsx:48` opens `cards.length === 0 ? (` and the form is at `:54`; the populated branch (`:59-80`) renders only workspace/location links plus the note at `:82`. No other component contains "Sign out"/"登出" (grep over `components/`), and `components/workspace/more-view.tsx:11-19` lists seven links, none of them account/session. The plumbing already exists: server action `app/[locale]/owner/actions.ts:9-13` and `POST /api/auth/sign-out` (`app/api/auth/[...path]/route.ts:68,76-80`, which also clears MANAGED_AUTH_COOKIES).

**Owner impact**

A signed-in owner, manager or viewer has no in-product exit from the session. The concrete trap: someone signs in as the wrong address (a shared shop iPad, or an owner who was invited under a personal email), opens a workspace slug they do not belong to, and `lib/auth.ts:130-132` sends them to `/owner/select-workspace?denied=<slug>`; the denial banner at `components/select-workspace-page.tsx:47` tells them to "choose one you have access to", but because they do hold at least one membership the sign-out button never renders, so they cannot change account without clearing cookies. Handing the device to staff also means the session cannot be ended.

**Verifier**

CONFIRMED. The owner's path ends at components/product-ui.tsx:438-442: the only account affordance in the authenticated shell is `<button className="account-button" type="button" title={workspace.account.email}>` with no onClick, no menu, no form — verified by reading; it is referenced nowhere but app/ramp-refresh.css:1064,1069. It also lives in the `hidden md:flex` Sidebar (product-ui.tsx:380), so mobile has no account affordance at all, and components/workspace/more-view.tsx:11-19 lists seven links with no account/session entry. The one sign-out in the owner product is unreachable for the affected population: components/select-workspace-page.tsx:48 opens `cards.length === 0 ? (` and the form is at :54, while `cards` is `listWorkspaceCards(user.id)` over accepted memberships (app/[locale]/owner/select-workspace/page.tsx:39), so any member always takes the :59-80 branch, which renders only links. A repo-wide grep for signOut/sign-out/登出 confirms the only other session-ending control is changeAccount() at components/auth/sign-in-completion.tsx:85-92, rendered only on the `no_access` screen (:97) — again exclusively for identities with no workspace access. What an authenticated member is left unable to do: end their session anywhere in the product. All three mechanisms exist and are unwired to any member-reachable control — signOutAction (app/[locale]/owner/actions.ts:9-13) has exactly one caller (select-workspace's empty branch), and POST /api/report-access/sign-out, which calls signOut() and clears MANAGED_AUTH_COOKIES (route.ts:50-58), has no component caller at all, only tests. CORRECTION to the offered impact: the claim that a denied user "cannot change account without clearing cookies" is overstated. select-workspace renders inside PublicPageFrame -> PublicHeader, whose "Sign in" button at components/product-ui.tsx:260-262 links to /{locale}/owner/sign-in, and app/[locale]/owner/sign-in/page.tsx does not redirect an already-authenticated user — it renders the full Google/magic-link flow, so re-authenticating as a different address replaces the session. Account switching is therefore possible, if unlabelled and undiscoverable; session termination is not. The shared-device half of the impact (handing a shop iPad to staff with an owner session that can approve deliveries, invite team and open billing) stands unmitigated. Not deliberate and not already fixed: no CLAUDE.md guardrail or code comment covers this; docs/integration/PHASE-2-REPORT.md:43 records the sign-out as an empty-state feature, and PHASE-1-REPORT.md:105-106 records a report sign-out control dropped with `report.signOut*` keys left "for Phase 2" — deferral, not omission by design. Not forbidden: a sign-out control needs no migration (no schema change, so scripts/neon/catalog.ts is untouched), no cron, no agent_runs write, no publishing connector, no OWNER_SELF_SERVICE_CLAIM, and no paid provider, OAuth consent, Stripe event or real email. Severity medium rather than high because an undocumented account-switch path exists via the public header and the defect neither corrupts data nor opens an authorization hole; classification missing_control is correct, since the control is absent from the entire authenticated surface rather than merely mislabelled.

#### 12. Owner area has no error boundary, so any workspace query failure replaces the whole workspace with a bare Next error page

**Surface:** Workspace shell and scope · **Type:** `dead_end`

**Evidence**

`lib/workspace/queries.ts:162-173` (`fail()` logs then `throw new Error(...)`; `read()` converts every repository rejection into that throw) is on the hot path of `loadWorkspaceContext` (`lib/workspace/queries.ts:204-220`), which the shell layout awaits unconditionally at `app/[locale]/owner/[workspaceSlug]/layout.tsx:31-33`, and of `listWorkspaceCards` (`:223-252`) awaited at `app/[locale]/owner/select-workspace/page.tsx:39`. `find app -name error.tsx -o -name global-error.tsx -o -name not-found.tsx` returns nothing — the repo has no error or not-found boundary at any level.

**Owner impact**

A single transient Neon failure while loading locations, usage or the unread count throws out of the layout, so the owner does not get a degraded workspace — they get Next's unstyled "Application error: a server-side exception has occurred", with no locale, no branding, no retry, and no link back to select-workspace or sign-out. The same screen appears for a workspace whose row is missing (`loadWorkspaceRow` `:175-179` fails when `rows[0]` is absent), so a half-created workspace is permanently a crash rather than a recoverable state.

**Verifier**

CONFIRMED. The owner's path ends at lib/workspace/queries.ts:164 (`throw new Error(\`Unable to load ${what}\`)`), reached from the unguarded awaits at app/[locale]/owner/[workspaceSlug]/layout.tsx:31-32 (loadWorkspaceContext + countUrgentActions) and app/[locale]/owner/select-workspace/page.tsx:39 (listWorkspaceCards). read() at queries.ts:167-173 is a bare `catch { fail(...) }`, so every repository rejection for workspace/locations/usage/notifications/actions/snapshots becomes that throw. I verified independently that the repo contains no error.tsx, global-error.tsx or not-found.tsx at any level (search over the whole repo, not just app/) and no React error boundary (no ErrorBoundary/componentDidCatch/getDerivedStateFromError anywhere in app, components or lib); app/layout.tsx has no catch either. Because the throw originates in the shell *layout*, a single transient Neon read failure takes down every route under /{locale}/owner/{slug}/* at once, and the owner is left unable to retry, change locale, switch workspace, or sign out from within the app — Next's default unstyled "Application error: a server-side exception has occurred" has no control at all, so the only recovery is hand-editing the URL. Not deliberate: nothing in CLAUDE.md, docs, or a code comment records this choice, and the codebase already degrades elsewhere (onboarding/page.tsx:48,69,77,105; actions/[actionId]/page.tsx:30 `.catch(() => [])`), showing the shell path is simply unprotected rather than intentionally fail-fast. Buildable under the hard constraints: an error.tsx/global-error.tsx (plus optional try/catch degradation in loadWorkspaceContext) needs no migration (frozen catalog untouched), no cron, no agent_runs write, no publishing connector, no OWNER_SELF_SERVICE_CLAIM, and no paid provider. Two corrections to the submitted write-up: (1) severity is medium, not high — the dead end is on the failure path only, conditional on an infrastructure fault, not on the routine owner path; (2) the sub-claim that a missing workspace row is "permanently a crash" is overstated — requireMembership (lib/auth.ts:128-131) resolves the workspace by slug and redirects to select-workspace?denied= before loadWorkspaceContext runs, so loadWorkspaceRow's fail() at queries.ts:177 is only reachable on a delete racing between the two reads. The core gap (no error boundary on the owner area) stands on its own evidence.

#### 13. The evidence report behind the snapshot is fully implemented for members but unreachable from the workspace

**Surface:** Owner Home · **Type:** `missing_control`

**Evidence**

Member access to the report is built and tested: lib/report-access/authorize-report.ts:19 declares `{ kind: "member"; workspaceId; role }`, :111 returns it, and lib/report/view-model.ts:214-215 renders the full authorized model (summary, per-module findings, sanitised proof, evidence) for it — matching CLAUDE.md §3.1's `/r/[slug]` "member = full". No owner surface links to it: grepping `/r/` across app/[locale]/owner/**, components/workspace/** and lib/workspace/** yields only a comment at app/[locale]/owner/onboarding/page.tsx:42. Home's two "see the evidence" links both go to Insights instead (components/workspace/home-brief.tsx:120 and :138). The data to build the link is missing by one column: lib/workspace/snapshots.ts:16-32 gives SnapshotRecord a `jobId` but no share slug, and lib/repositories/workspace-read.ts:13 SNAPSHOT_COLUMNS omits share_slug. The abandoned wiring is still in the tree — lib/repositories/workspace-read.ts:63-66 selects share_slug and status from audit_jobs, exposed by lib/workspace/queries.ts:255 as `latestWorkspaceReport`, which nothing calls.

**Owner impact**

Home's change ledger names bare finding keys (components/workspace/home-brief.tsx:182-185: "Regressed in the latest comparable scan"), and the score card withholds or shows a number, but the owner cannot open the document that explains any of it — the executive summary, the per-module findings with their owner messages, the sanitised proof and the stated limitations. Guardrail 1 is "evidence before score", yet the only route to that evidence is the public share URL from the original funnel scan. An owner who arrived by invite, or who simply did not keep the link, owns a workspace whose underlying report they can read (the authorization says so) but cannot find.

**Verifier**

CONFIRMED. The owner's path ends at components/workspace/home-brief.tsx:120 and :138 — both "evidence" affordances on the score and proof cards resolve to `${base}/insights` (and the ledger's "View proof" at :183 does the same), and components/workspace/insights-view.tsx renders only the score series, before/after metric cards, AEO presence rates and a ledger of findingLabel() strings. findingLabel is readableFindingKey (lib/funnel/report-labels.ts:8-10), which just strips the module prefix and de-underscores the key, so the workspace never shows the executive summary, the per-module findings with their owner_message_* text, or sanitizeReportProof output. Those exist only on /r/[slug].

No owner surface can reach that route. I grepped the literal "/r/" AND the helper paths (reportPath, absoluteReportUrl, shareSlug, share_slug) across app/[locale]/owner/**, components/workspace/** and lib/workspace/**: the sole hit is the pre-claim onboarding card (components/onboarding-page.tsx:216), which is circular — it needs the slug you are trying to find. The data is missing by one column exactly as claimed: lib/workspace/snapshots.ts:16-32 SnapshotRecord carries jobId but no share slug, and lib/repositories/workspace-read.ts:13 SNAPSHOT_COLUMNS omits it. The abandoned wiring is real: workspace-read.ts:63 latestReport selects share_slug/status from audit_jobs and lib/workspace/queries.ts:255 latestWorkspaceReport exposes it, but the only references anywhere are its own tests (queries.test.ts:200-212, neon-workspace-read.integration.test.ts:38).

Member access is genuinely built at the library level — authorize-report.ts:19 declares the kind, :109-115 returns it after the workspace_id fail-closed check at :82, and view-model.ts:214-215 returns the full authorized model. Two corrections to the evidence, neither of which rescues the code. (1) "Fully implemented for members" overstates the page wiring: app/[locale]/r/[slug]/page.tsx:33 calls loadReport(slug, locale) with no options, and lib/report/load-report.ts defaults getMembership to `noMembership` (returns null), so the member branch is never produced at runtime — a member who hand-built the URL today gets the public preview. The gap is therefore two wiring steps, not one column, and it is larger than claimed, not smaller. (2) The claimed impact ("cannot open the document that explains any of it") overstates the deficit: the evidence gallery renders on Home (home-brief.tsx:189), derived actions carry owner_message text as their summary (lib/workspace/actions.ts:154) with source/factType/observedAt, and Insights carries fact-typed before/after cards. What has no workspace surface at all is the executive summary, the full per-module findings list and the sanitised proof. That is why I set severity to medium rather than high.

Not refutable on the other grounds: no CLAUDE.md provision and no code comment documents the omission (the report page's only comment at :16-22 is about noindex for competitor-scanned businesses, not owner linking); the nearby already-fixed markers in these files concern the ScoreDial delta spread, the rescan cadence wording and the AEO denominators, none of them this; and the fix is buildable under the hard constraints — share_slug already exists on audit_jobs so no migration is needed, and nothing here touches crons, agent_runs, publishing, self-service claim, or a paid provider.

#### 14. The proof card claims an Attributed change but never names or links the action it is attributed to

**Surface:** Owner Home · **Type:** `missing_control`

**Evidence**

components/workspace/home-brief.tsx:123-139 renders the "Previous action outcome" card: a signed delta, the metric label, before → after, the window, a FactType badge and the caveat "Temporal association only". Its only link (:138) goes to Insights. The action identity is dropped on the way in: lib/workspace/queries-pages.ts:35-43 defines HomeProof with metricKey/before/after/delta/windowDays/observedAt and no action reference, and :427-437 maps the measurement row while discarding `proofRow.action_id`. The identity is already in hand — lib/repositories/workspace-read.ts:121-128 selects `m.action_id` and already joins `actions a ON a.id=m.action_id`. The destination cannot answer the question either: components/workspace/insights-view.tsx:90 renders measurement cards keyed by metric with no action reference.

**Owner impact**

"Attributed" in this product means a change temporally associated with an action the owner approved and exported (guardrail 4). Home asserts exactly that under the heading "Previous action outcome" / 上一項行動結果, then gives the owner no way to learn which action — not a name, not a link, and Insights does not carry it either. The last step of the loop, Prove change, terminates in a number the owner cannot connect to any work they did, which is precisely the causal-claim-without-provenance the fact-type labels exist to prevent. The traceability only runs the other way: open a specific action and you see its measurement.

**Verifier**

CONFIRMED. The owner's path ends at components/workspace/home-brief.tsx:138 — the proof card's only outbound link, `/{locale}/owner/{slug}/insights` — after the card has asserted an action-linked claim at :124 ("Observed after action" / 「行動後觀察」 whenever factType === "Attributed") and :130 ("Temporal association only"). From there the owner cannot learn WHICH of their approved-and-exported actions the change is attributed to: no name, no link, no id on Home, and the destination cannot answer either — components/workspace/insights-view.tsx:89-91 renders cards keyed by card.metricKey from metricCards() (lib/workspace/queries-pages.ts:566-581), which is computed purely from head/base snapshot.metrics; action_measurements rows never reach the Insights model at all.

The identity is dropped one layer up, not missing from the data: lib/repositories/workspace-read.ts:122-124 already does `SELECT m.id, m.action_id, … FROM action_measurements m JOIN actions a ON a.id=m.action_id`, the row type carries it (queries-pages.ts:130-142, action_id at :132), and lib/workspace/queries-pages.ts:427-437 maps the row field-by-field while never reading proofRow.action_id into HomeProof (:35-43), which has no action field.

Refutation attempts all failed. (1) No control elsewhere: Activity emits nothing for measurements (lib/workspace/measurements.ts has no audit emitter; activity-view.tsx detail() prints scalar payload keys only); the Actions list's `completed` view (queries-pages.ts:458-471) shows no metric or measurement on its cards; the assistant's references are ev_<snapshotId>_<metricKey> (lib/assistant/evidence.ts:169) with no measurement/action ref, and its trigger sits on the priority card (home-brief.tsx:92), not the proof card. Traceability exists only in reverse, at components/workspace/action-detail-client.tsx:555 ("Before and after" inside one action). (2) Not one of the 24 fixed findings: 19+24 (docs/implementation/owner-platform-v1/PHASE-2-GAP-REGISTER.md:89) fixed this card's LOCATION scoping, and the current code passes location?.id — a different defect. (3) Not documented as deliberate: CLAUDE.md §3.5.5 prescribes "proof = most recent action_measurements row (fact type shown)" — silence on action identity, not a recorded decision; the comments in home-brief.tsx and queries-pages.ts cover the delta spread and rescan cadence only. (4) Not forbidden: action_id is already selected and `actions` already joined, so surfacing a title/href is a query+view change with no migration (catalog stays frozen), no cron, no agent_runs write, no publishing and no paid provider.

Severity medium rather than high: the card renders only after a comparable rescan produces a measurement, the assertion is read-only and non-destructive, and today's one-location workspaces have few completed actions to confuse — but it is still the terminal step of the loop ("Prove change") asserting an Attributed association with no reachable referent, which is exactly what guardrail 4's fact-type labels exist to prevent.

#### 15. No dismiss control: the triage queue can only grow

**Surface:** Actions queue · **Type:** `missing_control`

**Evidence**

The list renders a "Dismissed / 已略過" option in its status filter (components/workspace/actions-list-view.tsx:79, labels at lib/copy-workspace.ts:105,162), the API accepts the transition (app/api/actions/[actionId]/route.ts:31-41), the audit event is registered (lib/workspace/audit.ts:12, lib/workspace/audit-labels.ts:13), the browser helper exists (lib/workspace/client.ts:39,100) and CLAUDE.md:552 specifies it. No component ever sends it: the only two calls to updateAction in the whole app pass provided_inputs (components/workspace/action-detail-client.tsx:265,282), and a repo-wide search for action_state in .tsx returns nothing. There is no dismiss, snooze or defer control on the card (actions-list-view.tsx:48-66), on the card footer CTA (actions-list-view.tsx:63) or anywhere on the detail page.

**Owner impact**

The owner's core triage verb -- "not this one" -- has no expression. Every derived action stays open until a future scan resolves its finding (lib/repositories/action-derivation.ts:104-109), and rescans are paid-tier and provider-gated. An owner who has decided not to do Instagram, or who fixed something outside the product, cannot clear it: the queue and the tab counts they triage against grow monotonically and the filter offers a Dismissed view that can never contain anything.

**Verifier**

CONFIRMED. The owner's path ends at components/workspace/actions-list-view.tsx:63 -- the action card's footer contains exactly one control, a Link to the detail page -- and at components/workspace/action-detail-client.tsx:583, where the sticky bar's terminal verbs are Save/Approve/Export. I inventoried every control on the detail page (:480 add review note, :507 save inputs and generate, :526 assistant/regenerate/save edits, :536 approve, :537 request changes, :538 reject draft, :543 export, :544 copy, :545 publish-disabled, :573 version select, :583 sticky bar); all of them act on the output_version, none on the action's own lifecycle. action-detail-view.tsx is a 29-line wrapper with zero controls. The owner is left unable to say "not this one": they cannot move an action to dismissed (decided not to do it) or to completed (fixed it outside the product), so an action they will never act on stays in the queue and in the tab counts at actions-list-view.tsx:91 until a rescan removes its finding.

Every cited fact checks out. The status filter at actions-list-view.tsx:79 includes "dismissed", labelled "Dismissed" (lib/copy-workspace.ts:105) and "已略過" (:162) -- a view that can never contain a row. The server side is complete and unused: app/api/actions/[actionId]/route.ts:31-41 validates action_state as 'dismissed'|'completed', :38-39 stamps completed_at, :125 emits action.dismissed, registered at lib/workspace/audit.ts:12 and labelled at lib/workspace/audit-labels.ts:13; the typed browser helper exists at lib/workspace/client.ts:39,100. A case-insensitive grep for "dismiss" across all of components/ returns a single hit, an unrelated comment on the Sheet close button (components/ui/sheet.tsx:52), and the only two updateAction call sites in the repo (action-detail-client.tsx:265,282) both pass provided_inputs. No .tsx sends action_state. Closure is otherwise scan-only: lib/repositories/action-derivation.ts:104-113 marks an open action completed or expired only when its finding keys leave the new snapshot, which needs a rescan.

None of the refutation routes hold. The control exists on no other page, dialog, shell or settings tab. It is not deliberate: CLAUDE.md:552 specifies the PATCH contract, and no code comment defers it. It is not among the already-fixed findings -- the only "dismiss" entry in the register (PHASE-1-REPORT.md:116, IMPLEMENTATION-TRACEABILITY.md:54) is the Sheet's hardcoded English screen-reader close label, a different control. It is not forbidden by the hard constraints: the action_state column and its 'dismissed' check value are already in the frozen catalog (test/integration/fixtures/legacy-final-catalog.json:3841, and the open-dedupe index at :4692 already excludes 'dismissed'), so no migration is needed, and the work touches no cron, no agent_runs write, no publishing connector, no paid provider, no OAuth and no deployment. This is a UI control over an existing authorized route -- classification missing_control is right.

One correction to the stated impact, which lowers severity rather than refuting: actions_open_dedupe_idx enforces one open action per (workspace, location, template), so the queue is bounded by the template table (~14 per location), not growing without limit. The queue still cannot be reduced by the owner at all, and the same gap silently blocks the 'completed' transition, so an owner who fixed something outside the product also has no way to record it. Medium, not high.

#### 16. Tab counts are computed over the filtered rows, so every tab but the active one reads 0

**Surface:** Actions queue · **Type:** `cosmetic`

**Evidence**

listActions narrows the SQL by the selected view/status (lib/workspace/queries-pages.ts:475 -> lib/repositories/workspace-read.ts:77-83) and then computes the tab badges from that same narrowed set (lib/workspace/queries-pages.ts:477-485). Clicking Completed loads states=['completed'], so `open` is empty and counts.all/needs_input/drafts/awaiting_approval are all 0; choosing any status filter sets states=[status], so counts.completed is 0 and counts.all shrinks to that one status. The badges are rendered straight from that object (components/workspace/actions-list-view.tsx:91) and tabHref preserves the channel/status params across tabs (actions-list-view.tsx:38-46). The existing test only asserts the unfiltered case (lib/workspace/queries-pages.test.ts:129-136), so this is untested rather than a pinned decision. The same object is returned by GET /api/workspaces/[id]/actions (app/api/workspaces/[workspaceId]/actions/route.ts:34-35).

**Owner impact**

The tab badges are the owner's map of the queue. The moment they click Completed or pick any status filter, the rest of the queue reads as empty -- "All 0, Needs input 0, Approvals 0" -- while real open work is sitting there. The owner either believes the queue is clear or has to blindly click back to All to find out. Filter combinations compound it: status=completed plus the Needs input tab yields the "No actions match these filters" empty state with every badge at 0.

**Verifier**

Mechanism reproduced, but the "dead_end" label is not supported — correcting to a display-correctness defect.

What is real: lib/workspace/queries-pages.ts:475 narrows the SQL by the selected view/status (`states = filters.view === "completed" ? ["completed"] : filters.status ? [filters.status] : filters.view && filters.view !== "all" ? OPEN_STATES : undefined`), that single narrowed read at :476 is the ONLY action query in the function (lib/repositories/workspace-read.ts:77-83 applies `action_state=ANY($3)`; overviewsFor at :309-316 only fans out to runs/versions for those same ids — there is no second, unnarrowed count query), and :479-485 computes all five badges from that same narrowed set. So:
- view=completed → `open` (:478) is empty → counts.all / needs_input / drafts / awaiting_approval all render 0 (components/workspace/actions-list-view.tsx:91) while real open work exists.
- any status filter → states=[status] → counts.completed is 0 and counts.all shrinks to that one status; tabHref (:38-46) carries `status` onto every tab, so the narrowing follows the owner across tabs.
- lib/workspace/queries-pages.test.ts:129-136 only asserts the unfiltered call (`{location:"all"}`), so nothing pins the filtered counts; no code comment or CLAUDE.md line documents it; it is not among the 24 register findings (docs/implementation/owner-platform-v1/PHASE-2-GAP-REGISTER.md headings 1-24 + P1-P3 contain nothing about tab counts); the same wrong object ships from app/api/workspaces/[workspaceId]/actions/route.ts:35. Fix is pure read-path logic — no migration, no provider, no forbidden constraint.

Why not dead_end: I cannot name a line where the owner's path ends. Every tab stays a live link (actions-list-view.tsx:91) and clicking a badge that reads 0 loads the correct rows for that tab — from Completed, clicking "Needs input" drops `states` to OPEN_STATES (:475) and both the list and the four open badges come back correct. The status-filter compounding case has a visible reset one control away ("All statuses", ActionFilterSelect at actions-list-view.tsx:97), and the empty state even tells the owner to reset filters (:100). The owner is misinformed, not blocked.

Also noting one overstatement in the headline: "every tab but the active one reads 0" is only true on the Completed tab and under a status filter. On view=needs_input/drafts/awaiting_approval, states=OPEN_STATES, so all four open badges are correct and only `completed` reads 0. The claim's own detailed evidence states this correctly; the summary line does not.

Severity medium: the false zeros can make the owner read "Approvals 0" while drafts await approval, but the default landing (view=all, no status — app/[locale]/owner/[workspaceSlug]/actions/page.tsx:20-26) shows correct counts and recovery is one click.

#### 17. Assignee and due date are rendered on every card but can never be set, and are never resolved even if they were

**Surface:** Actions queue · **Type:** `missing_control`

**Evidence**

Each card renders an Assignee cell and a Due cell (components/workspace/actions-list-view.tsx:62), the detail header repeats Assignee (components/workspace/action-detail-client.tsx:411) and Due (action-detail-client.tsx:413), and the Calendar page has a whole "Actions with a due date / Planned work" panel driven by due_at (components/workspace/calendar-view.tsx:36-39, lib/workspace/queries-pages.ts:657-675). Nothing can write either field: PATCH accepts assignee_user_id and due_at (app/api/actions/[actionId]/route.ts:42-60) and CLAUDE.md:552,743 specify both, but no UI sends them. Worse, the read path never resolves an assignee at all -- overviewsFor omits ctx.assignee (lib/workspace/queries-pages.ts:322-327) so buildActionOverview always falls through to undefined (lib/workspace/overview.ts:176) even though row.assignee_user_id is selected (lib/repositories/workspace-read.ts:15, column at lib/db/schema/business.ts:73).

**Owner impact**

Every card shows "Unassigned" and "Due —" forever, and the Calendar's Planned work panel permanently says "No action has a due date yet." An owner with a manager on the team cannot hand an action over or date it, which is the ordinary way a two-person shop triages a queue; three surfaces advertise a capability the product does not have, in a repo that otherwise pins copy to capability (tests/unhonoured-promises.test.ts:100-121).

**Verifier**

CONFIRMED. The owner's path ends at components/workspace/actions-list-view.tsx:62, where every card renders an Assignee cell (`action.assignee?.name ?? "Unassigned"/"未指派"`) and a Due cell (`action.dueAt ? formatDateTime(...) : "—"`) with no control on that surface, or on the detail page it links to, to set either. The owner is left unable to hand an action to a manager or date it — the two fields are read-only decoration.

Verified there is no writer anywhere in the UI. The browser helper exists (lib/workspace/client.ts:39 declares ActionPatch with assignee_user_id and due_at; :100 updateAction), but its only two call sites in the app send provided_inputs alone (components/workspace/action-detail-client.tsx:265 and :282). A repo-wide search for assignee/due_at/dueAt finds no member picker, date input, or dialog in any component; the only other renders are action-detail-client.tsx:411 (Assignee) and :413 (Due), plus calendar-view.tsx:36-39, whose whole "Actions with a due date / Planned work" panel is fed by getCalendar's `rows.filter((r) => r.due_at)` (lib/workspace/queries-pages.ts:665) and therefore permanently shows "No action has a due date yet."

The second half of the claim is also correct and makes the field unusable even via a direct API call. ActionOverviewContext.assignee (lib/workspace/overview.ts:87) is optional and NO call site of buildActionOverview ever supplies it: queries-pages.ts:322-327, lib/assistant/live.ts:127, live.ts:294, lib/workspace/runs.ts:278, lib/assistant/__fixtures__.ts:110 all omit it. So overview.ts:176 (`assignee: ctx.assignee ?? undefined`) is unconditionally undefined even though row.assignee_user_id is selected (lib/repositories/workspace-read.ts:15) and typed (overview.ts:74). dueAt, by contrast, is read correctly at overview.ts:177 — it simply never gets written.

Refutation routes checked and rejected: (1) the control does not exist elsewhere — team settings manages workspace_members, never action assignment; (2) it is not documented as deliberate — there is no CLAUDE.md deferral and no code comment, and notably calendar-view.tsx:11-19 DOES carry an explicit comment defending the non-dispatching rescan cadence as intentional while saying nothing about due dates being unsettable, so the deliberate limitation in that very file is documented and this one is not; (3) it is not forbidden by the hard constraints — app/api/actions/[actionId]/route.ts:42-60 already validates and persists both fields, lib/repositories/action-mutations.ts:17-18 already allows both columns, and the columns already exist in the frozen catalog (lib/db/schema/business.ts:73-74; test/integration/fixtures/legacy-final-catalog.json:549,558), so closing this needs no migration, no cron, no agent_runs write, no publishing connector, and no paid provider or OAuth/Stripe authorization.

Classification missing_control is correct (the write path exists end-to-end server-side; only the UI control is absent), with a secondary read-path correctness defect at queries-pages.ts:322-327 that must be fixed in the same change or the new control will appear to do nothing.

Severity corrected high → medium: the product's core loop (diagnose → draft → approve → export → measure) is fully intact and unaffected; what is lost is secondary queue triage plus one page panel that can never populate. Real and worth building, but not loop-blocking.

#### 18. Checklist and connection actions offer only a Generate control that always fails

**Surface:** Action detail · **Type:** `dead_end`

**Evidence**

Three templates carry `agentKey: null` — `gbp-profile-fix` (lib/workspace/templates.ts:98-110, capability "Live", delivery "checklist", requiredInputs opening_hours/categories), `ig-highlights` (templates.ts:167-180, capability "Live", delivery "checklist", no required inputs) and `google-reconnect` (templates.ts:237-250, delivery "system", requiredInputs google_account_owner). `google-reconnect` is derived for every workspace with no active Google connection (lib/workspace/actions.ts:214-225), so it is on nearly every workspace. The detail client never learns an agent is absent: `ActionOverview` carries no `agentKey` or `delivery` field (lib/workspace/overview.ts:30-53), and `canGenerate` gates only on capability (components/workspace/action-detail-client.tsx:182), so the "Generate a draft" button is enabled for both Live checklist templates (action-detail-client.tsx:526). Pressing it reaches `resolveAgentKey`, which throws `agent_unavailable` when the template's agent is null (lib/workspace/runs.ts:108-118), returned as 409 (app/api/actions/[actionId]/run/route.ts:51-61) and surfaced as "No agent is available for this action yet." (action-detail-client.tsx:219). The needs_input form is worse: `submitInputs` PATCHes the typed values, toasts "Owner inputs recorded; generating again.", then calls `generate(extraInputs)`, which bypasses the `canGenerate` guard entirely (action-detail-client.tsx:224, 248-270) — so even the disabled-Generate `google-reconnect` action runs the agent and 409s. `delivery: "checklist"` is declared in the template table and rendered nowhere (no "checklist" match outside templates.ts and copy strings), and the page links only to `${base}/assets` and `${base}/settings/billing` (action-detail-client.tsx:502, 542) — never to Settings → Integrations, the one place `google-reconnect` is actually resolved.

**Owner impact**

An owner opening "Complete the Google Business Profile basics" types their opening hours and categories into a form, is told drafting has started, and gets an error toast. "Add Instagram story highlights" errors on a single click of the only button offered. "Restore Google Business access" asks for the Google account owner's name, errors, and never links to the reconnect control. Three action types the product derived and prioritised have no in-product completion path at all — no checklist to work through, no draft, and no route to the real fix.

**Verifier**

CONFIRMED but scoped down to the two Live checklist templates; the google-reconnect third of the claim is refuted.

Verified dead end. templates.ts:101 (gbp-profile-fix) and :171 (ig-highlights) set agentKey: null with capability "Live" and delivery "checklist". action-detail-client.tsx:182 gates canGenerate only on capability !== "Requires connection", so for ig-highlights (requiredInputs [], therefore action_state 'recommended' per lib/repositories/action-derivation.ts:100) the Generate button at action-detail-client.tsx:526 is ENABLED and a single click ends at lib/workspace/runs.ts:112-113, which throws RunError("agent_unavailable") because the template has no agent; app/api/actions/[actionId]/run/route.ts:58-60 returns 409 and action-detail-client.tsx:219 shows "No agent is available for this action yet." For gbp-profile-fix the path is worse: opening_hours/categories are explicitly NOT server-resolvable (lib/workspace/evidence-inputs.ts:26,28), so the action persists as 'needs_input', the form renders, and the submit button at action-detail-client.tsx:507 runs submitInputs -> :269 generate(extraInputs), which bypasses the canGenerate guard at :224 ("if (!canGenerate && !extraInputs) return") -- the owner's typed hours and categories are PATCHed, they are told "Owner inputs recorded; generating again." (:268), and then they get the 409 error toast. delivery: "checklist" is declared at templates.ts:104 and :174 and rendered nowhere (repo-wide grep finds "checklist" only in templates.ts, copy-workspace.ts workflow labels, and unrelated lib/scan-modes.ts).

Owner is left unable to: work through the profile/highlights checklist the template promises. The only forward control on the detail page returns an error, so the action sits in needs_input/recommended with no completion step.

REFUTED portion: the evidence claims google-reconnect is on nearly every workspace and "never links to Settings -> Integrations." Both surfaces that link to actions redirect it away from the detail page: components/workspace/home-brief.tsx:44 and components/workspace/actions-list-view.tsx:50 both read `action.templateKey === "google-reconnect" ? `${base}/settings/integrations` : `${base}/actions/${action.id}``. An owner following the product's own navigation reaches the reconnect control; the failing form is reachable only by typed URL. The "Restore Google Business access asks for the account owner's name, errors, and never links to the reconnect control" impact line is therefore wrong.

ALSO OVERSTATED: "no draft, no in-product completion path at all." The draft textarea (action-detail-client.tsx:510) and "Save manual edits as a new version" (:526) do work for these templates, so a manual version -> approve -> export path exists. What is absent is the checklist and any working generation.

Not refuted on the other grounds I checked: no comment or CLAUDE.md text declares this UI behaviour deliberate (route.test.ts:212 asserts the SERVER refuses safely, which is a different fact); CLAUDE.md section 3.6.1 in fact specifies "Live (checklist)" delivery for both templates, so the checklist is planned-but-unbuilt. Not forbidden by the hard constraints: a fix needs no cron, no agent_runs write, no publishing connector, no OWNER_SELF_SERVICE_CLAIM, no schema change (ActionOverview already carries templateKey at lib/workspace/overview.ts:25, so the client can hide Generate and render a checklist from template data), and no paid provider.

Severity medium, not high: both trigger findings are ordinary and commonly derived, but the manual draft path still exists, no data is lost, and the scope is two templates rather than three.

#### 19. Version-conflict recovery loops: "Load latest safely" never re-bases the editor

**Surface:** Action detail · **Type:** `dead_end`

**Evidence**

`create_output_version` raises `version_conflict` whenever `p_base_version_id` is not the newest version (supabase/migrations/20260903000001_workspace_rpcs.sql:155-161). `saveDraft` always sends `base_version_id: selectedVersion?.id` and, once `conflict` is set, refuses to save at all (components/workspace/action-detail-client.tsx:291, 295-299). The only recovery offered is "Load latest safely" → `loadLatest`, which just clears the flag, calls `router.refresh()` and toasts "Latest state loaded; local text preserved" (action-detail-client.tsx:332-336, 527). But the adjust-on-prop-change block only re-points the editor when the selected version has disappeared from the list (action-detail-client.tsx:144-156) — and versions are immutable, so the stale one is still there. `selectedVersion` stays stale, the next Save posts the same stale base, and the RPC raises `version_conflict` again. The only real escape is clicking the newest row in the History tab, which calls `selectVersion` and overwrites the owner's unsaved text with the other reviewer's body (action-detail-client.tsx:206-213). The conflict panel's own copy promises the opposite — "Unsaved text is preserved locally. Load the latest version, compare, then create a new version" — yet there is no compare view and no way to keep the text while re-basing. A fix is available without schema change: `versions[0]` is already the newest (lib/repositories/workspace-read.ts:102, `ORDER BY v.version_no DESC`), so `loadLatest` can re-point `versionId` while leaving `content` untouched.

**Owner impact**

When a second manager (or the same owner in another tab, or an operator draft created elsewhere) appends a version while the owner is editing, the owner's save is rejected, and the button offered to fix it rejects them again on every retry. Their choice is an unbreakable loop or discarding the edit they just wrote — the exact loss the panel tells them cannot happen. Concurrent editing is a designed scenario: §3.9 grants managers full draft and approval rights on the same action.

**Verifier**

CONFIRMED. Verified each cited line myself. The owner's path ends at components/workspace/action-detail-client.tsx:332-336 — `loadLatest()` only calls setConflict(false), a toast, and router.refresh(); it never re-points `versionId`. The adjust-on-prop-change block at :146-156 cannot compensate: `target` resolves to null because `pendingSelect` is null (set only on success at :301) and `versions.some(v => v.id === versionId)` is true — create_output_version (20260903000001_workspace_rpcs.sql:168-171) only marks old rows 'superseded', never deletes them, so the stale version is always still in the list. `selectedVersion` therefore stays stale, and the next Save at :295 re-posts the same `base_version_id`, which the RPC rejects again at 20260903000001_workspace_rpcs.sql:149-160 (`p_base_version_id is distinct from latest_id` → 'version_conflict'), surfaced verbatim as 409 by app/api/actions/[actionId]/versions/route.ts:78-85 with no retry or re-base, and re-set at :298. Closed loop. The owner is left unable to keep their unsaved draft text and save it as a new version once another member (or another tab) appends one: every retry of the offered recovery button fails identically, and the only working escape — clicking the newest row in History (:206-213) — calls setContent(next.body) and destroys the text they wrote. The conflict panel's own copy at :527/:576 promises "Unsaved text is preserved locally. Load the latest version, compare, then create a new version", yet no compare/diff UI exists anywhere in components/workspace/ and no re-base path exists. Refutation checks all negative: action-detail-view.tsx has no conflict handling; no test exercises client-side recovery (lib/workspace/client.test.ts:60-63 and the route/integration tests only assert the 409 is produced); CLAUDE.md:1090 mandates the conflict UI rather than documenting an inert one, and PHASE-4-REPORT.md:43 claims only "the conflict UI on 409". Concurrency is a designed scenario per §3.9 (managers in scope hold full draft and approval rights). Buildable here: the fix is client-only — loadLatest re-points versionId/altText to versions[0], already newest per lib/repositories/workspace-read.ts:102 (ORDER BY v.version_no DESC), leaving `content` untouched. No migration, no cron, no agent_runs write, no publishing connector, no paid provider — clear of every hard constraint.

#### 20. Every assistant question on Create produces a draft the owner cannot keep, attached to an action they did not choose

**Surface:** Create · **Type:** `dead_end`

**Evidence**

components/workspace/create-view.tsx:118 mounts the assistant with `surface="create"`, `mode="live"`, `context={{ workspaceId, locationId }}` — no actionId and no `onCreateVersion` handler. All four questions offered on this surface are draft intents (components/pocket-assistant/assistant-sheet.tsx:47 → lib/assistant/live.ts:62-68). With no actionId, lib/assistant/live.ts:164-168 silently picks an implicit action (`candidates.find(a => spec.templates.includes(a.template_key)) ?? candidates[0]`), calls the model (live.ts:331) and persists the draft against that action at token cost (live.ts:347-368). The sheet then renders the artifact and the line "An authorised person must approve the exact version" (assistant-sheet.tsx:231), but the redeem button is conditional on `onCreateVersion` (assistant-sheet.tsx:216), which only the action detail page supplies (components/workspace/action-detail-client.tsx:311-321). The `draftRunId` needed by `saveAssistantVersion` is never shown — the sheet displays only the unrelated `run.runId` (assistant-sheet.tsx:197) — and nothing anywhere lists pending assistant drafts.

**Owner impact**

On the page whose job is starting new work, the assistant's only offers are drafts with no exit: the owner reads a finished caption or FAQ, is told it needs approval, and has no control to keep it. The run is recorded and billed against whichever open action matched first — so a draft the owner requested while selecting the "Instagram bio" goal can land in the review-response action's run history with no trace of why.

**Verifier**

CONFIRMED. The owner's path ends at components/pocket-assistant/assistant-sheet.tsx:216 — `{run.draftRunId && onCreateVersion && <Button …>Create a new version…}`. `onCreateVersion` is supplied at exactly one call site in the whole repo (components/workspace/action-detail-client.tsx:526); the Create page's mount (components/workspace/create-view.tsx:118) passes none, and so does the workspace shell's mount (components/product-ui.tsx:456), which resolves `surface = "create"` for this path at product-ui.tsx:371-372 — so there are two create-surface assistants on the page and neither renders the button.

All four offers on this surface are draft intents: assistant-sheet.tsx:47 lists draft_review_reply, generate_social, generate_faq, generate_menu, every one a key of DRAFT_AGENTS (lib/assistant/live.ts:62-68). app/api/assistant/run/route.ts requires only `workspaceId` in context, so these reach runLiveAssistant with no actionId, and live.ts:164-168 silently substitutes one (`candidates.find(a => spec.templates.includes(a.template_key)) ?? candidates[0]`), calls the model (live.ts:332) and writes a terminal `action_runs` row carrying cost_usd against that implicitly chosen action (live.ts:347-371 → lib/repositories/artifacts.ts:151-161).

What the owner is left unable to do: keep the draft. The sheet renders the artifact (line 212) and tells them "An authorised person must approve the exact version" (line 218) while withholding the only control that could act on it. There is no fallback: the redeem token `draftRunId` is never displayed (line 198 shows the unrelated `run.runId`), AssistantArtifactPreview has no copy control, and nothing lists pending drafts — the action detail Workflow tab (action-detail-client.tsx:564) renders runs as `agent_key · state` plus timestamps only, no body and no redeem control, so a draft written from Create is unreachable even after navigating to the action it landed on.

Checked and rejected as refutations: no CLAUDE.md provision or code comment sanctions a create-surface draft without a keep path — §3.8 states the opposite ("'Create a new version' calls POST /api/actions/[id]/versions"), and live.ts's own header comment presumes the owner clicks that control; the redemption route exists and works (app/api/actions/[actionId]/versions/route.ts accepts `assistant_run_id`), so this is a missing wire-up, not a missing capability; the adjacent per-run comments document earlier, different fixes (draft custody/attribution, assistant.run auditing), not this one; and nothing here touches crons, agent_runs, publishing, OWNER_SELF_SERVICE_CLAIM, migrations or paid providers — passing `onCreateVersion` on the create mount is client-side only and buildable.

Two small overstatements in the evidence, neither load-bearing: the run's `input` JSON does record `{source, intent, surface, locale}` (artifacts.ts:157), so the "why" exists in the database — it is simply never surfaced in any UI, and the route's audit event sets entityId null when no actionId is supplied, so Activity does not link it to the action either. Severity medium rather than high because the page's own primary flow (create-view.tsx:140 "Create the action and draft" → router.push to the action detail page) works and reaches a fully controlled draft/approve surface; the harm is a wasted model call, a run row costed against an action the owner did not choose, and an approval prompt with no control.

#### 21. A goal the owner started from their own objective comes back on the next visit labelled "Evidence-led" and stamped Observed

**Surface:** Create · **Type:** `missing_control`

**Evidence**

app/[locale]/owner/[workspaceSlug]/create/page.tsx:23-29 maps every open action to `{ id, templateKey, evidence, freshness }`, dropping both `source` and `evidence.factType` (both available — lib/workspace/overview.ts:28 carries factType, and owner objectives are written as "Recommended" at app/api/actions/route.ts:141 while derived actions are "Observed" at lib/workspace/actions.ts:164). components/workspace/create-view.tsx:66 then treats the mere existence of an open action as evidence backing (`recommended = GOALS.filter(goal => openByTemplate.has(goal.key))`), create-view.tsx:125 badges it "Evidence-led / 證據建議", and create-view.tsx:131-132 heads the panel "Why this is suggested" and renders `<FactType type="Observed" />` against the owner's own typed objective text.

**Owner impact**

The owner's own sentence is played back to them as a measured observation from the scan, and the "Recommended from evidence" tab count is inflated by goals nothing was measured for. That is exactly the fact-type conflation guardrail 4 exists to prevent, on the one page where the two kinds of work are supposed to be told apart — and the same page's own copy (create-view.tsx:133) promises the objective is recorded as Recommended.

**Verifier**

CONFIRMED. Verified end to end. app/api/actions/route.ts:140-147 writes owner objectives as evidence.factType "Recommended", source "Owner objective", detail = the owner's typed text, with action_state recommended/needs_input (both in OPEN_STATES); its own docblock at line 27 states "labelled Recommended (never Observed: nothing was measured)". lib/workspace/queries-pages.ts:473-490 returns every open action regardless of source for view:"all", so that row comes back on the next visit. app/[locale]/owner/[workspaceSlug]/create/page.tsx:24-29 maps it to {id, templateKey, evidence, freshness}, dropping evidence.factType even though ActionOverview carries it (lib/workspace/overview.ts:30, populated at :163). components/workspace/create-view.tsx:66 then treats mere existence of an open action as evidence backing, :122 counts it into the "Recommended from evidence" tab, :125 badges it "Evidence-led / 證據建議" and swaps the template summary for the owner's own sentence, :131 heads the panel "Why this is suggested", :143 appends "· evidence-led", and :132 hard-codes <FactType type="Observed" /> over that sentence — four lines above :133, which tells the owner "The objective is recorded on the action as a Recommended fact". Refutation routes all fail: every other surface renders the stored value dynamically (actions-list-view.tsx:60, action-detail-client.tsx:432, home-brief.tsx:90 all pass action.evidence.factType), so :132 is an outlier rather than a convention, and no other page repairs the Create page's own badge or tab count. The page comment at create/page.tsx:14-18 describes the "open action exists => evidence-led" rule but justifies it with "guardrail 4: the objective is recorded as a Recommended fact, never as a finding" — it documents the intent the code breaks, and it predates this same page gaining the ability to create owner_objective rows, which invalidated its assumption that open actions are finding-derived. git log on both files shows only Phase 4, Phase 5 and the unrelated run-budget fix 0e5a451, so this is not among the already-fixed findings. Not forbidden by any hard constraint: the fix is display-layer only (carry factType/source through the existing ActionOverview field and branch on it) — no migration, no provider, no authorization. Owner's path ends at components/workspace/create-view.tsx:132 (fed by :66, :122, :125): on the one surface whose whole structure is the evidence-led vs owner-objective split, the owner is left unable to tell which goals the scan actually measured — their own sentence is played back as an Observed fact under "Why this is suggested" and the "Recommended from evidence" count is inflated by goals nothing was measured for. Severity corrected to medium, not high: it is a real guardrail-4 fact-type conflation on a primary owner surface, but display-only — the persisted evidence is correct, the queue and detail pages show "Recommended", and no owner task is blocked.

#### 22. Insights never reads action_measurements, so the loop's "Prove change" step has no page — and Home's "View proof" button links here

**Surface:** Insights · **Type:** `missing_control`

**Evidence**

getInsights (lib/workspace/queries-pages.ts:599-647) loads snapshots, diffs and aeo_surface_snapshots only; InsightsModel (lib/workspace/queries-pages.ts:200-209) has no measurement field and components/workspace/insights-view.tsx renders none. repository.measurements is called in exactly two places in the whole app: the Home brief with limit 1 (lib/workspace/queries-pages.ts:411) and one action's detail page (lib/workspace/queries-pages.ts:553). Home's resolved-finding ledger card routes the owner here for the proof: <Link href={withLocation(`${base}/insights`, location)}>View proof / 查看證明 (components/workspace/home-brief.tsx:183). CLAUDE.md §3.1 defines this route's data source as "scan_snapshots series + scan_diffs (comparable flags) + aeo_surface_snapshots trend + action_measurements", and the Phase 6 verification block requires the measurement row to be "shown on Home ... and Insights"; docs/integration/PHASE-6-REPORT.md:47 records it as read by the Home proof card only. The repository method already supports the workspace-wide, location-scoped, limited read this page needs (lib/repositories/workspace-read.ts:121-129), including the deliberate location scoping on the after-snapshot.

**Owner impact**

An owner who approved a draft, exported it and then rescanned gets exactly one measured outcome anywhere in the product: the single newest action_measurements row on Home (limit 1), for the currently selected location. Every earlier proof exists in the database and is reachable only by remembering which action produced it and opening that action's detail page — there is no list of proved outcomes. Clicking "View proof" on a resolved finding lands on Insights, which repeats the word "Resolved" (insights-view.tsx:112) and shows no before/after value, no measurement window, no Attributed/Observed fact type and no link to the version that was exported. The final step of the product loop is persisted but never presented.

**Verifier**

CONFIRMED. The owner's path ends at lib/workspace/queries-pages.ts:599 (getInsights) and lib/workspace/queries-pages.ts:200-208 (InsightsModel): neither touches action_measurements, and components/workspace/insights-view.tsx renders no measurement, so an owner who follows Home's proof links reaches a page that contains no action-attributed outcome. Two links send them there: components/workspace/home-brief.tsx:138, the closing link of the proof card itself, labelled "Inspect before and after / 查看行動前後" (stronger evidence than the claim cited), and home-brief.tsx:183 "View proof / 查看證明" on each resolved finding. repository.measurements has exactly two non-test callers in the app — queries-pages.ts:411 (Home, limit 1, location-scoped) and queries-pages.ts:553 (one action's detail) — so the owner is left unable to see any measured outcome other than the single newest one on Home, and unable to see a history of proved outcomes anywhere. Not deliberate and not documented as such: CLAUDE.md §3.1 lists action_measurements as an Insights data source and the Phase 6 verification block requires the row shown on Home AND Insights, while docs/integration/PHASE-6-REPORT.md:21 falsely asserts "The Home proof card and Insights read them". Not among the 24 register findings (I enumerated all 24 plus P1-P3 in docs/implementation/owner-platform-v1/PHASE-2-GAP-REGISTER.md); finding 17's remedy even says rows stay persisted "for the insights view", assuming a reader that does not exist. Buildable under the hard constraints: lib/repositories/workspace-read.ts:121-129 already provides the workspace-wide, location-scoped, limited read (scoped on the after-snapshot's location so workspace-wide actions survive) — a read plus a render section, no migration, no cron, no agent_runs write, no publishing, no paid provider. Two corrections to the offered evidence, which narrow it rather than refute it: (1) Insights DOES show before/after values with an Observed/Unknown FactType via metricCards (queries-pages.ts:643, the before-after-grid in insights-view.tsx), so "no before/after value, no Attributed/Observed fact type" is overstated — what is genuinely absent is the action-attributed measurement: Attributed fact type, window_days, and the link to the exported version; (2) earlier proofs are browsable, not only recallable: the Actions page exposes a completed view/status filter (app/[locale]/owner/[workspaceSlug]/actions/page.tsx:10-12) and the "Measured" phase badge (lib/workspace/overview.ts:108) leads to the per-action measurement card (components/workspace/action-detail-client.tsx:555) — a worse path, not a missing one. Classification missing_control stands (the page lacks the outcome-evidence section the plan mandates; the two Home links are what makes the absence visible, matching how register item P3 "there is no disconnect" was classified). Severity medium, not high: nothing is fabricated, every row stays honest and reachable through Home and the action detail, and the defect is an incomplete surface plus two links promising more than the destination delivers.

#### 23. Before/after metric cards are labelled "Observed" without the intersection_modules test the assistant applies to the same metric on the same page

**Surface:** Insights · **Type:** `missing_control`

**Evidence**

metricCards (lib/workspace/queries-pages.ts:566-581) sets factType "Observed" and a delta whenever `comparable && before !== null && after !== null`, where `comparable` is the whole diff's flag (lib/workspace/queries-pages.ts:643); the cards render with <FactType> at components/workspace/insights-view.tsx:88-92. The assistant's metricChange for the identical metric refuses that label unless `diff.intersection_modules.includes(diffModuleOfMetric(key))`, citing §3.5.3 (lib/assistant/evidence.ts:99-111, helper at :62-66). intersection_modules holds only modules with status "measured" and a non-null score on BOTH sides (packages/scoring/src/diff.ts:96-101, 202-203); the column is already selected (lib/repositories/workspace-read.ts:14) and typed on ScanDiffRow (lib/workspace/snapshots.ts:57), so the check is available and simply not made. Build note: "website" is not a scored module key (packages/scoring/src/diff.ts:32-37), so it can never appear in intersection_modules — a fix must special-case website.checks_passed on both snapshots' websiteChecks.evaluated > 0 rather than reuse diffModuleOfMetric blindly.

**Owner impact**

Concrete case: in the head scan every google/maps SerpApi run errors and the surviving google_ai_mode run is a Success with no AI answer (`available: true`, `ai_answered: false` — packages/scan-engine/src/serpapi-normalizers.ts:324-332). scoreAEOPerformance then returns status "unavailable" (packages/scoring/src/modules/aeo.ts:196-206) so aeo is absent from intersection_modules, while deriveMetrics still writes `aeo.ai_citation_count: 0` because it filters only on `available !== false` (lib/workspace/metrics.ts:188-195). With ig and gbp still comparable, the card reads "AI citations 0 (−2) · Observed" — a fabricated regression for a surface the diff explicitly refused to compare — on the page that badges "No line across evidence gaps". The Visibility Operator sheet, opened on that same page, reports the same metric as Unknown, so the product contradicts itself about one number.

**Verifier**

CONFIRMED. The owner's path ends at lib/workspace/queries-pages.ts:571 — `const known = comparable && before !== null && after !== null;` where `comparable` is the whole-diff flag passed at :643 (`Boolean(headDiff?.comparable)`). The per-module verdict in `scan_diffs.intersection_modules` is never consulted, so :577 stamps factType "Observed" and :576 emits a delta for a module the diff explicitly refused to compare; components/workspace/insights-view.tsx:90 renders that straight into <FactType>. The control exists in this repo and is applied to the identical metric keys by the assistant (lib/assistant/evidence.ts:104-111, helper :62-66), citing §3.5.3 — so this is a control present on one surface and absent on the other, not an undecided question.

Verified independently, not taken on the evidence's word: (a) METRIC_CARD_KEYS at queries-pages.ts:265 does include `aeo.ai_citation_count` and `website.checks_passed`; (b) packages/scoring/src/diff.ts:98-105 admits a module to intersectionModules only when status==="measured" && score!==null on BOTH sides, and WEIGHTS at :32-37 covers only ig/gbp/aeo/trust; (c) the column is already selected (lib/repositories/workspace-read.ts:14) and typed (lib/workspace/snapshots.ts:59); (d) the fabricated-delta scenario is genuinely reachable — packages/scoring/src/modules/aeo.ts:177 keeps only `available && !unsupported` runs, :182-190 excludes google_ai_mode runs with ai_answered===false, and :196-205 returns status "unavailable" when organic/maps/ai sets are all empty, while lib/workspace/metrics.ts:188-195 still writes aeo.ai_citation_count because its `usable` filter is only `available !== false`; snapshots.ts:226 stores deriveMetrics output without filtering by module state, so the metric survives on a snapshot whose aeo module is unavailable. A brand-query-only google run reaches the same state by a second route.

Refutation routes checked and closed: no other surface applies the check for these cards (metricCards is the sole producer of MetricCard; grep shows only queries-pages.ts and its test reference it); CLAUDE.md §3.5.3 states the opposite of the shipped behaviour ("Per-metric deltas additionally require that metric's module in intersection_modules"), so it is not deliberate-and-documented; it is not among the 24 fixed register findings (docs/implementation/owner-platform-v1/PHASE-2-GAP-REGISTER.md:17-105 — #7 fixed the separate AEO presence-rate card, #6 the Home score dial, and the before/after grid was untouched); queries-pages.test.ts:158 pins only the non-comparable case, so no test asserts the current behaviour as intended; and the fix needs no migration, cron, agent_runs write, publisher or paid provider, so no hard constraint forbids it.

What the owner is left unable to do: distinguish, on the Insights page, which before/after cards are backed by a module the scan measured on both sides. On the page that badges "No line across evidence gaps" (insights-view.tsx:80) a card reads e.g. "AI citations 0 (-2) · Observed" for a surface scan_diffs refused to compare, while the Visibility Operator sheet opened from that same page reports the same metric as Unknown — the product contradicts itself about one number and the owner has no way to tell which answer is the honest one.

Severity medium, not high: it is a read-surface evidence-honesty defect with no authorization, money or data-loss consequence, matching the register's own grading of its two closest analogues (#6 Home score dial, #7 AI-surface trend), both Medium.

Build note carried forward (not a refutation): a fix must NOT reuse diffModuleOfMetric blindly. `website` is not a scored module key (diff.ts:32-37), so website.checks_passed can never appear in intersection_modules and the assistant's rule would permanently mark it Unknown even when both snapshots genuinely evaluated the site. Gate website.checks_passed on both snapshots' websiteChecks.evaluated > 0 instead, and apply the intersection_modules test to gbp./ig./aeo. metrics only. Fixing only the page would also leave the assistant over-refusing on website, so the two surfaces should end up sharing one helper rather than a second copy of the rule.

#### 24. A module that dropped out of the comparison (lost_coverage) is computed, stored and modeled but shown nowhere

**Surface:** Insights · **Type:** `missing_control`

**Evidence**

scan_diffs.lost_coverage / gained_coverage are selected (lib/repositories/workspace-read.ts:14), carried on ScanDiffRow (lib/workspace/snapshots.ts:66-67) and deliberately exposed by the trend model as lostCoverage/gainedCoverage (lib/trends/history-model.ts:34-35, 89-90), documented in the engine as "Measured before, not now. Its findings are unknown, not fixed" and "Not measured before, measured now. Its findings are new to us, not new" (packages/scoring/src/diff.ts:92-95). A repo-wide search for lostCoverage/gainedCoverage returns only the model, the schema and tests — no component reads them; components/workspace/insights-view.tsx uses trend only for showScores/delta/base/head/reasonCode (insights-view.tsx:49-51, 72-77).

**Owner impact**

When Instagram was measured in the base scan and not in the head scan, the pair remains comparable because the composite is recomputed over the intersection (packages/scoring/src/diff.ts:105-120). Insights then shows a confident score delta, "Resolved findings 0" and "Regressed findings 0" (insights-view.tsx:74-76) and never says Instagram left the comparison — so the silence about that module reads as "nothing changed there" when the truth is "we stopped looking". The only hint is the unlabelled coverage percentage in the series table (insights-view.tsx:85), which names no source.

**Verifier**

CONFIRMED — every cited line reproduces, and I could not find the control anywhere else.

Data reaches the component and dies there. `packages/scoring/src/diff.ts:202-203` computes `lostCoverage`/`gainedCoverage` and documents the semantic at :92-95 ("Measured before, not now. Its findings are unknown, not fixed"). It is persisted (`persist-diff.ts:58-59`), selected (`lib/repositories/workspace-read.ts:14`), carried on `ScanDiffRow` (`lib/workspace/snapshots.ts:66-67`), copied into `StoredDiff` by `getInsights` via `toStoredDiff` (`lib/workspace/queries-pages.ts:349-350, 641`), and deliberately exposed on the model at `lib/trends/history-model.ts:89-90`. A repo-wide grep for `lostCoverage|gainedCoverage|lost_coverage|gained_coverage` returns only the engine, the schema, the repositories, the model and tests — zero component reads.

Where the owner's path ends: `components/workspace/insights-view.tsx:49` (`const trend = model.trend`). The component holds `trend.lostCoverage` in its own props and reads only `showScores`, `delta`, `base`, `head`, `reasonCode` (:50-51, :74). The change ledger at :112 renders resolved/regressed/decayed only, and the model type that feeds it is truncated to those three upstream (`queries-pages.ts:207, 644`). The owner is left unable to learn that a source which was measured last scan is not measured this scan, and therefore that the delta, the ledger and the "0 resolved / 0 regressed" tiles cover fewer sources than last time.

Owner impact verified against the engine, not assumed. With IG measured in base and absent in head, `diffScans` takes the comparable path: `intersection = {gbp, aeo}`, `compositeOver` needs only 2 independent channels (:116-120), so `comparable: true` with a real `compositeDelta` (:191-204). Findings are compared only over the intersection (:169-176), so IG findings silently leave the ledger — not counted resolved (correct) and not declared unknown (the gap). Insights then shows a confident "comparable" delta (:74) beside Resolved 0 / Regressed 0 (:75-76).

Refutation paths I checked and rejected:
- Elsewhere in the product: Home shows only counts, not names — `measuredPrimarySources` drives "N measured / M unavailable" (`home-brief.tsx:116`). Integrations names Instagram's state (`integrations-view.tsx:59`) but it is owner-only and describes the current connection, never the comparison. Neither says a module left the comparison.
- Adjacent hints are real but unlabelled, as the finding concedes: the coverage tile (:77) and series table (:85) show a percentage that names no source; `intersection_modules` is used only in `lib/assistant/evidence.ts:108`, never rendered.
- Not one of the 24 fixed findings (`docs/implementation/owner-platform-v1/PHASE-2-GAP-REGISTER.md:17-105`). Finding 7 fixed the AEO surface trend; finding 18 fixed website checks not re-running (a cause of coverage loss, not its disclosure); finding 6 fixed the null-delta zero on Home. None discloses lost coverage.
- Not deliberate: no code comment, design doc or test treats the omission as a decision — unlike the AEO skip, which carries an explicit comment at insights-view.tsx:93-99 and a `skippedScans` surface.
- Not blocked by the hard constraints: the fix is read-model plus view only. `trend.lostCoverage` is already in the component's props, so no migration (catalog stays frozen), no cron, no `agent_runs` write, no publishing, no paid provider or deploy.

One adjacent defect noticed while verifying, not part of this finding: `metricCards` (`queries-pages.ts:566-580`) gates on `diff.comparable` alone and never checks `intersection_modules`, contrary to CLAUDE.md §3.5.3. In this scenario it is masked (the IG metric is absent from head, so `after` is null), though the resulting card then reads "No comparable earlier value" when it is in fact the later value that is missing.

Severity: medium. It is a display-only omission with no persisted or downstream consequence, and partial signals exist (coverage percentage, Home's measured/unavailable counts) — the same reasoning that moved finding 6 from high to medium. It stays a genuine gap because guardrails 1-4 are the product's differentiator and this is the one page that badges "No line across evidence gaps" while staying silent about the gap it knows by name.

#### 25. An asset can never be opened or downloaded in-product, so PDF/menu assets are rights-approved blind

**Surface:** Assets · **Type:** `missing_control`

**Evidence**

components/workspace/assets-view.tsx:52-56 renders a preview only when `asset.signedUrl && asset.kind !== "document" && !asset.storage_path.endsWith(".pdf")` — otherwise the card shows a bare `<FileImage/>` icon. There is no link, open or download control anywhere on the card; `AssetRightsControls` (components/workspace/assets-client.tsx:89-98) offers only an alt-text box, "Confirm rights" and "Reject". The signing helper already exists (lib/workspace/assets.ts:84-87, SIGNED_URL_SECONDS=60) and the list route already returns signed URLs (app/api/workspaces/[workspaceId]/assets/route.ts:29), so the file is reachable — it is simply never offered. Even for images the only rendering is an `<img>` with no open/download affordance.

**Owner impact**

The rights-confirmation control is the one decision this page exists for, and for a `menu` PDF or any `document` the owner is asked to make it without ever seeing the file. A manager uploads a menu PDF; the owner sees a filename and an icon and must either approve rights on an unseen document or leave it in `needs_review` forever. The same applies to any image a teammate uploaded that the owner wants to inspect at full size.

**Verifier**

CONFIRMED. The owner's path ends at components/workspace/assets-view.tsx:52-56: the preview ternary `asset.signedUrl && asset.kind !== "document" && !asset.storage_path.endsWith(".pdf")` falls through to a bare `<FileImage />` icon, and the next and last element on the card is AssetRightsControls (assets-view.tsx:59), which per components/workspace/assets-client.tsx:89-98 offers only an alt-text Textarea, "Confirm rights" (:94) and "Reject" (:95). No anchor, button, dialog or lightbox exists between them. The owner is left unable to open, enlarge or download any asset file, and for a `kind === "document"` or `.pdf` asset cannot see the file at all before pressing Confirm rights or Reject.

Verified it is not offered elsewhere: a repo-wide grep for `signedUrl` yields exactly two UI consumers — assets-view.tsx:55 (the `<img>`) and app/[locale]/owner/[workspaceSlug]/actions/[actionId]/page.tsx:30, which passes `{ signedUrls: false }`, so the draft-editor asset picker (action-detail-client.tsx:500) also lists assets by filename only. app/api/workspaces/[workspaceId]/assets/[assetId]/route.ts is PATCH-only; there is no GET that serves a file and no asset detail page.

The file is genuinely reachable and simply never offered: lib/workspace/assets.ts:80 signs every in-workspace row (the only guard is `storage_path.startsWith(`${workspaceId}/${row.id}/`)`), PDFs included, so the page already holds a live 60 s GET URL for the document it will not show.

Not deliberate: CLAUDE.md §3.1 specifies "private, signed URLs" and Phase 4 item 4 names only upload plus the rights toggle; nothing documents withholding the file. The only nearby comment (assets-view.tsx:53) explains the choice of a plain `<img>` over next/image, not the document exclusion. Not among the 24 fixed findings in docs/implementation/owner-platform-v1/PHASE-2-GAP-REGISTER.md (its finding 4 concerns the live assistant's approved-asset gate, unrelated). Not forbidden: no migration, cron, agent_runs write, publishing connector, or paid-provider/OAuth/Stripe authorization is required — signedUrlFor already exists and already runs on these rows.

Impact is sharpened by app/[locale]/owner/[workspaceSlug]/actions/[actionId]/page.tsx:33, which filters the draft picker to `rights_status === "approved" && asset.kind === "image"`: for a document or menu asset the rights decision is its entire in-product lifecycle, and that is the one decision made blind. Classification missing_control is correct. Severity medium rather than high: images (the class the product actually consumes in drafts) do get a thumbnail, and the blind decision is confined to document/menu rows that sit off the core scan-to-export loop; it is a broken owner-facing control, not a data-integrity or authorization failure. Implementation caveat: SIGNED_URL_SECONDS = 60, so a static `<a href={asset.signedUrl}>` goes stale within a minute — a correct fix likely needs on-demand re-signing via a GET on the existing [assetId] route, which still requires no schema change.

#### 26. The draft panel names the wrong approved asset, and claims one even for text-only posts

**Surface:** Assets · **Type:** `missing_control`

**Evidence**

components/workspace/action-detail-client.tsx:431 renders the "Approved asset" card as `approvedAssets[0].filename` whenever `social && approvedAssets.length > 0`. `approvedAssets` is every approved image in the workspace (app/[locale]/owner/[workspaceSlug]/actions/[actionId]/page.tsx:33), not the one the owner chose — the real choice is written to `actions.provided_inputs.asset_id` (components/workspace/action-detail-client.tsx:256) and is loaded from the database (lib/repositories/workspace-read.ts:15 includes `provided_inputs`) but is never put on the read model, so the view cannot know it. The card is also shown when the owner selected "Text only (no photo)" (components/workspace/action-detail-client.tsx:255).

**Owner impact**

The owner confirms rights on photo C, attaches photo C to the post, and the draft panel tells them photo A is the approved asset — beside a caption the model wrote about photo C. On a text-only draft the same card asserts an "Approved" photo that is not attached at all. This is a fabricated provenance line on exactly the claim guardrail 1 protects, and it makes the whole rights workflow untrustworthy: the owner cannot tell from the product which image they approved for this post.

**Verifier**

CONFIRMED. The owner's path ends at components/workspace/action-detail-client.tsx:431, the only place the product names the asset attached to a social draft: it renders `approvedAssets[0].filename` under the label "Approved asset" with an "Approved" badge, gated solely on `social && approvedAssets.length > 0`. `approvedAssets` is every approved workspace image (app/[locale]/owner/[workspaceSlug]/actions/[actionId]/page.tsx:30-33), not the chosen one. The owner's real choice is written to provided_inputs.asset_id (action-detail-client.tsx:256) and is selected from the database (ACTION_COLUMNS, lib/repositories/workspace-read.ts:15), but buildActionOverview consumes provided_inputs only to compute missingInputs (lib/workspace/overview.ts:139-144) and never exposes it, and ActionDetail carries only {action, versions, runs, measurements, scanInputs} (lib/workspace/queries-pages.ts:166-172) — so the view provably cannot know. Meanwhile the agent DOES use the real choice (lib/agents/agents/social-post.ts:10 passes ctx.providedInputs.asset_id), so the caption can describe photo C beside a card naming photo A. Text-only is a supported state (socialAssetSatisfied, lib/workspace/runs.ts:173 `if (provided.text_only === true) return true`) and the card still asserts an approved photo in it. Aggravating and not in the claim: line 511's alt-text helper tells the owner to "confirm it matches the approved asset" — the only asset named is the wrong one, so the product directs the owner to verify against a file that is not attached. Not a duplicate of the 24 fixes: gap-register finding 4 (PHASE-2-GAP-REGISTER.md:31-35) is the live assistant's pre-model generation gate, a different path; no comment, CLAUDE.md line or test documents this display as deliberate. Nowhere else surfaces the choice — the PATCH audit payload records changed keys only (app/api/actions/[actionId]/route.ts:87-88) and the history tab renders only event/actor/version_no (line 577). The owner is left unable to tell which approved photo this draft is for, and unable to tell that a text-only draft has no photo at all. Buildable: the column is already selected, so the fix is surfacing it on the read model — no migration, provider, cron or authorization. Classification missing_control stands. Severity corrected to medium: the misstatement is display-only — the correct asset_id reaches the agent and export sends body plus alt text (line 376), so the wrong filename never leaves the screen and no data or authorization is corrupted.

#### 27. The approved asset never reaches the export: the deliverable is caption plus alt text with no image and no filename

**Surface:** Assets · **Type:** `missing_control`

**Evidence**

components/workspace/action-detail-client.tsx:376-381 builds the export as `selectedVersion.body` plus an "Alt text:" block and nothing else; `copy` mode copies `selectedVersion.body` alone (line 380). Nothing in the export path references the asset — app/api/versions/[versionId]/export/route.ts has no asset lookup, and `socialAssetSatisfied` (lib/workspace/runs.ts:167-177) enforces the approved asset only at draft time. Combined with the absence of any download control (components/workspace/assets-view.tsx:52-56), the file the owner approved cannot be retrieved from the workspace at all.

**Owner impact**

The loop step this surface feeds is Export/Publish. The owner exports a `.md` containing a caption and alt text, then has to post it on Instagram with a photo the product will not give back and does not name. With two approved photos, or a photo a manager uploaded, the owner cannot tell which image the caption was written for, and cannot obtain it — the rights gate they satisfied produces nothing they can carry to Instagram.

**Verifier**

CONFIRMED, with the scope corrected. The owner's path ends at components/workspace/action-detail-client.tsx:376 — `const text = selectedVersion.alt_text ? `${selectedVersion.body}\n\n---\nAlt text: ${selectedVersion.alt_text}\n` : selectedVersion.body` — written to `${action.templateKey}-v${n}.md` at :378, with :380 copying `selectedVersion.body` alone. `approvedAssets` is in scope on this very component (:110) and `provided_inputs` is written from it (:265), and neither is consulted. app/api/versions/[versionId]/export/route.ts, read in full, does authorize -> exportVersion RPC -> usage/notification and never touches an asset. So the exported deliverable for a social post carries no identity of the rights-cleared photo it was written for.

The real defect is sharper than the one claimed: action-detail-client.tsx:431 renders `approvedAssets[0].filename` — the FIRST approved image in the workspace, not the asset recorded in `provided_inputs.asset_id`. `provided_inputs` is written (:265) and never read back anywhere on the page; app/[locale]/owner/[workspaceSlug]/actions/[actionId]/page.tsx:33 passes only `{id, filename}` and never resolves the selection; and the Select that did name the choice (:500) disappears once `showInputForm` goes false (:181). With two approved photos the panel affirmatively names a file that may not be the one the caption was drafted against — the owner is left unable to tell, from either the screen or the export, which rights-cleared photo the approved caption belongs to.

Two corrections to the claim. (1) "The file cannot be retrieved from the workspace at all" is FALSE: assets-view.tsx:52-56 renders `<img src={asset.signedUrl}>` for every non-PDF image, so the photo is on screen with its filename and is savable from the browser. The absence of an explicit download control is a convenience gap, not a dead end — this is why severity is medium, not high. (2) Bundling the image bytes into the deliverable is foreclosed by CLAUDE.md Phase 4 item 3 ("export = download `.txt`/`.md` via `downloadText`") and guardrail 6; only the asset-identity half is buildable here — resolve `provided_inputs.asset_id` for the :431 panel and append the resolved filename (and location) to the export text. That needs no migration, no provider, no connector, and `signedUrlFor` already exists in lib/workspace/assets.ts:84 if a download control is added alongside.

Not one of the already-fixed findings: PHASE-2-GAP-REGISTER finding 4 covers the live assistant bypassing `socialAssetSatisfied` at draft time, a different path. Nothing in CLAUDE.md or a code comment documents the export omitting asset identity as deliberate.

#### 28. The upload dialog offers locations the uploader cannot manage, and an "All locations" upload becomes unmanageable for the manager who made it

**Surface:** Assets · **Type:** `missing_control`

**Evidence**

components/workspace/assets-client.tsx:64 always renders an "All locations" option plus every workspace location; the list comes from `page.ctx.locations` unfiltered (app/[locale]/owner/[workspaceSlug]/assets/page.tsx:26, lib/workspace/queries.ts:204-219 loads all rows regardless of `location_scope`). Picking another location returns 403 from app/api/workspaces/[workspaceId]/assets/route.ts:63. Picking "All locations" sends no `location_id`, so the scope check is skipped (`...(locationId ? { locationId } : {})` at route.ts:63; lib/auth.ts:112 only checks scope when `opts.locationId` is truthy) and the upload succeeds — but the page then computes `inScope` with `inScopeFor(membership, null)`, which returns false for a scoped manager (lib/workspace/page-context.ts:62-64), so `canDecide` is false on that row forever. The PATCH route disagrees in the other direction: app/api/workspaces/[workspaceId]/assets/[assetId]/route.ts:48-51 skips the scope check entirely when `asset.location_id` is null, so the same manager's rights decision would be accepted over the API.

**Owner impact**

A manager scoped to one location uploads a photo, leaves the location select at its default "All locations", and lands in a state with no exit: the asset is theirs, it sits at `needs_review`, and the Confirm-rights button is permanently disabled for them — only an owner can rescue it, and nothing on screen says so. Meanwhile the server would accept the very decision the UI refuses, so the UI and the authority disagree about the same row (guardrail 9).

**Verifier**

Confirmed at the cited lines, with the classification corrected. The dialog's location list is unfiltered (components/workspace/assets-client.tsx:64, fed by page.ctx.locations at app/[locale]/owner/[workspaceSlug]/assets/page.tsx:26 and components/workspace/assets-view.tsx:43; lib/workspace/queries.ts:204-219 loads every location regardless of location_scope). A scoped manager who picks "All locations" sends no location_id, so app/api/workspaces/[workspaceId]/assets/route.ts:63 omits the locationId option and lib/auth.ts:112 skips the scope check — the upload succeeds. The page then stamps inScope = inScopeFor(membership, null) = false (lib/workspace/page-context.ts:62, applied at assets/page.tsx:17), so components/workspace/assets-view.tsx:59 passes canDecide=false and components/workspace/assets-client.tsx:92,94,95 disable the alt-text field, Confirm rights and Reject on that row permanently, with no on-screen text saying an owner must act. Meanwhile app/api/workspaces/[workspaceId]/assets/[assetId]/route.ts:48-51 runs the scope re-check only `if (asset.location_id)`, so the API would accept the same manager's rights decision the UI refuses — UI and authority disagree about one row (guardrail 9). No other surface offers a rights decision: AssetRightsControls is rendered only in assets-view.tsx. Two corrections to the offered claim. (1) The narrative "leaves the location select at its default 'All locations'" is wrong: assets-view.tsx:35 defaults the dialog to the currently scoped location, and assets/page.tsx:28 computes canUpload via inScopeFor(membership, scopedLocation?.id ?? null), so on ?location=all — the only case where the dialog would default to "All locations" — the Add-asset button is already disabled for a scoped manager. The stuck state needs a deliberate selection of an option the dialog offers with no marking that it is outside the manager's authority. (2) Because the manager can still re-upload under their own location and reach an approved asset, their goal is not blocked; what remains is an orphan needs_review row they can never act on plus the server/UI authority mismatch. That is a missing control (scope-filter the dialog list and close the null-location hole on both the POST and PATCH routes), not a no-exit dead end. Severity medium: it requires a manager with a non-null location_scope plus a non-default choice, causes no data loss, and the server hole only grants rights decisions on workspace-wide assets.

#### 29. Rights can be withdrawn, but nothing shows where the asset was used and the file can never be removed

**Surface:** Assets · **Type:** `missing_control`

**Evidence**

The page intro promises each asset shows "provenance, rights, location and current use" (components/workspace/assets-view.tsx:42), yet the card renders only kind, filename, rights, location and upload time (lines 57-58) — no usage at all, although the link exists in `actions.provided_inputs.asset_id` (components/workspace/action-detail-client.tsx:256; column loaded at lib/repositories/workspace-read.ts:15). Rejecting rights just flips the row (app/api/workspaces/[workspaceId]/assets/[assetId]/route.ts:52-57); nothing re-checks the asset at approval or export (no asset reference in app/api/versions/[versionId]/export/route.ts), because `socialAssetSatisfied` runs only at draft time (lib/workspace/runs.ts:167-177). There is also no DELETE: app/api/workspaces/[workspaceId]/assets/route.ts exposes only GET (line 23) and POST (line 37), and the [assetId] route only PATCH (line 26) — even though storage removal already exists (lib/workspace/assets.ts:124).

**Owner impact**

An owner who discovers a rights problem — a customer's face, a supplier photo used without permission — presses Reject and is told "Rights rejected for this asset", while the approved draft that used it stays approved and exportable and no screen tells them which posts to pull. The file itself stays in the private bucket permanently with no way to delete it, including an obviously wrong upload made seconds earlier.

**Verifier**

CONFIRMED. Two owner paths dead-end, both verified by reading the cited files.

(1) Usage is promised and never rendered. components/workspace/assets-view.tsx:42 states each asset shows "provenance, rights, location and current use"; the card's asset-meta list ends at line 58 with Rights / Location / Uploaded. A tree-wide grep for usedBy|usedIn|currentUse|current_use|inUse returns zero hits — no screen in the app maps an asset back to the actions or versions that consumed it, even though the link is already persisted and already SELECTed: action-detail-client.tsx:256 writes provided.asset_id, and provided_inputs is in ACTION_COLUMNS at lib/repositories/workspace-read.ts:15.

(2) Deletion does not exist. app/api/workspaces/[workspaceId]/assets/route.ts exports only GET (line 23) and POST (line 37); app/api/workspaces/[workspaceId]/assets/[assetId]/route.ts ends at line 76 with PATCH as its sole handler. lib/workspace/assets.ts:124 already calls storage.remove, but only inside the insert-rollback path — unreachable from any route. The owner cannot remove a wrongly uploaded file (PDFs and menus are accepted), ever.

Rights rejection genuinely does not propagate: socialAssetSatisfied (lib/workspace/runs.ts:168-177) is invoked only at runs.ts:326 and lib/assistant/live.ts:319, both draft-time; grep for "asset" across app/api/versions/, app/api/actions/ and lib/workspace/versions.ts returns nothing, so neither approve nor export re-checks.

Two corrections that bound the impact without refuting the gap: an approved version IS still reversible — decide_output_version (neon/migrations/0004_atomic_operations.sql:394) closes only rejected|superseded, and the Reject control (action-detail-client.tsx:538) is disabled only when already rejected — so remediation exists once the owner knows where to look; and v1 delivery is export/copy of text only, so the app itself never distributes the photo. The missing control is therefore discovery (which drafts used this asset) plus removal, not a publication leak.

Buildable under the hard constraints: no schema change (DELETE on assets is already granted to service_role per docs/integration/neon-dependency-map.json:2578; provided_inputs is already selected), no cron, no agent_runs write, no publishing connector, no paid provider.

#### 30. Calendar's "Planned work" card can never populate — no due-date control exists anywhere in the product

**Surface:** Calendar and Activity · **Type:** `missing_control`

**Evidence**

`components/workspace/calendar-view.tsx:36-41` renders a whole SectionCard headed "Actions with a due date / Planned work" whose list is `model.dueActions`. `lib/workspace/queries-pages.ts:665` builds that list as `rows.filter((r) => r.due_at)`. Nothing ever writes `due_at`: the derivation INSERT at `lib/repositories/action-derivation.ts:74` lists every action column except `due_at`, and the only client mutator, `updateAction` (`lib/workspace/client.ts:100`, type at `:39` which does declare `due_at`), is called from exactly two places — `components/workspace/action-detail-client.tsx:265` and `:282` — both passing `provided_inputs` only. The server route already validates and persists it (`app/api/actions/[actionId]/route.ts:52-59`, `lib/repositories/action-mutations.ts:18`), so the backend half was built and the control was never shipped. Two more surfaces advertise the same dead field: `components/workspace/actions-list-view.tsx:62` renders a "Due" cell and `components/workspace/action-detail-client.tsx:413` renders "Due —".

**Owner impact**

Half of the Calendar page is structurally empty for every workspace, forever, always showing "No action has a due date yet." The owner cannot plan work — there is no way to say when an action should be done — yet three surfaces show a Due field, so the owner reasonably hunts for a control that does not exist. The loop step between Prioritise and Draft has no scheduling affordance at all.

**Verifier**

CONFIRMED. The owner's path ends at components/workspace/action-detail-client.tsx:413, which renders the action's due date as static text ("到期：/Due " + action.dueAt ? formatted : "—") inside the .action-detail-next header block with no adjacent input, button or dialog. The owner is left unable to set a due date on any action anywhere in the product.

Verified independently, each claim in the evidence:
1. calendar-view.tsx:36-41 is a whole SectionCard ("Actions with a due date / Planned work") whose sole data is model.dueActions, with permanent empty state at :37.
2. lib/workspace/queries-pages.ts:665 builds it as rows.filter((r) => r.due_at) — the only source.
3. lib/repositories/action-derivation.ts:74 INSERT column list omits due_at, and the ON CONFLICT DO UPDATE SET branch (:77-98) never sets it either. Derivation is not a writer.
4. lib/workspace/client.ts:39 declares due_at in ActionPatch and :100 updateAction is the only mutator; grepping updateAction( repo-wide gives exactly two non-test call sites, action-detail-client.tsx:265 and :282, both passing provided_inputs only.
5. The backend half is built and unreachable: app/api/actions/[actionId]/route.ts:52-59 validates due_at into both patch and audit changes; lib/repositories/action-mutations.ts:18 allow-lists the column; workspace-read.ts:15 selects due_at::text; overview.ts:177 maps it to dueAt.
6. Two further dead surfaces: actions-list-view.tsx:62 ("到期日/Due" cell) and action-detail-client.tsx:413.

Refutation attempts that failed. (a) No control elsewhere: grep -rn 'type="date"|datetime-local|DatePicker|date-picker' over components/, lib/ and app/ returns zero hits; components/ui has no calendar.tsx (dropped in Phase 0.2); no form, dialog, settings tab or shell writes due_at; no direct fetch to /api/actions/[id] outside client.ts. (b) Not deliberate: the only comment on the subject (calendar-view.tsx:11-19) deliberately documents the OTHER card — that the rescan cadence is a reminder nothing dispatches — and about due dates asserts the opposite ("due items are open actions with a due_at"). No doc under docs/ records a due-date deferral. CLAUDE.md section 3.1 specifies Calendar as "scan_schedules.next_run_at + action due dates" and section 3.2.3 lists due_at in the PATCH contract, so it is planned, not excluded. (c) Not forbidden: needs no cron or second scheduler, no agent_runs write, no publishing connector, no claim flag, no paid provider, and no migration — due_at already exists at neon/migrations/0002_business.sql:58 and in the frozen catalog (test/integration/fixtures/legacy-final-catalog.json:558), so db:verify is unaffected. (d) Not already fixed: the control is absent in the current tree.

Classification missing_control is correct: column, validated route field, repo allow-list, read model and three render sites all exist; only the owner-facing control was never shipped.

Severity lowered from high to medium. Nothing is misreported — "No action has a due date yet" and "Due —" are truthful, and the core loop (Prioritise, Draft, Approve, Export) works without due dates, so no evidence or trust guardrail is violated. The real harm is one permanently empty card plus two advertised fields the owner hunts for a control that does not exist.

#### 31. Activity log names no person — every human action is attributed to "Member"

**Surface:** Calendar and Activity · **Type:** `missing_control`

**Evidence**

`lib/workspace/audit-labels.ts:36` maps actor_type `user` to `{ en: "Member", zh: "成員" }`, and `components/workspace/activity-view.tsx:32,36` renders only that label. The identity is loaded and then dropped: `actor_id` is part of the row type (`lib/workspace/queries-pages.ts:219` region, field at `:216`) and is selected by the query (`lib/repositories/workspace-read.ts:156-158`). Resolving it needs no new data — `lib/repositories/membership.ts:59` already lists `workspace_members` (email, role) for a workspace by `user_id`. The prototype design this page ports shows the intended form: `lib/demo-data.ts:292` reads `actor: "Willy Lai · Owner"`.

**Owner impact**

CLAUDE.md §3.11 and guardrail 10 promise an append-only accountability record, and §3.9 gives managers the right to approve, export and rescan. In any workspace with more than one member the log cannot answer the only question an owner asks it — who approved this version, who exported it, who invited that person. Four rows reading "Member · Version approved" are indistinguishable from each other.

**Verifier**

Confirmed by direct read. The owner's path ends at components/workspace/activity-view.tsx:36, where the Actor cell renders only AUDIT_ACTOR_LABELS[row.actor_type] — the constant at lib/workspace/audit-labels.ts:36 maps every human actor to "Member"/"成員". The identity is loaded and then thrown away: lib/repositories/workspace-read.ts:158 selects actor_id in activity(), lib/workspace/queries-pages.ts:216 types it on AuditEventRow, and app/[locale]/owner/[workspaceSlug]/activity/page.tsx:15-16 hands the rows straight to the view; a repo-wide grep shows actor_id has no reader in components/ at all, only writers passing actorId into recordNeonEvent. The control exists nowhere else: the action-detail history tab repeats the dead end at components/workspace/action-detail-client.tsx:577 (auditActorLabel(row.actor_type)), the version list at :573 prints a literal "Member" for author_type 'user', and the approval panel at :534 shows approved_at and reviewer_comment but no approver — output_versions.approved_by is never even selected (lib/repositories/workspace-read.ts:100 selects author_user_id, which no UI reads). Not deliberate-and-documented: no CLAUDE.md clause or code comment declares the anonymization intentional; §3.11 says the Activity page renders "the prototype's actor/event/detail rows" and the prototype form is lib/demo-data.ts:292 actor: "Willy Lai · Owner". Not one of the 24 fixed findings: register item 12 (docs/implementation/owner-platform-v1/PHASE-2-GAP-REGISTER.md:430-450, closed by commit 2eb86f3) concerns the missing assistant.run emitter and agent-vs-user authorship, never that a person should be named. Not forbidden: resolution needs no migration, no cron, no agent_runs write, no publishing and no paid provider — lib/repositories/membership.ts:58-60 team() already returns user_id, email and role, and components/workspace/team-view.tsx:52 already renders member emails, so identity display is not categorically avoided by design. Owner impact: in any workspace with more than one human member, the owner cannot tell which person approved a version, exported a delivery, invited or removed a member, dismissed an action, or edited the brand profile — four version.approved rows are byte-identical in the Actor column. Two build constraints: app_users carries only email (neon/migrations/0001_identity.sql:12-16, no display-name column), so the label resolves to an email plus role rather than a person's name; and audit_events.actor_id deliberately has no FK (test/integration/neon-integrations.integration.test.ts:411), so a departed member's id needs a "Former member" fallback. Activity is readable by viewers while Team is owner-only, so the email disclosure needs role scoping. Severity medium rather than high: the append-only record itself stays intact in the database and no workflow is blocked — what is lost is the owner-facing attribution.

#### 32. Activity shows only the newest 100 events, with no paging and no notice that it is truncated

**Surface:** Calendar and Activity · **Type:** `missing_control`

**Evidence**

`lib/workspace/queries-pages.ts:653-655` calls `repository.activity(ctx.workspace.id, opts.limit ?? 100)`, and the page (`app/[locale]/owner/[workspaceSlug]/activity/page.tsx`) never passes a limit or an offset. `lib/repositories/workspace-read.ts:156-158` is a plain `ORDER BY created_at DESC LIMIT $2` with no cursor. `components/workspace/activity-view.tsx:24-44` renders the table with no filter, no date range and no load-more control, and the caption at `:26` says only "Workspace events, latest first" — nothing indicates that older events exist.

**Owner impact**

Guardrail 10's append-only record is only readable for as long as it is short. Once a workspace passes 100 events (scans, per-snapshot derivations, runs, versions, approvals, exports, assistant runs, member changes all write rows), older history silently disappears with no hint, so an owner checking whether something was ever approved or exported can conclude it never happened.

**Verifier**

CONFIRMED. The owner's path ends at app/[locale]/owner/[workspaceSlug]/activity/page.tsx:15, which calls getActivity(page.ctx) with no options; lib/workspace/queries-pages.ts:654 therefore applies opts.limit ?? 100, and lib/repositories/workspace-read.ts:157-159 issues a plain "FROM audit_events WHERE workspace_id=$1 ORDER BY created_at DESC LIMIT $2" with no OFFSET, no cursor and no total count. components/workspace/activity-view.tsx:21-45 ends the table at </TableBody> with no pager, date filter or load-more, while the caption at :26 says "Workspace events, latest first" and the intro at :23 bills the page as the "Append-only owner audit". The owner is left unable to see, or even learn of the existence of, any audit event older than the newest 100 — no control reaches row 101 and nothing on the page says rows were withheld. Refutation attempts all failed: (a) the only other getActivity caller, actions/[actionId]/page.tsx:29, asks for 200 workspace-wide rows and filters by entity id (:32), so for an action older than that window action-detail-client.tsx:577 renders the affirmatively false "No audit events yet." — it worsens rather than covers the gap; (b) no comment or doc records the cap as deliberate (the only comment near activity() is the pageLimit guard at workspace-read.ts:17-20, and CLAUDE.md 3.11 caps nothing); (c) it is not among the 24 numbered entries in docs/implementation/owner-platform-v1/PHASE-2-GAP-REGISTER.md; (d) it is buildable within the hard constraints — keyset/offset paging plus a truncation notice is a read-query and UI change, needing no migration (so scripts/neon/catalog.ts stays intact), no cron, no provider and no publishing. Volume is realistically reached: action.derived writes one row per snapshot (lib/repositories/action-derivation.ts:116) and each drafted-to-exported action emits run.started, run.succeeded, version.created, version.approved and delivery.exported, so a paid workspace on unlimited deliveries crosses 100 rows within a few months. One correction to the stated impact: current approval and delivery state is still visible on the action detail, so an owner would not conclude an approval "never happened" as flatly as claimed; what silently disappears is the provenance (which actor, when, from which fingerprint) that guardrail 10's append-only record exists to preserve — plus the action-detail history tab's false empty state for older actions. Classification missing_control is correct: the data exists and is authorized, only the control to reach it (and the honesty notice that it is truncated) is absent.

#### 33. No control anywhere to add or correct the website URL, so "Not evaluated" is permanent

**Surface:** Settings: brand and integrations · **Type:** `missing_control`

**Evidence**

The website card is read-only by construction: `action: null` (components/workspace/integrations-view.tsx:62-70). Website state falls to `unsupported` / `WEBSITE_URL_NOT_PROVIDED` whenever no URL was captured (lib/workspace/module-states.ts:79), and the 15 checks are keyed off the job row only — `websiteUrlOf(job)` reading `audit_jobs.website_url` / `input_snapshot` (lib/workspace/website-evidence.ts:47,54-55; lib/workspace/snapshots.ts:126-129). `locations.website_url` has exactly one writer, `claimsRepository.updateLocation` (lib/repositories/claims.ts:145-146), whose only call site is the one-time claim (lib/workspace/claim.ts:219); no settings page, route or form updates it afterwards (no match for a location-edit route under app/api/workspaces/**). A rescan re-reads the URL from the frozen snapshot as well (lib/workspace/rescan.ts:87).

**Owner impact**

The website field is optional in the scan wizard, and SMEs launch sites after their first scan. Such an owner has no way to tell the product their site exists: the Integrations page shows "Not evaluated" with no input, "3 of 4 primary sources" stays wrong forever, and the website-derived templates (website-basics, and the faq_schema trigger for visibility-content) can never be derived. The one surface named "Integrations" shows the website as a source while offering no way to connect it.

**Verifier**

CONFIRMED. The owner's path ends at components/workspace/integrations-view.tsx:68 — the website card's `action: null`, the only one of the three integration cards with no writer (Google has connect/re-authorise/disconnect at :48, Instagram has InstagramHandleForm at :58), while :65 still declares `capability: "Live"` and renders it as a Live badge at :79 next to "Not evaluated" at :64. An owner who left the optional website field blank in the scan wizard (components/scan-page.tsx:528, the only website-URL input in the app; :185 renders "not provided") is left unable to tell the product their site exists, ever.

Every citation reproduced. lib/workspace/module-states.ts:79 returns unsupported/WEBSITE_URL_NOT_PROVIDED when no URL was captured, capping measuredPrimarySources (:97-99) at 3 of 4. The URL is read only from the job row — websiteUrlOf (lib/workspace/snapshots.ts:126-131) checks audit_jobs.website_url, then input_snapshot, then raw_data.aeo.website.url, and has exactly two non-test callers (snapshots.ts:219, website-evidence.ts:54), neither of which reads the locations table. locations.website_url has one writer, claimsRepository.updateLocation (lib/repositories/claims.ts:145-146) / insertLocation (:149-150), called only from lib/workspace/claim.ts:219 and :222 inside the one-time claim. Rescan cannot carry a new URL: rescan.ts:87 rebuilds websiteUrl from the frozen parent input_snapshot and EnqueueRescanInput (:114-128) has no override. No surface elsewhere: the five owner settings pages are billing/brand/integrations/notifications/team with no location page, none of the twelve dirs under app/api/workspaces/[workspaceId]/ edits a location, brand has no website field, and onboarding only displays the URL read-only (components/onboarding-page.tsx:216).

Not refutable on any of the four escape routes. (a) Not one of the 24 fixed findings: register finding 18 (PHASE-2-GAP-REGISTER.md:574-594) concerns checks never being RECOMPUTED for a job that already has a URL, fixed by collecting them in lib/scan/run.ts before the completion claim — that does nothing for a job that never had one, and no entry covers supplying a URL. (b) Not documented as deliberate: CLAUDE.md §3.1 and Phase 6 item 3 name writers for Google and Instagram and merely omit one for website; there is no code comment defending `action: null` (the comment at :45-47 is about the Google disconnect). (c) Not forbidden by the hard constraints: both audit_jobs.website_url and locations.website_url already exist so no migration is needed and scripts/neon/catalog.ts stays satisfied; runWebsiteChecks is an unauthenticated globalThis.fetch (lib/website/checks.ts:158), not a paid provider; nothing touches crons, agent_runs, publishing or OWNER_SELF_SERVICE_CLAIM.

Two corrections. (1) The claim's '"3 of 4 primary sources" stays wrong forever' overstates: unsupported/WEBSITE_URL_NOT_PROVIDED/"Not evaluated" is truthful, so there is no guardrail 1/2 violation — the defect is a permanently unreachable capability, not a fabricated number. Severity medium on that basis (no severity was claimed; medium matches finding 18's rating for the same subsystem), with high likelihood since the field is genuinely optional in the wizard. (2) For the builder: a form alone is insufficient. Because websiteUrlOf never reads the locations table and scanInputFromSnapshot reads the frozen parent snapshot, writing locations.website_url would change nothing — the fix must also reach lib/workspace/snapshots.ts:126 and/or lib/workspace/rescan.ts:87, and note that post-finding-18 the checks run only in lib/scan/run.ts before completion, so a newly supplied URL takes effect on the next scan unless the fix also patches the current snapshot's website_checks on demand.

#### 34. Connect / Re-authorise Google is a plain link to an API route that renders raw JSON when it refuses

**Surface:** Settings: brand and integrations · **Type:** `dead_end`

**Evidence**

The card's primary action is a full-page anchor: `<a href={`/api/oauth/google/start?workspace=…&locale=…`}>` (components/workspace/integrations-view.tsx:48). That route answers with bare JSON on every refusal path — `{"error":"unavailable"}` 503 when Google OAuth env is not configured (app/api/oauth/google/start/route.ts:32-34), `{"error":"unauthenticated"}` 401 on an expired session (:50), `{"error":"no_workspace"}` 403 (:72) — so the owner lands on a JSON document outside the app shell with no back path. The page is a server component and could read the same flag: `googleOAuthConfigured()` is a pure env check (lib/oauth/google-connection.ts:44-52). Onboarding already does exactly this, gating its Google CTA on a server-read flag and rendering an alternative when it is off (app/[locale]/owner/onboarding/page.tsx:157; components/onboarding-page.tsx:233).

**Owner impact**

On any deployment where Google OAuth credentials are absent — the default state, since provider setup is separately authorized — every owner who clicks the integrations page's main button is navigated out of the product onto a raw error blob. The page meanwhile keeps advertising the connection as available (`Requires connection` capability, "Connect Google"), so the owner has no way to learn that connecting is simply not offered here yet.

**Verifier**

Verified against the files. components/workspace/integrations-view.tsx:48 renders the Google card's sole action as an unconditional full-page anchor `<a href={`/api/oauth/google/start?workspace=…&locale=…`}>` (labelled "Connect Google"/"Re-authorise"), and a repo-wide grep shows that href appears in no other .tsx — there is no dialog, tab, shell control, or fallback branch offering the same thing. The owner's path ends at app/api/oauth/google/start/route.ts:34, the handler's first statement: `if (!googleOAuthConfigured()) return NextResponse.json({ error: "unavailable" }, { status: 503 })`, reached by a top-level browser navigation, so the browser paints `{"error":"unavailable"}` as a bare JSON document outside the app shell. Same shape at :46 ({"error":"unauthenticated"} 401 on an expired session) and :51/:73 ({"error":"no_workspace"} 403). This is the default deployment state, not a corner case: googleOAuthConfigured() (lib/oauth/google-connection.ts:44-55) demands GOOGLE_OAUTH_CLIENT_ID, _CLIENT_SECRET, _REDIRECT_URI and a >=32-byte OAUTH_TOKEN_ENCRYPTION_KEY (stateSigningKey, :79-99, throws in production without it), and provider setup is separately authorized, so the 503 branch is the one that fires. The owner is left unable to learn that connecting Google is not offered on this deployment and unable to reach any next step from the card — the page keeps advertising the action ("Requires connection" capability, "Connect Google") with no in-app recovery path. Refutation attempts all failed: (a) the control exists nowhere else; (b) the component's own header comment (:13-19) describes the link as the Phase 2 route and never documents an unconfigured state as deliberate, and no CLAUDE.md rule covers it; (c) the PHASE-2 gap register's entry on this route (lines 317-350) is a different, already-fixed finding about manager-vs-owner gating — the route is now owner-only at :72 — and nothing in the register covers the unconfigured-OAuth JSON landing; (d) it is not forbidden by the hard constraints, because the fix needs no OAuth consent, paid provider, migration, cron or publishing connector — the page is a server component (app/[locale]/owner/[workspaceSlug]/settings/integrations/page.tsx:14-17) and googleOAuthConfigured() is a pure env predicate it can call directly, exactly as onboarding already does with a server-read flag (app/[locale]/owner/onboarding/page.tsx:157) and renders a real alternative when it is off (components/onboarding-page.tsx:238-248). IntegrationsModel (lib/workspace/queries-pages.ts:224-228) carries no such flag today. Classification dead_end stands. Severity medium: the primary action of an owner-only settings page fails this way on the default configuration, but nothing is corrupted or exposed and browser Back still returns to the app.

#### 35. Brand profile promises versioning and draft provenance that no stored data supports

**Surface:** Settings: brand and integrations · **Type:** `missing_control`

**Evidence**

The page's save button reads "Save new version" and a whole card asserts "Every save is a new version" / "Drafts cite the profile that was current when they were generated; an audit event records each change" (components/workspace/brand-client.tsx:148,159). There is no version: `putBrand` performs a single upsert over the one `brand_profiles` row (lib/workspace/brand.ts:151), so the previous profile is destroyed and unrecoverable. The `brand.updated` audit payload stores only counts — voice, languages, and the lengths of claims/terms/facts (lib/workspace/brand.ts:162-168) — and the Activity feed renders it as the bare label "Brand profile updated" (lib/workspace/audit-labels.ts:28), so nothing records what changed. Drafts cannot cite the profile either: `action_runs.input` stores `agent_key`, `provided_inputs`, `snapshot_id`, `prompt_version`, `locale` and no brand reference (lib/workspace/runs.ts:302-308), and version meta carries only the agent's self-reported `facts_used` and `prompt_version` (lib/workspace/versions.ts:131-142).

**Owner impact**

An owner who tightens their approved claims or prohibited terms cannot see what the profile said before, cannot restore it, and cannot tell which already-approved drafts were generated under the old guardrails — while the page tells them both are recorded. Under guardrail 1 (evidence before score) and the approval boundary of guardrail 5, this is the one surface where the owner sets the rules agents must obey, and its own change history is the thing that is not evidenced.

**Verifier**

CONFIRMED. The owner's path ends at lib/repositories/brand.ts:14 — `ON CONFLICT(workspace_id) DO UPDATE SET voice=excluded.voice,approved_claims=excluded.approved_claims,prohibited_terms=excluded.prohibited_terms,languages=excluded.languages,facts=excluded.facts,updated_at=excluded.updated_at` — a single-row upsert over a table whose sole primary key is `workspace_id` (lib/db/schema/business.ts:259-269). I searched lib/db/schema/, scripts/neon/catalog.ts and all SQL for any brand history/version/snapshot table: none exists. The prior approved_claims and prohibited_terms are overwritten in place and are unrecoverable, while components/workspace/brand-client.tsx:146,148,159 tells the owner across all three locales that this is "Versioned workspace guardrails", that the button is "Save new version", that "Every save is a new version", and that the save succeeded "as a new version" — repeated to non-owners at components/workspace/brand-view.tsx:19. The owner is left unable to see what the profile said before, unable to restore it, and unable to tell which already-approved drafts were generated under the old guardrails.

The draft-provenance half is equally unsupported: lib/workspace/runs.ts:302-308 stores only {agent_key, provided_inputs, snapshot_id, prompt_version, locale}; the run-path version meta at lib/repositories/artifacts.ts:312 is {title, acceptance_criteria, warnings, facts_used, agent_key, prompt_version}; the assistant path at lib/workspace/versions.ts:131-143 adds only origin/intent. No brand reference on either path, not even the profile's updated_at — so brand-client.tsx:159 "Drafts cite the profile that was current when they were generated" is false. The brand content IS read at generation time (runs.ts:266-272), so drafts are correctly constrained; they are merely untraceable.

Two corrections to the offered evidence, neither refuting it. (1) The Activity feed does NOT render a bare label: components/workspace/activity-view.tsx:8-17 prints every scalar payload entry, so brand.updated shows "voice warm · approved_claims 3 · prohibited_terms 2 · facts 5" (the `languages` array is skipped as an object). That is counts and voice — still no content and no before/after, so the substance holds. (2) "an audit event records each change" is literally satisfied by lib/workspace/brand.ts:153-169 (one event per save); the false claims are the versioning and draft-citation ones.

Not previously fixed: I read the fixed list in docs/implementation/owner-platform-v1/PHASE-2-GAP-REGISTER.md (findings 1-24 plus P1-P3) and grepped it for brand-profile/versioning entries — absent, though this is the same promises-copy family as confirmed findings 5, 13, 14, 15 and P1. Not documented as deliberate: lib/workspace/brand.ts:4-9 ("One row per workspace, upserted on the primary key") corroborates the finding, and CLAUDE.md §3.3 specifies brand_profiles keyed on workspace_id with no version table — so the schema is deliberate and the copy is what over-promises. NOT forbidden_by_plan: the fix needs no migration (frozen catalog untouched) — correct the copy, per the register's own precedent for findings 5/13/14/P1, and close the provenance half inside existing jsonb columns this app already writes (audit_events.payload for a before/after diff, action_runs.input and output_versions.meta for a brand_updated_at stamp).

Severity corrected to medium: no money-bearing field, no security boundary, no wrong customer-facing number, and the guardrails are genuinely applied to every draft. The real harm is a page that invites destructive editing behind a safety net that does not exist, plus approved and exported drafts that cannot be tied to the guardrails in force when they were written (guardrails 1 and 5).

#### 36. A paid workspace with no stored Stripe customer has only a button that always fails

**Surface:** Settings: team and billing · **Type:** `dead_end`

**Evidence**

components/workspace/billing-view.tsx:57 renders the owner's single billing control as `<BillingActions paid={paid && model.stripeCustomer} />`, so tier='paid' with a null `stripe_customer_id` yields paid=false. lib/owner/billing-client.ts:64 then dispatches that to `createOwnerCheckoutLink`, and app/api/workspaces/[workspaceId]/checkout-link/route.ts:75-77 returns 409 `already_paid` for any paid workspace; components/workspace/billing-actions.tsx:28 turns that into the toast "This workspace is already on the paid plan." The Billing Portal is unreachable in that state too (app/api/workspaces/[workspaceId]/billing-portal/route.ts:50-52 returns 409 `no_subscription`), and nothing else on the page links to a human. The state is reachable: (a) the Stripe webhook sets tier from `metadata.workspace_id` alone (app/api/webhooks/stripe/route.ts:175-190, 205-211) and never persists the customer — lib/repositories/billing.ts:97 `saveCustomer` has exactly one caller, app/api/workspaces/[workspaceId]/checkout-link/route.ts:111 — so a subscription created anywhere but this app's own Checkout leaves the column null; (b) a `staff_grant` tier change, which this very page renders as a ledger source (components/workspace/billing-view.tsx:81) and names in its own copy (components/workspace/billing-view.tsx:64). No test covers the branch (lib/workspace/billing.test.ts:108,122 assert only the flag).

**Owner impact**

An owner who is being charged (or was granted the paid tier by Fimmick) sees "Growth Workspace · Subscription active · Your subscription is managed by Stripe" next to a button labelled "Subscribe via Stripe" that can only ever answer "you are already on the paid plan". There is no way to update a card, download an invoice, or cancel, and no contact route offered — the page contradicts itself and the subscription-management job cannot be finished in-product.

**Verifier**

CONFIRMED. The owner's path ends at components/workspace/billing-actions.tsx:39 — the single billing control — dispatched via lib/owner/billing-client.ts:64 into app/api/workspaces/[workspaceId]/checkout-link/route.ts:75-77, which returns 409 'already_paid' for every paid workspace, rendered as the toast at components/workspace/billing-actions.tsx:28. The cause is components/workspace/billing-view.tsx:57, `paid={paid && model.stripeCustomer}`: with tier='paid' and stripe_customer_id null (model.stripeCustomer is Boolean(stripe_customer_id), lib/workspace/billing.ts:74-79) the flag collapses to false, so a paid owner gets the Checkout button instead of the Portal button, and the Portal is unreachable anyway (app/api/workspaces/[workspaceId]/billing-portal/route.ts:50-52 returns 409 'no_subscription'). The owner is left unable to update a payment card, retrieve an invoice, or cancel the subscription, and unable to reach a human about it: the billing page's only outbound link is components/workspace/billing-view.tsx:93 to /pricing, whose managed-plan CTA points at /trust (components/public-pages.tsx:104), and the owner surface index (components/workspace/more-view.tsx:11-19) links only back to this same page. getMarketCtas is wired into onboarding (app/[locale]/owner/onboarding/page.tsx:148) and the report but never into billing. Meanwhile the page asserts the opposite of what it can do: 'Growth Workspace', 'Subscription active' (billing-view.tsx:54) and 'Your subscription is managed by Stripe' (billing-view.tsx:62). Reachability verified rather than assumed: lib/repositories/billing.ts:97 saveCustomer has exactly one caller, checkout-link/route.ts:111, and the webhook (app/api/webhooks/stripe/route.ts:174-190, 204-211) applies the tier from metadata.workspace_id without ever persisting the customer id, so any subscription created outside this app's own Checkout leaves the column null; and staff_grant is a first-class product state — DB check constraint neon/migrations/0002_business.sql:586, exercised at test/integration/neon-integrations.integration.test.ts:522 inserting tier='paid', source='staff_grant', rendered in the ledger at billing-view.tsx:81 and named in the page's own copy at billing-view.tsx:64 — and a staff grant creates no Stripe customer by construction. Refutation checks all failed: no control exists elsewhere; nothing in CLAUDE.md or any code comment documents the paid-without-customer third state (the BillingModel comment at lib/workspace/billing.ts:33 justifies only the Portal's precondition); it is not among the 24 fixed findings in docs/implementation/owner-platform-v1/PHASE-2-GAP-REGISTER.md:17-105 (#2 is the mid-period allowance, #15 is the onboarding staff-assignment fallback); and lib/workspace/billing.test.ts:108,122 assert only the boolean, never this render branch. Not forbidden by the hard constraints: the correction is render-state plus the market contact channels the repo already resolves server-side for onboarding — no migration, no Stripe call, no cron, and no entitlement write, consistent with the page's own statement at billing-view.tsx:64 that it never edits entitlements. Classification dead_end is correct (a reachable control that always fails, with no alternative path); suggested severity medium — narrow but real population, self-contradictory and unresolvable in-product, no data loss.

#### 37. Creating an invite produces nothing the owner can actually send to the colleague

**Surface:** Settings: team and billing · **Type:** `missing_control`

**Evidence**

app/api/workspaces/[workspaceId]/members/route.ts:53-73 writes the pending `workspace_members` row and the audit event, and returns `{memberId}` — no mail, no token, no link. components/workspace/team-client.tsx:51-55 just toasts and closes the sheet; the copy at components/workspace/team-client.tsx:167-168 says "The invite is saved here; the member joins by signing in with this address" / "Ask them to sign in with this address", but never shows the owner the address to send them to. Mail only exists in the opposite direction: app/api/workspace-invites/magic-link/route.ts:66 sends a link only when the invitee themselves requests one (via components/auth/sign-in-flow.tsx:91 → app/api/auth/[...path]/route.ts:43-53, gated on `hasSignInMembership`). The team table (components/workspace/team-view.tsx:52) then shows "Invite pending" with no resend, no link, and no way to hand anything over.

**Owner impact**

The owner's core job on this page ends half-done: the row exists, but the colleague is never told anything, and the product never gives the owner the sign-in URL or the "use exactly this address" instruction to paste into WhatsApp/LINE/email. Invites will sit pending because the invitee was never reachable, and the owner has no signal distinguishing "not told yet" from "ignored it".

**Verifier**

CONFIRMED, with one correction to the evidence. The owner's path ends at components/workspace/team-client.tsx:52-55: after a successful POST the handler only calls toast.success(t.invited), setOpen(false), setEmail(""), router.refresh(). Upstream of it, app/api/workspaces/[workspaceId]/members/route.ts:53-73 writes the pending workspace_members row and the member.invited audit event and returns {memberId} only — no token, no link, no dispatch in the file. Downstream, components/workspace/team-view.tsx:52 renders the row as "Invite pending · <date>"; the owner-only controls on that row (:53-57) are role select, location scope and remove — no resend, no link, no copy. Mail runs only in the opposite direction: app/api/workspace-invites/magic-link/route.ts:66 gates on hasSignInMembership, and lib/repositories/membership.ts:50-57 confirms a pending row qualifies, but that route is reached only when the invitee themselves is already standing on /{locale}/owner/sign-in. So the owner is left unable to hand the colleague anything that gets them to the sign-in page: no URL, no copyable message, no resend, and no state beyond "pending" to distinguish "never told" from "ignored".

Refutation attempts that failed: (1) No control elsewhere — the only navigator.clipboard use under components/ is scanning-page.tsx, and "owner/sign-in" appears in no component or copy file, only in proxy logic, tests and e2e. (2) Not deliberate-and-documented — the reverse: components/workspace/team-client.tsx:18-25 and docs/integration/PHASE-6-REPORT.md:27 both assert the invite route "sends the magic link", which the route does not do; that is stale documentation, not a recorded decision to omit the handoff. The one real code comment on this (team-client.tsx:164-166) only justifies not CLAIMING a send; it says nothing about the owner having no artifact. (3) Not already fixed — PHASE-2-GAP-REGISTER.md:114 records P1.7 removing the false "email sent" toast on the team invite, which removed the false claim without adding a handoff. (4) Not forbidden by the hard constraints: the buildable fix is owner-facing only — surface the locale-correct sign-in URL plus the exact invited address with a copy affordance on the invite confirmation and on each pending row. No mail, no provider, no schema change, so it does not touch the crons/agent_runs/auto-publish/OWNER_SELF_SERVICE_CLAIM/migration/paid-provider prohibitions. Only an email-dispatching variant would be needs_authorization.

Correction, which is why I set severity to medium rather than high: the claim that the product "never gives the owner ... the 'use exactly this address' instruction" is wrong. team-client.tsx:167 ("the member joins by signing in with this address") and the :168 success toast ("Ask them to sign in with this address.") say exactly that in all three locales. What is actually missing is the destination URL and any shareable artifact or resend. Since the sign-in page is a public URL on the same origin the owner is already browsing, the owner can improvise the handoff by copying it from the address bar — degraded and unguided, not a dead end.

#### 38. Ownership has no handover or exit — the owner row is permanently frozen with no explanation

**Surface:** Settings: team and billing · **Type:** `dead_end`

**Evidence**

components/workspace/team-view.tsx:49 sets `editable = owner && member.role !== "owner"`, so the owner row gets no role select, no scope control and an empty Actions cell (components/workspace/team-view.tsx:53,57). The server matches: app/api/workspaces/[workspaceId]/members/[memberId]/route.ts:84 returns 403 "owner row is immutable", app/api/workspaces/[workspaceId]/members/route.ts:93-101 returns 409 `owner_removal_forbidden`, and neon/migrations/0005_owner_removal_guard.sql:31-33 backstops it in the database (its own comment: "Ownership transfer is intentionally still unbuilt"). No route promotes an accepted member to owner (lib/repositories/membership.ts:74-75 explicitly excludes `role<>'owner'`), and the existing staff path (lib/workspace/access-request.ts:29) only files a request against a scan slug, so it cannot serve a handover. The team page says nothing about any of this — the owner row simply has blank cells.

**Owner impact**

If the person who claimed the workspace with their Google account leaves the business, changes email, or was the wrong colleague, the workspace is stuck with them as sole owner forever: nobody else can invite, manage billing, or approve at owner level, the owner cannot step down, and the UI offers neither an explanation nor a route to Fimmick. The team surface silently dead-ends on the one membership change a small business most often needs.

**Verifier**

CONFIRMED. The owner's path ends at components/workspace/team-view.tsx:57 — `{owner && <TableCell>{editable && <RemoveMemberButton …/>}</TableCell>}` with `editable = owner && member.role !== "owner"` (line 49) — so the owner's own row (sorted to the top by lib/workspace/team.ts:52) renders a blank Actions cell, a static role Badge (line 53) and static scope text (line 54), with no message anywhere on the page explaining why. The owner is left unable to hand ownership to anyone or leave the workspace: no route writes role='owner' onto an existing member (lib/repositories/membership.ts update() is `… AND role<>'owner'` and its type only accepts manager|viewer), PATCH members/[memberId] returns 403 "owner row is immutable" (route.ts:84), DELETE members returns 409 owner_removal_forbidden (route.ts:93-101), and neon/migrations/0005_owner_removal_guard.sql backstops it in the database. lib/workspace/access-request.ts:22-29 cannot serve as a handover — it returns false when hasWorkspace and needs a scan slug, i.e. it is for users with no workspace.

Refutation grounds tested and rejected. (a) No control elsewhere: Team is the only membership surface (more-view.tsx links to it and nothing else); the OAuth claim mints a first owner, it does not transfer one. (b) The "documented deliberate" ground is the strongest and still fails: the route/migration comments document the server refusal as intentional but frame the capability as pending — "a separate, not-yet-built concern", "intentionally still unbuilt … until that ships" — and they are internal comments; nothing in CLAUDE.md or any comment records a decision to leave the owner-facing surface silent, and the page's only caption (team-view.tsx:43) is about consultant/branch-manager roles, not ownership. (c) Not among the already-fixed findings: PHASE-2-GAP-REGISTER.md and IMPLEMENTATION-TRACEABILITY.md log only the removal guard that created this state. (d) Not forbidden by the hard constraints: no migration is required — the 0005 trigger fires on DELETE only and workspace_members_one_owner_idx (0002_business.sql:696) tolerates a sequential demote→promote inside one transaction — and the minimum fix (explanatory copy plus the getMarketCtas contact CTA already shipped at components/onboarding-page.tsx:247) needs no email, OAuth, Stripe or schema change. Guardrail 15 and OWNER_SELF_SERVICE_CLAIM govern how a workspace gets its FIRST owner from an unauthenticated scan, not a proven owner handing the role to a member they already invited, though that tension is worth naming in the fix.

Two corrections to the claim as written. Impact is overstated: per team-view.tsx:56 and §3.9 managers keep draft/approve/export/rescan, so the workspace is not bricked — what is lost is every owner-only setting (team membership, billing/Stripe portal, brand, integrations) plus the owner's own exit, and Fimmick can still repair it out of band. Hence severity medium, not high. Classification dead_end stands (I am leaving it unchanged): missing_control is an arguable alternative, but the defect is not merely an absent feature — the product's one membership-management table is presented with an Actions column and per-row controls and then silently inerts the top row, so the owner following the surface the product itself offers stops at a blank cell with no explanation and no onward route, the same shape as already-fixed finding 15 (onboarding staff fallback that "simply stopped" with no href, number or address).

#### 39. On mobile, Assets and Calendar have no entry point anywhere in the workspace

**Surface:** Settings: notifications and More · **Type:** `dead_end`

**Evidence**

The sidebar that holds the seven secondary destinations is desktop-only: `<Sidebar className="workspace-sidebar hidden md:flex">` (`components/product-ui.tsx:380`), listing Assets and Calendar at `:306-307`. The mobile bottom nav renders only the four primary items plus More (`components/product-ui.tsx:464-472`). The More page — whose own description says secondary settings "stay out of the five-item primary mobile navigation" (`components/workspace/more-view.tsx:29`) — links to Locations, Activity, Brand, Integrations, Team, Notifications and Billing (`:10-18`) and omits Assets and Calendar. A repo-wide search for links to those routes finds only the desktop sidebar entries and one contextual link from an action detail page (`components/workspace/action-detail-client.tsx:502`).

**Owner impact**

An HK/TW merchant on a phone cannot reach the Calendar at all, and can reach Assets only by first opening an action that happens to need one. That blocks the Draft step of the loop for `social-post`, which requires an approved asset or an explicit text-only choice: with no route to Assets, the owner cannot upload a photo or confirm its rights, and the action stays in `needs_input` with no in-product way out. The page whose job is "finds everything else" is missing two of the seven things there are to find.

**Verifier**

Verified in code. The seven "Manage" destinations including Assets and Calendar exist only in the desktop-only sidebar (components/product-ui.tsx:380 `hidden md:flex`; confirmed collapsible="none" renders a plain div carrying that class at components/ui/sidebar.tsx:168-181, and no ramp-refresh.css/globals.css rule re-displays .workspace-sidebar on small screens). The mobile bottom nav renders only the four primaryWorkspaceNav items plus More (components/product-ui.tsx:464-473), and WorkspaceShell's topbar (:446-462) has no hamburger or Sheet — the only nav Sheet in the file (:270-285) is PublicHeader's, for public pages. The owner's mobile path therefore ends at components/workspace/more-view.tsx:11-19, whose seven links are Locations, Activity, Brand, Integrations, Team, Notifications and Billing: Assets and Calendar are absent, and a repo-wide grep for /assets|/calendar over **/*.{ts,tsx} finds no other link to either except action-detail-client.tsx:502. Calendar has zero non-sidebar entry points, so on a phone it is reachable only by typing the URL. Not documented anywhere: more-view.tsx:30's description explains only why secondary items stay out of the five-item bottom nav, and CLAUDE.md §3.1 says merely "static links". Buildable with no migration, cron, provider or publishing dependency — two entries added to the more-view links array. Severity corrected from the claim's framing: the asserted Draft-step block is refuted. action-detail-client.tsx:496-503 renders the asset_or_text_only input with the "Go to assets" link at :502 fired exactly when approvedAssets.length === 0 (the stuck case), and :500 offers an explicit "text_only" option, so social-post never strands in needs_input. Calendar's key datum (rescan cadence) is also mirrored on Home at components/workspace/home-brief.tsx:147. Real impact is narrower: a mobile owner cannot proactively reach Assets to upload or confirm rights outside an action that happens to need one, and cannot reach Calendar at all.

#### 40. An in-flight rescan has no entry point in the workspace, and nothing stops the owner queuing a second one

**Surface:** Rescan and measurement · **Type:** `missing_control`

**Evidence**

The only link to the progress page in the whole workspace is the transient `router.push` in components/workspace/rescan-button.tsx:53 (grep for "scanning/" finds just that and components/scan-page.tsx:227). getHomeBrief reads snapshots, diffs, actions, measurements, drafts and schedules but never a non-terminal job (lib/workspace/queries-pages.ts:386-452), so Home shows only the previous scan's date. enqueueRescan rebuilds from `latestFinishedJob` and has no in-flight guard (lib/repositories/rescan.ts:17-21, lib/workspace/rescan.ts:130-139). With no cron or reaper in this repo, a queued job that nobody POSTs /api/scan/process for never starts, and a job stalled mid-collection can only be resumed from the scanning page's Resume button (components/scanning-page.tsx:251-255) — the page the owner can no longer reach.

**Owner impact**

An owner who reloads or closes the tab during a rescan (likely, given the blocking button above) has no way back to the running scan and no sign one is running. Their only visible control is "Rescan" again, which queues a second job of the same merchant, burns another of the 3/day workspace budget and another round of paid provider calls — while the first job sits queued or collecting with nothing to start or reap it.

**Verifier**

CONFIRMED. The owner's path ends at components/workspace/rescan-button.tsx:53 — `router.push(`/${locale}/scanning/${encodeURIComponent(result.data.jobId)}`)` is the sole navigation into the progress page anywhere in the workspace, and it is transient. Verified by reading the files: a repo-wide grep for "scanning/" yields only that line, components/scan-page.tsx:227 (the public funnel) and app/robots.ts:15, which disallows the path; there is no href/Link to it, and the jobId is never rendered or stored outside that push. Once the owner reloads or closes the tab they cannot get back and cannot see that a scan is running: getHomeBrief (lib/workspace/queries-pages.ts:386) reads snapshots, diff, action rows, measurements, draft versions, completed actions, schedules, integrations and evidence — all of which only exist after a job reaches done|partial — so a queued/collecting job is structurally invisible. components/workspace/activity-view.tsx (47 lines, no Link/href) renders the `scan.queued` audit row as dead text, and lib/workspace/queries.ts:255 `latestWorkspaceReport`, the one function reading audit_jobs.status, is dead code called only by its tests. Nothing guards a repeat: lib/repositories/rescan.ts:20 filters `status IN ('done','partial')`, so an in-flight sibling cannot be seen by the enqueue path, and app/api/workspaces/[workspaceId]/rescan/route.ts gates only on body, consent, authorization, tier and enforceRateLimit (line 68, 3/day per workspace) — so up to three concurrent jobs for the same merchant can be queued, each burning workspace budget and paid provider calls. There is no reaper: vercel.json contains only a git.deploymentEnabled block (no crons), `find app/api -d -name "cron*"` returns nothing, and the only resume control is components/scanning-page.tsx:251/350 on the page the owner can no longer reach, so a job stalled mid-collection stays stalled. Refutation paths checked and rejected: the behaviour is documented nowhere as deliberate (calendar-view.tsx:16 documents "no cron", not "no way back"); IMPLEMENTATION-TRACEABILITY.md:40-41 covers only the rescan *consent* fix and the scanning page's own stall/resume hardening, not the entry point or a duplicate guard; and the fix is buildable here — surfacing the latest non-terminal audit_jobs row on Home with a link to /scanning/[jobId], plus a 409 when one exists — requiring no cron, no scheduler, no migration and no paid provider. One precision on the claim: the 3/day limiter does cap repeats at three, so "nothing stops a second one" is accurate only in the sense that there is no in-flight guard; and the "never starts" case needs the owner to leave before the scanning page mounts, while the stronger, always-true half is that a stalled job has no reachable resume. Severity medium: wasted paid budget and an invisible/unresumable scan for a paying owner, not data loss or an authorization boundary.

#### 41. Insights — the page for outcome evidence — never shows a single action measurement, though Home sends the owner there for exactly that

**Surface:** Rescan and measurement · **Type:** `dead_end`

**Evidence**

getInsights returns series, trend, aeoTrend, metricCards, ledger and perLocation only; it never reads measurements (lib/workspace/queries-pages.ts:599-647), and InsightsView renders nothing from action_measurements (components/workspace/insights-view.tsx:52-116; its before/after grid at :88-92 is generic per-metric snapshot deltas, always Observed or Unknown). Home's proof card links "Inspect before and after" / 查看行動前後 to that page (components/workspace/home-brief.tsx:138) and the change ledger's "View proof" does the same (:183). The data and a location-scoped query already exist (lib/repositories/workspace-read.ts:121-129) and are used for exactly one row on Home (queries-pages.ts:411) and on one action's detail page (queries-pages.ts:553, components/workspace/action-detail-client.tsx:555). CLAUDE.md §3.1 names `action_measurements` as an Insights data source.

**Owner impact**

After a comparable rescan the owner can see that one measured outcome on Home, or hunt action by action. The page titled "Comparable change and outcome evidence" lists none of them, and the Attributed proof card's own "Inspect before and after" link lands on a page that does not contain that measurement. The owner cannot answer "which of the things I approved and exported actually moved?" in one place — the loop's final step.

**Verifier**

CONFIRMED. The owner's path ends at components/workspace/insights-view.tsx:88-92 — the page's only "before and after" grid, fed by model.metricCards, which lib/workspace/queries-pages.ts:566-581 builds from five hard-coded snapshot metric keys (METRIC_CARD_KEYS, :265) with factType restricted to "Observed" or "Unknown" (:577). getInsights (lib/workspace/queries-pages.ts:599-647) never calls repository.measurements; its only extra read is repository.aeoSnapshots (:635). The Insights page composes nothing else (app/[locale]/owner/[workspaceSlug]/insights/page.tsx:15-16). So the owner is left unable to see, on the page titled "Comparable change and outcome evidence" (insights-view.tsx:54), any action_measurements row: not the Attributed fact type, not the window_days, not which approved-and-exported action the movement belongs to. They cannot answer "which of the things I approved and exported actually moved?" anywhere in one place — Home shows exactly one row (repository.measurements(..., 1, ...) at queries-pages.ts:411 -> home-brief.tsx:123-139) and the rest are one-per-page on action detail (queries-pages.ts:553 -> action-detail-client.tsx:555). Dead end, not merely a missing control: Home's Attributed proof card links "Inspect before and after" / 查看行動前後 to that page (home-brief.tsx:138) and the ledger's "View proof" does the same (:183). For 7 of the 12 metrics in TEMPLATE_METRIC (lib/workspace/measurements.ts:25-38) — gbp.reviews_count, gbp.hours_complete, gbp.photos_count, gbp.days_since_last_review, ig.followers, ig.highlights_count, aeo.best_organic_rank — the destination contains no card for that metric at all; for the five that overlap it shows a generic snapshot delta labelled Observed, never the action's Attributed measurement. Refutation checks all fail: no other page, dialog, shell or settings tab renders measurements (only action-detail-client.tsx:555 in the whole components tree); it is not one of the 24 register findings (the nearest, #17 measurement_state on untouched actions and #24 Home's workspace-wide proof card, are both listed fixed and are different defects); and it is not deliberate — lib/workspace/measurements.ts:181-183 asserts the opposite intent, "Every measurement row is still INSERTED, so Insights keeps the honest Observed evidence", which the Insights reader never fulfils, while CLAUDE.md §3.1 names action_measurements an Insights data source. Not forbidden by any hard constraint: action_measurements and a location-scoped read already exist (lib/repositories/workspace-read.ts:121-129, deliberately scoped on the after-snapshot's location so workspace-wide actions survive), so no migration, no cron, no agent_runs write, no publishing connector and no provider/deploy authorization is needed to render them.

#### 42. A second location can never be named or renamed — the only name field on screen belongs to the first shop and is discarded

**Surface:** Multi-location · **Type:** `missing_control`

**Evidence**

lib/workspace/claim.ts:206 sets a non-primary location's name from `job.business_name`; the owner's typed value is only a fallback when the scan has no name. lib/workspace/claim.test.ts:337-341 asserts exactly this ("the client's `primaryLocation` is not an answer about this location at all"). The onboarding form offers one location field, labelled "Primary location" (components/onboarding-page.tsx:264), prefilled at :136 from the FIRST shop's saved name, and posted as `primary_location` at :193. On a second claim `target` is null (lib/workspace/claim.ts:197), so lib/workspace/claim.ts:219 `updateLocation` never runs either — the typed name and address write nothing at all, to either shop. The only writers of `locations.name` are lib/repositories/claims.ts:146 and :150, both reachable only from the claim path; no route under app/api/workspaces/[workspaceId]/ touches locations, and lib/repositories/workspace-read.ts:41 exposes locations read-only.

**Owner impact**

Shop B is permanently stuck with whatever Google/the scan called it. Two shops of one brand whose listings share a name render as two identical rows in every place the owner picks a location — components/workspace/location-select.tsx:47 (home, actions, create, assets, insights), components/select-workspace-page.tsx:84, and the asset upload dialog (components/workspace/assets-view.tsx:43) — with only slugs (`kam-man-house`, `kam-man-house-2`, lib/workspace/slug.ts:74) to tell them apart, and slugs are never shown. The owner cannot tell which shop they are approving, exporting or uploading for, and has no control anywhere to fix the name. Half the stated job — add, NAME and work on the second location — has no control at all.

**Verifier**

CONFIRMED. The owner's path ends at lib/workspace/claim.ts:206: for any non-primary location, name = `optionalString(job.business_name) || input.primaryLocation.name.trim()`, so the owner's typed value is reachable only when the scan supplied no name. lib/workspace/claim.ts:198 (`targetIsPrimary = target ? target.is_primary : primary === null`) is false for a non-primary location in every branch — attached non-primary, sameListing miss, and fresh insert alike — so that first write is the only name shop B can ever receive. lib/workspace/claim.test.ts:334-338 pins it verbatim ("the client's `primaryLocation` is not an answer about this location at all").

There is no second path. lib/repositories/claims.ts:146 (updateLocation) and :150 (insertLocation) are the only writers of locations.name — a repo-wide grep for `UPDATE locations`/`INSERT INTO locations` finds nothing else outside test fixtures — and both are reachable only from completeWorkspaceClaim. No `locations` route exists under app/api/workspaces/[workspaceId]/ (full route listing checked); the only two workspace-API fetches in all of components/ are instagram-handle and /api/workspaces/claim; the five settings pages are billing/brand/integrations/notifications/team; lib/repositories/workspace-read.ts:39-42 is SELECT-only.

The owner is left unable to give their second (and every subsequent) shop a name of its own, or to correct the one Google supplied. Because location-select.tsx:47, select-workspace-page.tsx:~75 and the assets upload dialog (assets-view.tsx:43) all render `name` alone — no address, district or slug — two branches sharing one Google listing name become indistinguishable rows in every location picker, with no in-product control to fix it.

Refutations attempted and failed: (1) No control elsewhere — routes, settings pages, dialogs and the shell were all searched. (2) "Deliberate and documented" is the strongest attempt and is only half right: claim.ts:200-204 and gap-register finding 9 deliberately source non-primary names from job evidence, but that documents a SOURCING decision at creation ("onboarding only asks for it while there is none"), never that shop B should be permanently unnameable; register line 89 ("fixed before multi-location makes it visible — which finding 9 now allows") shows multi-location is treated as newly opened. (3) Not a duplicate of finding 9, which concerned a second claim OVERWRITING shop A's identity; this is the residue that fix created and does not address. (4) Not forbidden: no cron, no agent_runs write, no publishing, no OWNER_SELF_SERVICE_CLAIM, and no migration — locations.name/address already exist in the frozen catalog, so a rename is an UPDATE on existing columns.

Severity medium rather than high, on likelihood and blast radius, not on the control's absence. Reaching a second location requires a job pre-attached by staff assignment (or the default-off WORKSPACE_CLAIM_VIA_OAUTH_ENABLED), i.e. a human Fimmick step — though this is genuinely the shipped default path, which onboarding-page.tsx:239-246 itself calls "the path most owners actually see" and actively instructs owners to request, so it needs nothing from the authorization-gated list. Nothing is corrupted, misattributed or wrongly charged: the stored name is a true fact (the Google listing name), just not the owner's chosen label, and the dangerous ambiguity needs two branches to share an identical listing name. One correction to the claim's framing: the PRIMARY location can be renamed by re-running onboarding on its own slug (targetIsPrimary true, updateLocation:219) — which supports rather than contradicts the headline that the only name field on screen belongs to the first shop.

#### 43. No control anywhere inside the workspace to add a second location

**Surface:** Multi-location · **Type:** `missing_control`

**Evidence**

The workspace shell's only outward link is the switcher to the picker (components/product-ui.tsx:386); its nav is Home/Actions/Create/Insights plus settings (components/product-ui.tsx:394-400+) and contains no link to /scan or /owner/onboarding — the two `/${locale}/scan` links in that file (:264, :276) are in PublicHeader, which the workspace shell does not render. More's "Locations — N locations and comparison" row (components/workspace/more-view.tsx:12) points at /owner/select-workspace, which only enumerates memberships and existing locations (components/select-workspace-page.tsx:66) and shows the "Start with a free scan" CTA solely in the zero-membership empty state (:55). The destination that would work already exists and already says the right thing for a returning owner — components/onboarding-page.tsx:228 ("A workspace starts from a scan report. Run a free scan… then continue the claim from the report page") — but nothing in /owner/** links to it.

**Owner impact**

A merchant who opens their second shop has no way to discover how to add it: the one screen named "Locations" lists what exists and offers nothing. They must know, unprompted, to leave the workspace, run the public 4-step wizard anonymously, wait out the scan, and find the claim CTA on the report. Adding the signpost is a link plus copy; the ownership attach behind it stays on the existing gated path (app/api/oauth/google/claim/callback/route.ts:70 requires WORKSPACE_CLAIM_VIA_OAUTH_ENABLED) or Fimmick staff assignment, so the CTA must state that rather than promise self-service.

**Verifier**

CONFIRMED. The owner's path ends at components/select-workspace-page.tsx:59-80, reached from components/workspace/more-view.tsx:12 (the row literally named "Locations", detail "N locations and comparison", href `/${locale}/owner/select-workspace`). That page enumerates only existing workspace and location cards; its sole `/${locale}/scan` CTA sits at :55 inside the `cards.length === 0` branch opened at :48, so any owner who already has a workspace never sees it. The owner is left unable to discover that a second shop is added by leaving the workspace, running the public 4-step scan anonymously, unlocking the report and taking the claim hand-off.

Independently verified, not just accepted: components/product-ui.tsx:293-303 and :305-313 are the shell's complete nav arrays (Home/Actions/Create/Insights; Assets/Calendar/Activity/Brand/Integrations/Team/Billing), :386 is its only outward link (the switcher), and the mobile bottom nav at :464-473 repeats the same set. The two `/${locale}/scan` links at :264 and :276 are inside PublicHeader, which WorkspaceShell/WorkspacePageFrame never render. A grep of every href in components/workspace/** resolves to `${base}`, `/${locale}/pricing` (billing-view.tsx:93), `/api/oauth/google/start`, or action/tab hrefs — no /scan, no /owner/onboarding. There is no route or endpoint either: app/[locale]/owner/[workspaceSlug]/** has no locations page and app/api/workspaces/** has no locations route; `insertLocation` has exactly one production caller, lib/workspace/claim.ts:222, reachable only through POST /api/workspaces/claim.

Refutation attempts that failed: (a) Not a duplicate of already-fixed finding 9 — that fix (PHASE-2-GAP-REGISTER.md:99-101; lib/workspace/claim.ts:187 case 4, pinned by claim.test.ts:315) made the *data* model able to hold a second location and its own comment at claim.ts:179 says the multi-location model "could never be entered at all" before it; it added no UI entry point. (b) Not documented as deliberate — CLAUDE.md and lib/copy.ts:638/958 say only that the multi-location *plan* is activated by the Fimmick team (billing), and no code comment states that the workspace intentionally offers no path. (c) The destination is live and correct: components/onboarding-page.tsx:228 already carries the returning-owner copy, and components/report/dashboard-summary.tsx:62-70 gates the claim hand-off on `access === "viewer"` ("Members already have a workspace"), which a second shop's unattached job satisfies.

Not forbidden and not needs_authorization: the fix is a link plus copy — no migration (locations exists), no cron, no agent_runs write, no publishing, no OWNER_SELF_SERVICE_CLAIM. The ownership attach behind it stays on the existing gated path (app/api/oauth/google/claim/callback/route.ts:70 `claimViaOAuthEnabled()`) or Fimmick staff assignment, so the CTA must name that requirement instead of promising self-service.

Severity medium rather than high: nothing is corrupted or falsely claimed, the affordance is only undiscoverable, and the completion path is dark in this build anyway (WORKSPACE_CLAIM_VIA_OAUTH_ENABLED unset), which caps today's real-world impact. It is still a genuine misleading affordance, since the one screen named "Locations" promises management and delivers a membership picker.

#### 44. Integrations is hard-wired to the primary location, so shop B's owner is shown shop A's provider health and can never set shop B's Instagram handle

**Surface:** Multi-location · **Type:** `missing_control`

**Evidence**

app/[locale]/owner/[workspaceSlug]/settings/integrations/page.tsx:16 calls `getIntegrations(page.ctx)` and drops `page.locationSlug` entirely. lib/workspace/queries-pages.ts:363-364 then falls back to `ctx.locations.find(l => l.isPrimary)` for the snapshot that drives the Instagram provider badge and the "n of 15 checks passed" website card, and :369 reads the single workspace-level `ctx.workspace.instagramHandle`. The save path writes `workspaces.instagram_handle` and then syncs only `WHERE workspace_id=$1 AND is_primary=true` (lib/repositories/workspace-profile.ts:12; app/api/workspaces/[workspaceId]/instagram-handle/route.ts:50). Home calls the same function with the location-scoped snapshot (lib/workspace/queries-pages.ts:418), so Home under ?location=B and Integrations disagree about the same two cards; and a `google-reconnect` action scoped to location B deep-links here carrying ?location= (components/workspace/actions-list-view.tsx:50), which this page ignores.

**Owner impact**

With two shops, the owner follows "fix your Instagram" from shop B's Home into Integrations and reads shop A's handle, shop A's Instagram state, shop A's website check count and shop A's "last sync" time, with nothing on the page saying which shop it describes. Correcting a wrong or missing handle for shop B is impossible: the single control overwrites the workspace field and shop A's row. `locations.ig_handle` already exists (lib/repositories/claims.ts:150) and is simply never read back (it is absent from LOCATION_COLUMNS, lib/repositories/workspace-read.ts:23), so scoping this page and the handle control needs no schema change.

**Verifier**

CONFIRMED. The owner's path ends at app/[locale]/owner/[workspaceSlug]/settings/integrations/page.tsx:16 — `getIntegrations(page.ctx)` discards the `page.locationSlug` that lib/workspace/page-context.ts:41 just resolved. Every sibling owner page forwards it (page.tsx:22 home, insights/page.tsx:15, create/page.tsx:23, assets/page.tsx:18, actions/page.tsx:22); this one alone does not. lib/workspace/queries-pages.ts:363-364 therefore falls back to `ctx.locations.find(l => l.isPrimary)`, and that primary snapshot feeds the Instagram ProviderBadge (:370-371), the "n of 15 checks passed" string and the "Last sync" timestamp (:374-377). Line :369 reads the single workspace-level handle.

The owner arrives here already scoped: components/product-ui.tsx:310 lists Integrations in the nav and :398/:419/:466 wrap every nav href in `scopedHref` (:332), which appends `?location=<current>`, while :388 and :449 print the selected location name in the shell chrome. components/workspace/actions-list-view.tsx:50 and components/workspace/home-brief.tsx:45 deep-link a `google-reconnect` action through `withLocation`. So with two shops the owner stands under a header reading "Kam Man House · Yik Yam" while the page body shows Tin Hau's Instagram state, Tin Hau's website check count and Tin Hau's observed_at — and components/workspace/integrations-view.tsx renders no location name and no LocationSelect (contrast insights-view.tsx:54), so nothing on the page discloses the substitution. Home contradicts it directly: queries-pages.ts:418 calls the same getIntegrations with the location-scoped snapshot, so home-brief.tsx:165-173 shows shop B's values for the same three cards, and the "Manage" link at :166 lands on the page that flips them to shop A's.

What the owner is left unable to do: view or act on shop B's provider health from the Integrations page at all, and target shop B with the only control on it — InstagramHandleForm posts to /api/workspaces/[workspaceId]/instagram-handle, which writes `workspaces.instagram_handle` (lib/repositories/workspace-profile.ts:9) and then syncs `WHERE workspace_id=$1 AND is_primary=true` (workspace-profile.ts:12, invoked at route.ts:50), so a correction entered under ?location=B lands on shop A's row. `locations.ig_handle` is written per location at lib/repositories/claims.ts:146,150 (whose :148 comment confirms a real non-primary second location) but never read back — it is absent from LOCATION_COLUMNS at lib/repositories/workspace-read.ts:23.

Refutation bases checked and rejected. No other surface offers the control: there is no location settings page (only settings/{brand,integrations,team,billing,notifications}), and onboarding sets the handle once at claim. Not an already-fixed finding: PHASE-2-GAP-REGISTER #8 concerns role gating on this page, and #19/#24 are this exact defect class ("workspace-wide data presented under the selected location") fixed for getHomeBrief only — the fix comment sits at queries-pages.ts:405-414, four lines below the unfixed getIntegrations. Not documented as deliberate: getIntegrations carries no comment defending the primary fallback, and CLAUDE.md §3.1 states location scoping as a general owner-route rule. Buildable within the hard constraints: locations.ig_handle already exists, so no migration; no cron, no agent_runs write, no publishing, no provider/OAuth/Stripe action is needed — passing page.locationSlug through, adding ig_handle to LOCATION_COLUMNS, and labelling the page are pure code changes.

Two honest qualifications, neither refuting. (1) CLAUDE.md §3.1 does name `workspaces.instagram_handle` as this page's handle source and §3.2.3 copies the workspace-level upstream route, so making the handle per-location is a plan amendment; the unambiguous implementation gap is the snapshot scoping plus a location label, with the per-location handle the follow-on question. (2) The owner is not wholly blind to shop B's provider health — Home shows it correctly under ?location=B — which is precisely what makes the two surfaces disagree about the same two cards. Severity medium rather than high: real evidence misattributed across shops under an explicit location scope (guardrail 1 provenance), but no data loss, no authorization breach, and only multi-location workspaces are affected.

#### 45. A lite owner blocked at the delivery allowance has no working upgrade path, and the failure reads as a transient outage

**Surface:** Settings: team and billing · **Type:** `dead_end` · **BLOCKED** (not buildable under the plan's constraints)

**Evidence**

components/workspace/action-detail-client.tsx:542 sends an allowance-blocked owner to `/settings/billing` ("upgrade the plan or wait for next month's allowance"). There, app/api/workspaces/[workspaceId]/checkout-link/route.ts:61-66 returns 500 "Stripe is not configured" whenever the Stripe keys are absent — the state of this deployment, since Stripe is separately authorized and unavailable — and components/workspace/billing-actions.tsx:27-31 maps every error other than `already_paid`/`no_subscription` to "The payment service is unavailable right now; please try again later." The page's only alternative route is a link to /pricing (components/workspace/billing-view.tsx:93).

**Owner impact**

After three approved deliveries in a month, the loop's Export step stops and the single escape the product points at is a button that fails with a message implying it will work if they retry later. Restoring the real path needs Stripe keys, webhook endpoint and price ids, which are outside what can be built here; what is buildable is telling the truth (self-serve subscription not available yet) and routing the owner to the Fimmick market contact instead of an endless retry.

**Verifier**

CONFIRMED. The owner's path ends at components/workspace/billing-actions.tsx:27-31, where every error except already_paid/no_subscription becomes "The payment service is unavailable right now; please try again later." The error it is almost always handling is app/api/workspaces/[workspaceId]/checkout-link/route.ts:61-66, a 500 "Stripe is not configured" returned whenever STRIPE_SECRET_KEY is absent (lib/stripe.ts:3-5) — a permanent state in this deployment, since Stripe setup is an unresolved provider action (docs/integration/DEPLOY.md:107,109) and .env.example:62 ships the key empty. A second permanent 500 exists at route.ts:90-95 for a missing STRIPE_HK/TW_TIER_PRICE_ID, mapped to the same "try again later" sentence.

A lite owner who hits the cap is left unable to (a) export the version they already approved, and (b) take any action that could lift the cap, while being told to retry a button that cannot ever succeed. Nothing upstream warns them: lib/workspace/billing.ts:28-37 defines BillingModel with tier/usage/tierEvents/stripeCustomer/marketPrice and no stripeConfigured flag, so components/workspace/billing-view.tsx:57 renders the Subscribe button for every owner regardless, and the server-rendered page never states the capability is off.

The claimed alternatives do not exist. The blocked banner (components/workspace/action-detail-client.tsx:542, reached via :370 on allowance_exceeded) points only at /settings/billing; billing-view.tsx:93 points only at /pricing, whose Growth CTA is /owner/sign-in?plan=growth (components/public-pages.tsx:98) — a sign-in link for an already-signed-in owner — and whose "Contact Fimmick" (lib/copy.ts:357) is a price label with no href. getMarketCtas is wired into exactly one surface, app/[locale]/owner/onboarding/page.tsx:148; no mailto/wa.me/line.me/contact link exists anywhere under components/workspace/.

Refutation attempts all failed: not among the 24 fixed findings (PHASE-2-GAP-REGISTER.md:17-105 — #2 is mid-period allowance reconciliation, #20 is the 80% warning threshold, neither touches the upgrade control or its copy); not documented as deliberate (CLAUDE.md §3.10 documents the link to billing, not a billing page whose only control 500s; §5 asserts "Subscribe via Stripe", the opposite of what ships); and fixed finding 15 — onboarding's staff-assignment fallback that pointed at an email never sent, repaired by rendering the market's real contact channels resolved server-side — is the direct precedent that the honest-copy-plus-contact fix is legitimate and buildable.

Classification corrected from needs_authorization to dead_end. Restoring real Stripe checkout does require keys, price ids and a webhook endpoint and stays unauthorized, but the remedy the finding itself proposes — state that self-serve subscription is not enabled yet and route the owner to the market contact via the same server-resolved getMarketCtas pattern already shipped in onboarding — needs no authorization; labelling the whole finding needs_authorization risks it being discarded as unbuildable. Severity medium: it stops the loop's Export step with no escape and misleads about why, but only for owners who reach 3 deliveries in a month, and the approved version itself survives.

### Low

#### 46. Activity ignores the selected location and offers no way to see or choose one

**Surface:** Workspace shell and scope · **Type:** `missing_control`

**Evidence**

`app/[locale]/owner/[workspaceSlug]/activity/page.tsx:14-16` resolves `page.locationSlug` through `loadOwnerPage` (`lib/workspace/page-context.ts:41`) and then never uses it — it calls `getActivity(page.ctx)`, which is workspace-wide by signature (`lib/workspace/queries-pages.ts:653-655` → `lib/repositories/workspace-read.ts:157` `activity(workspaceId, limit)`). `components/workspace/activity-view.tsx:19-47` renders no `LocationSelect` (unlike home, actions, insights, create and assets, which all mount it) and its four columns (`:27`) carry no location, even though `audit_events.location_id` is written by the mutation routes (`lib/workspace/audit.ts:57`, `app/api/actions/route.ts:166-167`, `app/api/workspaces/[workspaceId]/assets/route.ts:86-87`, `lib/workspace/rescan.ts:181-182`).

**Owner impact**

On a multi-location workspace the shell keeps showing the chosen location in the switcher subtitle (`components/product-ui.tsx:388`) and every nav link keeps carrying `?location=tin-hau`, yet the Activity table silently mixes both shops' approvals, exports and rescans with no column saying which shop each row belongs to. An owner auditing "what happened at this shop" cannot answer that question from the one append-only surface built for it, and cannot narrow it either.

**Verifier**

CONFIRMED. Verified in the files myself. app/[locale]/owner/[workspaceSlug]/activity/page.tsx:14-16 resolves the scope via loadOwnerPage (lib/workspace/page-context.ts:41 resolveLocationSlug) and then discards it: line 15 is `const events = await getActivity(page.ctx)`. lib/workspace/queries-pages.ts:653-655 forwards to lib/repositories/workspace-read.ts:157-160, whose SQL is `SELECT ... FROM audit_events WHERE workspace_id=$1 ORDER BY created_at DESC LIMIT $2` with no location predicate and no parameter to add one - unlike its immediate neighbours draftVersions/completedActions (workspace-read.ts:134-139), which take locationId and carry the ($n::uuid IS NULL OR location_id=$n OR location_id IS NULL) arm. components/workspace/activity-view.tsx:27 renders exactly four headers (Time, Actor, Event, Detail); the component neither imports LocationSelect nor accepts a locations/location prop, while grep confirms LocationSelect is mounted on home (home-brief.tsx:70), actions (actions-list-view.tsx:86), create (create-view.tsx:118), insights (insights-view.tsx:54) and assets (assets-view.tsx:43). The shell keeps asserting a location: product-ui.tsx:332 scopedHref appends ?location= to every nav link including /activity (:308), and :388 shows the location name as the switcher subtitle. audit_events.location_id exists, is selected (workspace-read.ts:158; AuditEventRow at queries-pages.ts:213) and is written on the action/asset/rescan/run paths (audit.ts:59, app/api/actions/route.ts:167, assets/route.ts:87, rescan.ts:164,182, artifacts.ts:279-281 joining a.location_id).

Refutation grounds checked and rejected: (a) no equivalent control elsewhere - the action-detail history tab (actions/[actionId]/page.tsx:29) is per-action, not per-location; (b) not documented as deliberate - no comment on the page, view, getActivity or activity(), which is conspicuous next to the five-line location_id comment on draftVersions, and CLAUDE.md 3.1 lists Assets as a bare `assets` data source too yet Assets was built with a selector, so the table's omission is not a decision; the prototype's ActivityPage (9b37f41 workspace-operations.tsx:146-155) had no selector, but the current view already dropped that page's export button, so "keep layouts" is not being treated as a prohibition; (c) not already fixed - git log for both files stops at b3c0416 (Phase 4); the recent run fixed only the sibling case on Home (6df503a); (d) not forbidden - a selector, filter argument and location column are read-side only.

Two corrections that lower severity without refuting: (1) neon/migrations/0004_atomic_operations.sql:44, :409 and :490 insert version.approved, version.changes_requested, version.rejected, delivery.exported and delivery.copied WITHOUT location_id (only version.created at :366-367 sets it), so the claim's "mixes both shops' approvals, exports" is wrong about those rows - they carry NULL today, and because 0001-0004 are immutable and the catalog frozen, attributing them needs a read-side join (audit_events.entity_id -> output_versions/deliveries -> actions.location_id), not a migration; (2) activity-view.tsx:26 captions the table "Workspace events, latest first", so the page does not claim location scope - this is a missing filter, not a silent mislabel, the same mitigation that graded the Home analogue low in docs/implementation/owner-platform-v1/PHASE-2-GAP-REGISTER.md:623.

Owner's path ends at app/[locale]/owner/[workspaceSlug]/activity/page.tsx:15 and components/workspace/activity-view.tsx:27: on a multi-location workspace the owner cannot narrow the append-only audit surface to the location the shell says they are in, and cannot tell from any row which location it belongs to. Single-location workspaces are unaffected.

#### 47. Activity's Detail column prints the raw 64-character IP HMAC on every member-initiated row

**Surface:** Calendar and Activity · **Type:** `cosmetic`

**Evidence**

`components/workspace/activity-view.tsx:8-17` flattens the whole payload into the Detail cell, skipping only `locale`, nulls and objects. `lib/workspace/audit.ts:55` builds every payload as `{ locale, ...(ipHash ? { ip_hash } : {}), ...payload }`, so `ip_hash` is the first surviving key and leads the cell. `lib/security/request-fingerprint.ts:32-36` returns a hex SHA-256 HMAC, i.e. 64 characters. Routes supply it throughout — `app/api/actions/[actionId]/route.ts:129`, `app/api/actions/_shared/mutation.ts:134`, `app/api/assistant/run/route.ts:87`, `app/api/workspaces/[workspaceId]/members/[memberId]/route.ts:103` — and `lib/repositories/artifacts.ts:281` writes it directly into run.* payloads. It is persisted verbatim (`lib/repositories/claims.ts:111`).

**Owner impact**

Every draft, approval, export and assistant row opens with 64 characters of opaque hex before the meaningful part (`agent_key`, `version_no`, `action_id`), so the audit table is unreadable at a glance and unusable on mobile. The one column that should explain what happened is dominated by an internal security fingerprint the owner has no use for and cannot interpret.

**Verifier**

CONFIRMED, with the scope narrowed. The owner's path ends at components/workspace/activity-view.tsx:11-14: `for (const [key, value] of Object.entries(payload)) { if (key === "locale" || value === null || value === undefined || typeof value === "object") continue; parts.push(`${key} ${String(value)}`) }`. Because lib/workspace/audit.ts:55 builds every payload as `{ locale, ...(ipHash ? { ip_hash } : {}), ...payload }`, and `locale` is the single key the loop skips, `ip_hash` is the first surviving key and leads the Detail cell. lib/security/request-fingerprint.ts:32-36 returns a hex SHA-256 HMAC (64 chars), null only if RATE_LIMIT_SECRET is unset, which fails closed in production. The read path applies no filter: lib/repositories/workspace-read.ts:158-159 selects `payload` verbatim, getActivity (lib/workspace/queries-pages.ts:653) returns AuditEventRow unchanged, and app/[locale]/owner/[workspaceSlug]/activity/page.tsx:16 hands it straight to ActivityView. The owner is left unable to scan the Detail column for the meaningful fields (agent_key, action_id, from_role, role) on affected rows, because each begins with an unlabelled 64-character hex string that consumes the cell and pushes the substantive part off-screen on mobile.

No control exists elsewhere. The only other renderer of these rows, the action-detail history tab at components/workspace/action-detail-client.tsx:577, reads exactly one payload key (`row.payload?.version_no`) — a selective renderer already exists in this codebase, which makes the Activity page's blanket flatten an outlier rather than a deliberate design. CLAUDE.md section 3.11 documents only that the payload must carry an HMAC ip_hash rather than a raw IP; it does not sanction displaying it, and no code comment defends the flatten. Not among the already-fixed findings: the gap register cites activity-view.tsx:12 only as evidence for the unrelated dropped-`warnings` finding (PHASE-2-GAP-REGISTER.md:414,428). Not forbidden by the hard constraints — the fix is a key allowlist/denylist in the UI layer, requiring no migration, scheduler, agent_runs write, publishing connector or paid provider.

Scope correction to the claim: "every member-initiated row" and specifically "approval and export rows" is wrong. version.created, version.approved, delivery.exported and delivery.copied are written by the SQL RPCs at neon/migrations/0004_atomic_operations.sql:43-46, 366-369 and 490-496, whose jsonb_build_object payloads contain neither locale nor ip_hash, so those rows correctly lead with version_no. The rows that do lead with the hash are run.started/succeeded/failed (lib/repositories/artifacts.ts:281), assistant.run (app/api/assistant/run/route.ts:87), action.updated/dismissed (app/api/actions/[actionId]/route.ts:129), action creation (app/api/actions/route.ts:174,190), asset.uploaded/rights_confirmed, brand.updated, scan.queued and member.invited/role_changed (app/api/workspaces/[workspaceId]/members/[memberId]/route.ts:103). That is still about half the event vocabulary and the highest-frequency member rows, so the defect is real.

Classification cosmetic is correct and severity is low: nothing the owner needs to do is blocked and no decision is made wrong; the value is a one-way HMAC, not a raw IP, so this is table readability rather than a PII leak.

#### 48. The rescan cadence, once created, can never be paused or removed by the owner

**Surface:** Calendar and Activity · **Type:** `missing_control`

**Evidence**

`scan_schedules` is INSERT-only in this app: `lib/repositories/rescan.ts:24-25` is the sole writer and there is no `UPDATE scan_schedules` or `DELETE FROM scan_schedules` anywhere outside the schema file. `lib/workspace/rescan.ts:211-225` (`ensureMonthlySchedule`) creates the row on the first paid rescan and returns `{ created: false, reason: "exists" }` forever after. `components/workspace/calendar-view.tsx:30` renders a `cadence === "paused"` branch for a state this app can never produce, and the card's only badge is the non-interactive "You run these" at `:27`.

**Owner impact**

The Calendar's remaining card is read-only. An owner who no longer wants a monthly rhythm — or who wants to move it off the anniversary day — has no control on the surface that shows it, so the reminder nags indefinitely. Note that actually dispatching the cadence is forbidden here (no crons, legacy scheduler disabled), but pausing or clearing the reminder the page itself shows is not.

**Verifier**

CONFIRMED. The owner's path ends at components/workspace/calendar-view.tsx:27 (the card's only badge is a non-interactive Badge variant="outline" reading "You run these") and :30 (the only interactive element per cadence row is a Link to the Insights page). The owner is left unable to pause, re-date, or delete a scan_schedules row that their own first "Rescan now" silently created.

Verified independently:
- lib/repositories/rescan.ts:6-11 — the RescanRepository interface declares only tier, latestFinishedJob, scheduleExists, insertSchedule. No update/delete method exists in the contract at all. :23-26 is the sole INSERT.
- Repo-wide search for UPDATE/DELETE on scan_schedules outside neon/migrations and supabase/migrations returns only test/integration/neon-rescan.integration.test.ts:15 (teardown). No production writer besides the insert.
- app/api/workspaces/[workspaceId]/ holds 12 routes (actions, assets, billing-portal, brand, checkout-link, fix-pack-drafts, google-connection, instagram-handle, members, notification-preferences, rescan, usage) — there is no schedules route.
- app/api/workspaces/[workspaceId]/rescan/route.ts:86 invokes ensureMonthlySchedule as a silent side effect; the owner never opts in. lib/workspace/rescan.ts:218 and :222 then return {created:false, reason:"exists"} permanently.

Refutation paths all fail:
- Control elsewhere: none exists on any page, dialog, shell or settings tab.
- Deliberate/documented: the comment at calendar-view.tsx:11-19 documents only that DISPATCH is absent and forbidden; it never states the row is intentionally immutable. Upstream's own documented off-switch (supabase/migrations/20260816000000_scan_schedules.sql:18, "a paused schedule is the off switch, and staff delete the row") is unreachable here because CLAUDE.md 3.9 states staff sessions are not accepted in this app and the legacy staff console runs against Supabase, not this Neon database. No actor of any kind can pause or remove the row.
- Already fixed: gap register #13 is a distinct finding about the false dispatch promise, resolved by copy changes ("a reminder, not an automatic schedule"). It added no owner control over the row.
- Forbidden by plan: no. Pausing requires no cron and no second scheduler (it is the inverse of dispatch). No migration is needed — cadence already accepts 'paused' (neon/migrations/0002_business.sql:571) and sme_app_runtime already has UPDATE, DELETE (neon/migrations/0003_workflows.sql:138) — so the frozen catalog is untouched and db:verify still passes. No paid provider, email, OAuth, Stripe or deployment is involved.

Severity corrected from the implied high/medium to low. The "nags indefinitely" framing overstates the impact: tests/unhonoured-promises.test.ts:126-131 establishes no mail library is installed, so nothing pushes a reminder; the cadence is static text on Calendar and Home, explicitly labeled "You run these" and "a reminder, not an automatic schedule". Nothing is blocked, charged, or at risk. It is still real persisted state the owner created implicitly and can never undo or re-date, so it is a genuine missing control rather than cosmetic.

#### 49. The More page still advertises "quiet hours", a control that does not exist

**Surface:** Settings: notifications and More · **Type:** `cosmetic`

**Evidence**

`components/workspace/more-view.tsx:17` describes the Notifications destination as "Consent and quiet hours" / "同意及靜音時段". The string "quiet hour"/"靜音" appears nowhere else in the repo — no column, no field, no UI. The notifications page's own intro was already corrected for exactly this (`components/workspace/notifications-view.tsx:21-23` plus the comment at `:20` noting the description "used to advertise location, channel and quiet-hour controls"), but the link that sends the owner there was not.

**Owner impact**

The owner navigates to Notifications expecting to set quiet hours and finds three switches for a channel that is off. Minor on its own, but it is the same false promise that was fixed one file away, so the two surfaces now contradict each other.

**Verifier**

CONFIRMED. The owner's path ends at components/workspace/more-view.tsx:17, which renders the Notifications destination as "Consent and quiet hours" / "同意及靜音時段" (rendered at :31, mounted on the live route app/[locale]/owner/[workspaceSlug]/more/page.tsx:14). Following that link, the owner is left unable to set quiet hours, or any timing/schedule control, on notifications: the destination offers only three category switches — rescanComplete, regressionAlert, monthlyDigest (components/workspace/notifications-client.tsx:47-51) — plus a read-only in-app list. A repo-wide case-insensitive grep for quiet|靜音 returns only this string and the corrective comment at notifications-view.tsx:22-24; every other hit is the unrelated adverb "quietly". No quiet-hours field exists in the form, the PATCH route, or lib/db/schema/business.ts. Not already fixed: gap-register finding #14 (docs/implementation/owner-platform-v1/PHASE-2-GAP-REGISTER.md:63) removed this exact phrase from the notifications page's own PageIntro (fix visible at notifications-view.tsx:25) but never touched more-view.tsx, so this is unfixed residue of that finding rather than a duplicate of it. Not forbidden: the remedy is a one-line copy edit — no migration, cron, provider, or schema change — and no test pins the string (grep over **/*.test.* for more-view, "Consent and quiet", 同意及靜音 returns nothing). Classification cosmetic is correct and should not be upgraded to missing_control: quiet hours appears nowhere in CLAUDE.md §3.1, which scopes the page to workspaces.notify_* plus workspace_notifications, so the fix is deleting the claim, not building the feature; it is not a dead_end because the link resolves to a working page. Severity low: no capability is lost and the owner lands on the correct page, but the false promise now contradicts the corrected sibling surface. Note the "Consent" half is accurate (the destination eyebrow is "Consent-led alerts" / "以同意為本的提示"), so only the quiet-hours clause needs removal.

#### 50. Calendar tells the owner to use a "Rescan now" control on Insights that does not exist there

**Surface:** Rescan and measurement · **Type:** `cosmetic`

**Evidence**

components/workspace/calendar-view.tsx:33: "This is a reminder, not an automatic schedule: a due date never starts a scan on its own. Use “Rescan now” on Home or Insights." RescanButton is mounted in exactly one place — components/workspace/home-brief.tsx:70 (grep for RescanButton finds only that import site plus tests) — and components/workspace/insights-view.tsx has no rescan control at all. The Calendar's own per-location rows link to Insights (calendar-view.tsx:30), reinforcing the wrong destination.

**Owner impact**

The cadence card is where an owner goes on their monthly anniversary day, having been told nothing dispatches it. It sends them to Insights, which offers no way to start the scan; they have to find Home on their own.

**Verifier**

CONFIRMED. The owner's path ends at components/workspace/insights-view.tsx — the whole 117-line file contains zero matches for "rescan"/"Rescan", and its PageIntro actions slot (insights-view.tsx:54) holds only a LocationSelect. An owner who follows the Calendar's instruction lands on Insights and is unable to start the rescan that note just told them to start there; they must navigate to Home instead.

Evidence verified first-hand: (1) components/workspace/calendar-view.tsx:33 renders the limitation-note en "This is a reminder, not an automatic schedule: a due date never starts a scan on its own. Use “Rescan now” on Home or Insights." and zh "請在首頁或成效頁按「立即重新掃描」". (2) RescanButton is mounted in exactly one place, components/workspace/home-brief.tsx:70 (import at :11); a repo-wide grep returns only rescan-button.tsx:23 (definition), that one import site, and tests/phase6-ui.test.tsx. No file under app/ mounts it. (3) app/[locale]/owner/[workspaceSlug]/insights/page.tsx renders InsightsView and nothing else, so there is no page-level rescan control either. (4) calendar-view.tsx:30 gives each cadence row a single link to `${base}/insights`, so the row's only affordance lands on the page without the control.

Refutation attempts that failed: the control does not exist on another page, in a dialog, in the shell, or in a settings tab — the all-.tsx grep for rescan/Rescan surfaces only rescan-button.tsx, home-brief.tsx, calendar-view.tsx, notifications-client.tsx (email prefs), billing-view.tsx and landing/public copy, none of which puts a control on Insights. The behaviour is not documented as deliberate: the docstring at calendar-view.tsx:11-19 documents only the absence of a dispatcher ("A rescan happens when the owner starts one") and never mentions Insights, while CLAUDE.md §3.1 assigns Insights to snapshots/trends and Phase 6 places the rescan control on Home. It is not one of the already-fixed findings: PHASE-2-GAP-REGISTER.md:459 finding #13 prescribed relabelling the Calendar as "Rescan cadence (run manually)", and that fix landed (the "You run these" badge and this very note are its output) — this is a residual inaccuracy introduced by that fix, not the fix itself. It is not forbidden by the hard constraints: the remedy is a copy edit (name Home only, match the real label) plus optionally re-pointing the row link, requiring no cron, no migration, no agent_runs write, no publishing connector and no paid provider.

Two corrections to the finding as filed. First, strengthening it: the button's actual label is "Rescan" / "重新掃描" (rescan-button.tsx:101, :118, :135); "Rescan now" / "立即重新掃描" is the consent-dialog confirm text (:114, :131, :148), so the note names both a page that lacks the control and a label that appears on no button. Second, softening the claimed impact: the owner is not left to "find Home on their own" — Home is named first in the same sentence and does work, so this is a wayfinding-copy inaccuracy rather than a true dead end. Classification cosmetic is correct; severity low, since no control is missing, no mutation is blocked, and a correct destination is named in the same string.

#### 51. Completing the second claim drops the owner on the first shop, with nothing confirming the new location exists

**Surface:** Multi-location · **Type:** `missing_control`

**Evidence**

app/api/workspaces/claim/route.ts:53 returns `{ ok, workspaceSlug, locationId }`, but components/onboarding-page.tsx:206-207 reads only the workspace slug and pushes `/${locale}/owner/${slug}` with no `?location=`. lib/workspace/page-context.ts:41 then resolves the scope through lib/workspace/shell.ts:58-59, which defaults to the primary location — shop A.

**Owner impact**

After spending a scan and an ownership verification on shop B, the owner lands on shop A's Home: shop A's score, shop A's actions, shop A's next scan. Nothing states that a location was created, and combined with the naming gap above they may not recognise it in the picker either, so the natural read is that the claim failed and the obvious next move is to run it again. The fix is to return the new location's slug and push `?location=<slug>` (and say which shop was added); no schema or provider work involved.

**Verifier**

CONFIRMED. The owner's path ends at components/onboarding-page.tsx:207 — `router.push(slug ? `/${locale}/owner/${slug}` : `/${locale}/owner/select-workspace`)` — which discards the `locationId` that app/api/workspaces/claim/route.ts:53 just returned and pushes with no `?location=`. lib/workspace/page-context.ts:41 then calls resolveLocationSlug(undefined, ...) and lib/workspace/shell.ts:58-59 falls back to the primary location, i.e. shop A.

The scenario is real and newly reachable, not hypothetical. app/api/oauth/google/claim/callback/route.ts:128-143 reuses the caller's existing workspace (`existing != null ? { id: existing.workspaceId } : createWorkspaceWithOwner(...)`), so a second job attaches to shop A's workspace. lib/workspace/claim.ts:194-224 case 4 (target null, differing place_id) then calls insertLocation with `is_primary: primary === null` = false, creating shop B as a non-primary location. app/[locale]/owner/onboarding/page.tsx:141 (`resumeStep = !owned ? 1 : saved?.hasLocation ? 4 : 3`) sends the second claim straight to step 4, so the owner presses "Open workspace" and completeWorkspace() runs.

Refutation checks all came back negative. This is NOT one of the 24 fixed findings: register finding 9 (lines 352-378, fixed entry at 99-101) was scoped to the identity OVERWRITE; its fix is precisely what made insertLocation reachable, and neither the fix entry nor claim.ts's comment addresses the redirect. It is not documented as deliberate: CLAUDE.md §3.1 documents the primary-location default for an absent param but says nothing about where a claim lands. No confirmation control exists on the landing page — grep for claimed=/toast/success across app/[locale]/owner/[workspaceSlug] returned nothing. Not forbidden by the hard constraints: the slug is already in scope at claim.ts:221 and needs only to be returned and appended as ?location=; no migration, provider, cron, or agent_runs write.

What the owner is left unable to do: at the end of a claim on shop B they cannot tell from the destination that the claim succeeded or that a location was created — they get shop A's score, actions and next scan, and the scope the operation just produced is thrown away.

Two corrections to the stated impact, which is why I set severity to low rather than higher. (1) Recovery is not absent: buildShellWorkspace (shell.ts:94) maps every location into the shell picker, and a non-primary location takes its name from job.business_name (claim.ts:206), which /api/scan/start requires — so shop B is visible under its own name one click away. The "may not recognise it in the picker" half of the impact only holds if business_name is null, which that route forbids. (2) The claimed "natural next move is to run it again" is harmless, not destructive: claim.ts:225 attaches location_id to the job, so a re-run takes the `attached` branch and updates rather than duplicating. Likelihood is also low on the same grounds the register used to lower finding 9 — the OAuth claim flow is dark at both gates (WORKSPACE_CLAIM_VIA_OAUTH_ENABLED and GOOGLE_OAUTH_CLAIM_REDIRECT_URI), leaving staff assignment as the live trigger. Classification held at missing_control: a state-changing operation returns the id of what it created and the client drops it, with no confirmation in its place.
