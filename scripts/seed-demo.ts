/** Task15 owns the Neon demo fixture rewrite. Keep this entrypoint inert until then. */
throw new Error(
  "demo_seed_neon_migration_pending: use owned integration fixtures; Task15 must port demo seeding before release",
);
